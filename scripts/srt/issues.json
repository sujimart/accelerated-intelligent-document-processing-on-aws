[
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/discovery/discovery_agent.py",
    "line": 294,
    "issue": "By default, jinja2 sets autoescape to False. Consider using autoescape=True or use the select_autoescape function to mitigate XSS vulnerabilities.",
    "fix": "Enable Django's CSRF protection. Use `@csrf_protect` decorator and include `{% csrf_token %}` in forms. Don't use `@csrf_exempt` unnecessarily.",
    "check_id": "B701",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b701_jinja2_autoescape_false.html",
    "status": "resolved",
    "firstDetectedAt": "2026-04-27T18:09:25.686Z",
    "assessmentCount": 4,
    "resolvedAt": "2026-04-30T20:15:11.405Z"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/discovery/discovery_agent.py",
    "line": 564,
    "issue": "By default, jinja2 sets autoescape to False. Consider using autoescape=True or use the select_autoescape function to mitigate XSS vulnerabilities.",
    "fix": "Enable Django's CSRF protection. Use `@csrf_protect` decorator and include `{% csrf_token %}` in forms. Don't use `@csrf_exempt` unnecessarily.",
    "check_id": "B701",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b701_jinja2_autoescape_false.html",
    "status": "resolved",
    "firstDetectedAt": "2026-04-27T18:09:25.686Z",
    "assessmentCount": 4,
    "resolvedAt": "2026-04-30T20:15:11.405Z"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/conftest.py",
    "line": 46,
    "issue": "Possible hardcoded password: 'testing'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.686Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/conftest.py",
    "line": 47,
    "issue": "Possible hardcoded password: 'testing'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.686Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/conftest.py",
    "line": 48,
    "issue": "Possible hardcoded password: 'testing'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.686Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/agents/test_common_config.py",
    "line": 77,
    "issue": "Possible hardcoded password: 'test_secret'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.688Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_mcp_connector_pkg/idp_mcp_connector/__main__.py",
    "line": 37,
    "issue": "Possible hardcoded password: 'MCPTokenURL'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.850Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: flagged strings are CloudFormation output *names* ('MCPTokenURL', 'MCPConnectorClientSecret') used to look up env vars; no secret value is hardcoded."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_mcp_connector_pkg/idp_mcp_connector/__main__.py",
    "line": 39,
    "issue": "Possible hardcoded password: 'MCPConnectorClientSecret'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.850Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: flagged strings are CloudFormation output *names* ('MCPTokenURL', 'MCPConnectorClientSecret') used to look up env vars; no secret value is hardcoded."
  },
  {
    "source": "Bandit",
    "path": "notebooks/examples/step3_extraction_using_yaml.ipynb",
    "line": 364,
    "issue": "Possible hardcoded password: '10-30% fewer tokens than JSON'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-27T18:09:25.876Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive: flagged string is demo output text '10-30% fewer tokens than JSON' stored under a dict key named 'token_efficiency' in a documentation notebook; Bandit matches the 'token' substring in the key. Not a credential."
  },
  {
    "source": "security-matrix",
    "path": "notebooks/examples/demo-lambda/template.yml",
    "resourceType": "AWS::KMS::Key",
    "resourceName": "DemoKmsKey",
    "issue": "KMS key deployed without monitoring infrastructure for events and compliance",
    "fix": "Add EventBridge rule with EventPattern source \"aws.kms\" and Lambda function target for KMS event monitoring. The Lambda function should have X-Ray tracing enabled. The Lambda function should have CloudWatch Alarms for the following Lambda metrics: Errors, Throttles, Duration, Invocations, ConcurrentExecutions, and DeadLetterErrors.",
    "priority": "HIGH",
    "check_id": "KMS-007",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.895Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: KMS key event monitoring (CloudTrail/EventBridge/alarms) is account-level infrastructure owned by the deploying customer, out of scope for the solution template."
  },
  {
    "source": "security-matrix",
    "path": "patterns/unified/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "BDAMetadataTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.911Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/sdlc-iam-role.yml",
    "resourceType": "AWS::IAM::Role",
    "resourceName": "BuilderRole",
    "issue": "EC2 instance role violates principle of least privilege",
    "fix": "Replace the overly permissive managed policy with a custom policy that grants only the specific permissions required by the EC2 instance.",
    "priority": "HIGH",
    "check_id": "EC2-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.912Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: BuilderRole is SDLC pipeline scaffolding for dev accounts and intentionally broad to build/publish all solution artifacts."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/s3-sourcecode.yml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "InitialInstallBucket",
    "issue": "S3 bucket violates access logging or least privilege requirements",
    "fix": "Add LoggingConfiguration property to enable S3 access logging with a dedicated log bucket.",
    "priority": "HIGH",
    "check_id": "S3-001",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.913Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: SDLC bootstrap scaffolding deployed in dev accounts; access logging omitted for short-lived pipeline source bucket."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/s3-sourcecode.yml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "InitialInstallBucket",
    "issue": "S3 bucket lacks lifecycle policy",
    "fix": "Configure a lifecycle policy to manage S3 objects during their lifetime.",
    "priority": "HIGH",
    "check_id": "S3-008",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.913Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: SDLC pipeline scaffolding bucket in solution-builder dev accounts; retention of pipeline source/artifact objects is the pipeline owner's decision, and a lifecycle policy here is cost optimization rather than a security control."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/codepipeline-s3.yml",
    "resourceType": "AWS::KMS::Key",
    "resourceName": "PipelineKMSKey",
    "issue": "KMS key deployed without monitoring infrastructure for events and compliance",
    "fix": "Add EventBridge rule with EventPattern source \"aws.kms\" and Lambda function target for KMS event monitoring. The Lambda function should have X-Ray tracing enabled. The Lambda function should have CloudWatch Alarms for the following Lambda metrics: Errors, Throttles, Duration, Invocations, ConcurrentExecutions, and DeadLetterErrors.",
    "priority": "HIGH",
    "check_id": "KMS-007",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.913Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: KMS key event monitoring (CloudTrail/EventBridge/alarms) is account-level infrastructure owned by the deploying customer, out of scope for the solution template."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/codepipeline-s3.yml",
    "resourceType": "AWS::IAM::Role",
    "resourceName": "CodeBuildRole",
    "issue": "Compute role with IAM-mutating permissions lacks permissions boundary",
    "fix": "Add PermissionsBoundary to limit IAM resource creation scope",
    "priority": "HIGH",
    "check_id": "IAM-009",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.913Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CodeBuild role in SDLC dev-account scaffolding; a permissions boundary is the account owner's policy decision, the template cannot assume one exists."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/codepipeline-s3.yml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "ArtifactBucket",
    "issue": "S3 bucket violates access logging or least privilege requirements",
    "fix": "Add LoggingConfiguration property to enable S3 access logging with a dedicated log bucket.",
    "priority": "HIGH",
    "check_id": "S3-001",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.914Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: SDLC pipeline artifact bucket in dev accounts; access logging omitted for pipeline scaffolding."
  },
  {
    "source": "security-matrix",
    "path": "scripts/sdlc/cfn/codepipeline-s3.yml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "ArtifactBucket",
    "issue": "S3 bucket lacks lifecycle policy",
    "fix": "Configure a lifecycle policy to manage S3 objects during their lifetime.",
    "priority": "HIGH",
    "check_id": "S3-008",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.914Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: SDLC pipeline scaffolding bucket in solution-builder dev accounts; retention of pipeline source/artifact objects is the pipeline owner's decision, and a lifecycle policy here is cost optimization rather than a security control."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::KMS::Key",
    "resourceName": "CustomerManagedEncryptionKey",
    "issue": "KMS key deployed without monitoring infrastructure for events and compliance",
    "fix": "Add EventBridge rule with EventPattern source \"aws.kms\" and Lambda function target for KMS event monitoring. The Lambda function should have X-Ray tracing enabled. The Lambda function should have CloudWatch Alarms for the following Lambda metrics: Errors, Throttles, Duration, Invocations, ConcurrentExecutions, and DeadLetterErrors.",
    "priority": "HIGH",
    "check_id": "KMS-007",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: KMS key event monitoring (CloudTrail/EventBridge/alarms) is account-level infrastructure owned by the deploying customer, out of scope for the solution template."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "EvaluationBaselineBucket",
    "issue": "S3 bucket lacks lifecycle policy",
    "fix": "Configure a lifecycle policy to manage S3 objects during their lifetime.",
    "priority": "HIGH",
    "check_id": "S3-008",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: bucket retention is a customer data-governance decision (evaluation baselines, working documents); imposing a lifecycle policy in the template risks deleting customer data."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "WebUIBucket",
    "issue": "S3 bucket used as CloudFront origin lacks access restriction (OAC or OAI)",
    "fix": "Configure Origin Access Control (OAC) or Origin Access Identity (OAI) for CloudFront distributions using this bucket, and update bucket policy to restrict access.",
    "priority": "HIGH",
    "check_id": "S3-005",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "False positive at scan time: WebUIBucket is fronted by CloudFront with an OriginAccessControl (sigv4, SigningBehavior always) plus a bucket policy conditioned on the distribution ARN when CloudFront hosting is enabled; the conditional resources cannot be evaluated statically."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::S3::Bucket",
    "resourceName": "WebUIBucket",
    "issue": "S3 bucket lacks lifecycle policy",
    "fix": "Configure a lifecycle policy to manage S3 objects during their lifetime.",
    "priority": "HIGH",
    "check_id": "S3-008",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: WebUIBucket holds only the current built web-app bundle, redeployed in full by CodeBuild on each update; there is no object accumulation to manage, and an expiry lifecycle rule could delete live UI assets."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "TrackingTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ConcurrencyTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.915Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ConfigurationTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "DiscoveryTrackingTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "AgentTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "UsersTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ChatSessionsTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ChatMessagesTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "IdHelperChatMemoryTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.916Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/template.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "No X-Ray tracing configured for Lambda function",
    "fix": "Add TracingConfig property to the Lambda function configuration. For CloudFormation templates, ensure the Lambda function's exeuction role has the AWSXrayDaemonWriteAccess policy attached.",
    "priority": "HIGH",
    "check_id": "LAMBDA-004",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.920Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: X-Ray tracing intentionally omitted on helper/custom-resource Lambdas; enabling tracing is a customer deployment choice for this sample solution."
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/template.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "Lambda function lacks CloudWatch alarms for monitoring",
    "fix": "Create CloudWatch alarms for the following Lambda metrics: Errors,Throttles,Duration,Invocations,ConcurrentExecutions,DeadLetterErrors. Configure alarm actions (e.g., SNS notifications) to assign an owner who will monitor and respond to incidents.",
    "priority": "HIGH",
    "check_id": "LAMBDA-011",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.920Z",
    "assessmentCount": 22,
    "suppressionReason": "Accepted: CloudWatch alarms for helper/custom-resource Lambdas are intentionally omitted; failures surface via CloudFormation stack status. Alarm topology is a customer operations decision."
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/template.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "Lambda function shares an IAM execution role with another function",
    "fix": "Create a unique IAM execution role for this Lambda function instead of sharing role GetAtt:StartIngestionJobFunctionRole with . Lambda functions should have a 1:1 relationship with IAM execution roles.",
    "priority": "HIGH",
    "check_id": "LAMBDA-012",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.920Z",
    "assessmentCount": 17,
    "resolvedAt": "2026-07-07T20:59:36.073Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/template.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "RegisterTargetsFunction",
    "issue": "Lambda function shares an IAM execution role with another function",
    "fix": "Create a unique IAM execution role for this Lambda function instead of sharing role GetAtt:RegisterTargetsRole with . Lambda functions should have a 1:1 relationship with IAM execution roles.",
    "priority": "HIGH",
    "check_id": "LAMBDA-012",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-27T18:09:25.924Z",
    "assessmentCount": 10,
    "suppressionReason": "This is a false-positive",
    "resolvedAt": "2026-07-10T01:37:31.318Z"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 67,
    "issue": "Possible hardcoded password: 'j.w.t'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.167Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 74,
    "issue": "Possible hardcoded password: 'None'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.168Z",
    "assessmentCount": 36,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 105,
    "issue": "Possible hardcoded password: 's1'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.169Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 106,
    "issue": "Possible hardcoded password: 's2'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.170Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 115,
    "issue": "Possible hardcoded password: 's'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.170Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_log_sanitizer.py",
    "line": 137,
    "issue": "Possible hardcoded password: 'should-hide'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.172Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: deliberate dummy secret-shaped values ('j.w.t', 's1', 'should-hide', None) in fixtures that exercise the log sanitizer's redaction of password/token/secret keys; the test's purpose requires these literals. No real secret."
  },
  {
    "source": "Bandit",
    "path": "src/lambda/api_handler/test_index.py",
    "line": 286,
    "issue": "Possible hardcoded password: 'access'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-04-30T20:15:11.278Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: 'token_use': 'access' is a JWT claim-type discriminator in a test event fixture, not a credential."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::KMS::Key",
    "resourceName": "BastionKey",
    "issue": "KMS key deployed without monitoring infrastructure for events and compliance",
    "fix": "Add EventBridge rule with EventPattern source \"aws.kms\" and Lambda function target for KMS event monitoring. The Lambda function should have X-Ray tracing enabled. The Lambda function should have CloudWatch Alarms for the following Lambda metrics: Errors, Throttles, Duration, Invocations, ConcurrentExecutions, and DeadLetterErrors.",
    "priority": "HIGH",
    "check_id": "KMS-007",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-30T20:15:11.352Z",
    "assessmentCount": 18,
    "suppressionReason": "Accepted: KMS key event monitoring (CloudTrail/EventBridge/alarms) is account-level infrastructure owned by the deploying customer, out of scope for the solution template."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::IAM::Role",
    "resourceName": "BastionRole",
    "issue": "EC2 instance role violates principle of least privilege",
    "fix": "Ensure the IAM role has at least CloudWatch Logs and Systems Manager permissions. Consider adding the AmazonSSMManagedInstanceCore and CloudWatchAgentServerPolicy managed policies.",
    "priority": "HIGH",
    "check_id": "EC2-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-30T20:15:11.353Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: BastionRole (Condition ShouldDeployBastionHost, off by default) carries exactly the managed policy the finding's own fix recommends - AmazonSSMManagedInstanceCore - and nothing else; the instance profile attaches only that role. No excess privilege exists."
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::EC2::Instance",
    "resourceName": "BastionInstance",
    "issue": "EC2 instance role violates principle of least privilege",
    "fix": "Attach an IAM role with minimal permissions (at least CloudWatch Logs and Systems Manager access) to the EC2 instance.",
    "priority": "HIGH",
    "check_id": "EC2-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-04-30T20:15:11.353Z",
    "assessmentCount": 18,
    "suppressionReason": "False positive: BastionRole (Condition ShouldDeployBastionHost, off by default) carries exactly the managed policy the finding's own fix recommends - AmazonSSMManagedInstanceCore - and nothing else; the instance profile attaches only that role. No excess privilege exists."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/rule_validation/policy_classification.py",
    "line": 292,
    "issue": "Possible hardcoded password: '0'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-05-02T20:11:37.618Z",
    "assessmentCount": 14,
    "suppressionReason": "False positive: flagged strings are dict keys 'pass_count'/'pass_percentage' in a statistics payload; Bandit matches the substring 'pass'. No credential involved."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/rule_validation/policy_classification.py",
    "line": 295,
    "issue": "Possible hardcoded password: '0.0'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-05-02T20:11:37.618Z",
    "assessmentCount": 14,
    "suppressionReason": "False positive: flagged strings are dict keys 'pass_count'/'pass_percentage' in a statistics payload; Bandit matches the substring 'pass'. No credential involved."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/rule_validation/policy_classification.py",
    "line": 316,
    "issue": "Possible hardcoded password: '0'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-05-02T20:11:37.618Z",
    "assessmentCount": 14,
    "suppressionReason": "False positive: flagged strings are dict keys 'pass_count'/'pass_percentage' in a statistics payload; Bandit matches the substring 'pass'. No credential involved."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/idp_common/rule_validation/policy_classification.py",
    "line": 319,
    "issue": "Possible hardcoded password: '0.0'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-05-02T20:11:37.618Z",
    "assessmentCount": 14,
    "suppressionReason": "False positive: flagged strings are dict keys 'pass_count'/'pass_percentage' in a statistics payload; Bandit matches the substring 'pass'. No credential involved."
  },
  {
    "source": "security-matrix",
    "path": "nested/appsync/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ChatDocumentSessionsTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-05-12T19:10:27.154Z",
    "assessmentCount": 7,
    "suppressionReason": "This is not required/permitted by the customer",
    "resolvedAt": "2026-07-07T01:25:57.818Z"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_openai_responses.py",
    "line": 218,
    "issue": "Possible hardcoded password: 'secret'",
    "fix": "Remove hardcoded passwords from function arguments. Create a .env file named after the Python file (e.g., for script.py create script.env) containing the ACTUAL secret values. Update the Python code to: 1) add 'from dotenv import load_dotenv' import, 2) call load_dotenv('script.env'), 3) use os.environ.get(). Add *.env to .gitignore.",
    "check_id": "B106",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b106_hardcoded_password_funcarg.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.299Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture passes a dummy 'secret' literal to exercise auth code paths; no real credential.",
    "suppressedAt": "2026-06-10T15:52:31.776009"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_openai_responses.py",
    "line": 381,
    "issue": "Possible hardcoded password: 'secret'",
    "fix": "Remove hardcoded passwords from function arguments. Create a .env file named after the Python file (e.g., for script.py create script.env) containing the ACTUAL secret values. Update the Python code to: 1) add 'from dotenv import load_dotenv' import, 2) call load_dotenv('script.env'), 3) use os.environ.get(). Add *.env to .gitignore.",
    "check_id": "B106",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b106_hardcoded_password_funcarg.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.300Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture passes a dummy 'secret' literal to exercise auth code paths; no real credential.",
    "suppressedAt": "2026-06-10T15:52:31.776026"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 72,
    "issue": "Possible hardcoded password: 'SECRET'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776029"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 73,
    "issue": "Possible hardcoded password: 'TOKEN'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776032"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 101,
    "issue": "Possible hardcoded password: 'SECRET'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776034"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 102,
    "issue": "Possible hardcoded password: 'TOKEN'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776036"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 126,
    "issue": "Possible hardcoded password: 'SECRET'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776039"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 127,
    "issue": "Possible hardcoded password: 'TOKEN'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776041"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 152,
    "issue": "Possible hardcoded password: 'SECRET'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776043"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/test_bedrock_session.py",
    "line": 153,
    "issue": "Possible hardcoded password: 'TOKEN'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-06-09T22:11:41.302Z",
    "assessmentCount": 9,
    "suppressionReason": "False positive: test fixture uses dummy placeholder credentials (e.g. 'testing') required by moto/boto3 test setup; no real secret.",
    "suppressedAt": "2026-06-10T15:52:31.776045"
  },
  {
    "source": "Semgrep",
    "path": "src/ui/.npmrc",
    "line": 1,
    "issue": "This .npmrc does not set a minimum release age or sets it too low. Newly published packages can be malicious or unstable. Add `min-release-age = 7` to wait 7 days before resolving newly published package versions. Added in: v11.10 Reference: https://github.blog/changelog/2026-02-18-npm-bulk-trusted-publishing-config-and-script-security-now-generally-available/",
    "check_id": "package_managers.npm.npm-missing-minimum-release-age.npm-missing-minimum-release-age",
    "priority": "HIGH",
    "references": "https://github.blog/changelog/2026-02-18-npm-bulk-trusted-publishing-config-and-script-security-now-generally-available/ | https://github.com/npm/cli/pull/8965",
    "status": "resolved",
    "firstDetectedAt": "2026-07-07T01:25:57.555Z",
    "assessmentCount": 4,
    "suppressionReason": "Accepted: UI dependencies are pinned via package-lock.json and installed with npm ci; minimum-release-age is a repo-wide supply-chain policy decision tracked separately.",
    "resolvedAt": "2026-07-08T23:11:40.738Z"
  },
  {
    "source": "Bandit",
    "path": "lib/idp_feature_sdk/idp_feature_sdk/publisher.py",
    "line": 162,
    "issue": "subprocess call with shell=True identified, security issue.",
    "fix": "Avoid `shell=True` in subprocess calls. Use `shell=False` and pass command arguments as a list to prevent shell injection attacks.",
    "check_id": "B602",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b602_subprocess_popen_with_shell_equals_true.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.661Z",
    "assessmentCount": 6,
    "suppressionReason": "Mitigated + residual accepted: the manifest schema now provides structured shell-free forms (ui.build / agentSource.package step lists, each {cwd, argv} run with shell=False in _run_steps) which all in-repo manifests use. The single remaining shell=True is the deprecated legacy buildCommand/packageCommand string path (_run_legacy_shell_command), kept for backward compatibility with existing feature.yaml files: it executes the feature developer's OWN command string from their OWN manifest on their OWN machine (equivalent to a Makefile target), emits a runtime deprecation notice, and is not reachable with attacker-controlled input."
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/feature-api/tests/test_handler.py",
    "line": 40,
    "issue": "Possible hardcoded password: '4'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.569Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/feature-api/tests/test_handler.py",
    "line": 91,
    "issue": "Possible hardcoded password: '4'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.569Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/feature-api/tests/test_handler.py",
    "line": 107,
    "issue": "Possible hardcoded password: '4'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.569Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 115,
    "issue": "Possible hardcoded password: '5'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 116,
    "issue": "Possible hardcoded password: '4'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 118,
    "issue": "Possible hardcoded password: '1'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 120,
    "issue": "Possible hardcoded password: '3'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 131,
    "issue": "Possible hardcoded password: '3'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 134,
    "issue": "Possible hardcoded password: '3'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 141,
    "issue": "Possible hardcoded password: '7'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 156,
    "issue": "Possible hardcoded password: '4'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 164,
    "issue": "Possible hardcoded password: '2'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 183,
    "issue": "Possible hardcoded password: '1'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 200,
    "issue": "Possible hardcoded password: '3'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "Bandit",
    "path": "feature-platform/sample-health-insurance-review/hook/tests/test_handler.py",
    "line": 205,
    "issue": "Possible hardcoded password: '2'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-07T01:25:57.570Z",
    "assessmentCount": 6,
    "suppressionReason": "False positive: flagged values are numeric rule-validation counters in test fixtures (e.g. {'Pass': 4, 'Fail': 1}, passCount); Bandit B105 matches dict keys containing 'pass'. No credential involved.",
    "suppressedAt": "2026-07-07T02:15:42.487410Z"
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/main-stack-extensions/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "InstalledFeaturesTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T02:33:12.109Z",
    "assessmentCount": 5,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails.",
    "suppressedAt": "2026-07-07T02:53:44.559674Z"
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/sample-health-insurance-review/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ClaimsStatusTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T02:33:12.114Z",
    "assessmentCount": 5,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails.",
    "suppressedAt": "2026-07-07T02:53:44.559674Z"
  },
  {
    "source": "security-matrix",
    "path": "template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "BootstrapTrackingTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T03:02:04.767Z",
    "assessmentCount": 4,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "Checkov",
    "path": "nested/api-resolvers/template.yaml",
    "line": 2751,
    "issue": "Ensure WAF prevents message lookup in Log4j2. See CVE-2021-44228 aka log4jshell",
    "check_id": "CKV_AWS_192",
    "priority": "HIGH",
    "fix": "Add a rule to your AWS::WAFv2::WebACL resource that blocks requests containing Log4j2 JNDI lookup patterns. Create a ByteMatchSet with pattern strings like '${jndi:' and configure your WAF to block these patterns. This protects against Log4Shell vulnerabilities (CVE-2021-44228) that could allow remote code execution.",
    "status": "resolved",
    "stack": "nested-api-resolvers-template",
    "resourceType": "WAFv2 WebACL",
    "resourceName": "ApiWafWebACL",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T03:02:04.772Z",
    "assessmentCount": 2,
    "suppressionReason": "Accepted: WebACL uses DefaultAction Block with an explicit IP allowlist (deny-by-default), so the Log4j managed rule group adds no protection for already-blocked traffic.",
    "resolvedAt": "2026-07-08T23:11:40.738Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ChatDocumentSessionsTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T03:02:04.772Z",
    "assessmentCount": 4,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::ApiGateway::RestApi",
    "resourceName": "HttpApi",
    "issue": "API Gateway has 1 method(s) without request validation",
    "fix": "Create a RequestValidator resource AFTER the RestApi definition (it references the API, so the API must exist first). Then update EACH Method resource to include a reference to the validator.",
    "priority": "HIGH",
    "check_id": "API-GW-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-07T03:02:04.772Z",
    "assessmentCount": 4,
    "suppressionReason": "Accepted: REST API in Lambda-proxy mode; the data-plane POST method is Cognito-authorized and the dispatcher Lambda validates each request body per-field, and the remaining method is the auth-less MOCK OPTIONS CORS preflight (static 200, no backend). API GW request validators add no value for opaque proxy payloads."
  },
  {
    "source": "Bandit",
    "path": "lib/idp_common_pkg/tests/unit/bedrock/test_sizing.py",
    "line": 78,
    "issue": "Possible hardcoded password: '9999'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-08T23:11:40.438Z",
    "assessmentCount": 2,
    "suppressionReason": "False positive: '9999' is a token-budget number asserted in a dict whose key 'shard_token_budget' contains 'token'; Bandit B105 keys on the substring. Not a credential."
  },
  {
    "source": "Checkov",
    "path": "patterns/unified/.aws-sam/packaged.yaml",
    "line": 3714,
    "issue": "Ensure ECR image scanning on push is enabled",
    "check_id": "CKV_AWS_163",
    "priority": "HIGH",
    "fix": "Enable ECR image scanning on push by setting 'ScanOnPush: true' in the ImageScanningConfiguration property of AWS::ECR::Repository resource. This automatically scans container images for vulnerabilities when they are pushed to the repository, enhancing container security.",
    "status": "resolved",
    "stack": "patterns-unified-.aws-sam-packaged",
    "resourceType": "ECR Repository",
    "resourceName": "ECRRepository",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.700Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "patterns/unified/.aws-sam/packaged.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "BDAMetadataTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.708Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "No X-Ray tracing configured for Lambda function",
    "fix": "Add TracingConfig property to the Lambda function configuration. For CloudFormation templates, ensure the Lambda function's exeuction role has the AWSXrayDaemonWriteAccess policy attached.",
    "priority": "HIGH",
    "check_id": "LAMBDA-004",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.712Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "Lambda function lacks CloudWatch alarms for monitoring",
    "fix": "Create CloudWatch alarms for the following Lambda metrics: Errors,Throttles,Duration,Invocations,ConcurrentExecutions,DeadLetterErrors. Configure alarm actions (e.g., SNS notifications) to assign an owner who will monitor and respond to incidents.",
    "priority": "HIGH",
    "check_id": "LAMBDA-011",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.712Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/bedrockkb/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "StartIngestionJobFunction",
    "issue": "Lambda function shares an IAM execution role with another function",
    "fix": "Create a unique IAM execution role for this Lambda function instead of sharing role GetAtt:StartIngestionJobFunctionRole with . Lambda functions should have a 1:1 relationship with IAM execution roles.",
    "priority": "HIGH",
    "check_id": "LAMBDA-012",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.712Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "SGIngressManagerFunction",
    "issue": "No X-Ray tracing configured for Lambda function",
    "fix": "Add TracingConfig property to the Lambda function configuration. For CloudFormation templates, ensure the Lambda function's exeuction role has the AWSXrayDaemonWriteAccess policy attached.",
    "priority": "HIGH",
    "check_id": "LAMBDA-004",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.713Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "SGIngressManagerFunction",
    "issue": "Lambda function lacks CloudWatch alarms for monitoring",
    "fix": "Create CloudWatch alarms for the following Lambda metrics: Errors,Throttles,Duration,Invocations,ConcurrentExecutions,DeadLetterErrors. Configure alarm actions (e.g., SNS notifications) to assign an owner who will monitor and respond to incidents.",
    "priority": "HIGH",
    "check_id": "LAMBDA-011",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.713Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "RegisterTargetsFunction",
    "issue": "No X-Ray tracing configured for Lambda function",
    "fix": "Add TracingConfig property to the Lambda function configuration. For CloudFormation templates, ensure the Lambda function's exeuction role has the AWSXrayDaemonWriteAccess policy attached.",
    "priority": "HIGH",
    "check_id": "LAMBDA-004",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.714Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/.aws-sam/packaged.yaml",
    "resourceType": "AWS::Lambda::Function",
    "resourceName": "RegisterTargetsFunction",
    "issue": "Lambda function lacks CloudWatch alarms for monitoring",
    "fix": "Create CloudWatch alarms for the following Lambda metrics: Errors,Throttles,Duration,Invocations,ConcurrentExecutions,DeadLetterErrors. Configure alarm actions (e.g., SNS notifications) to assign an owner who will monitor and respond to incidents.",
    "priority": "HIGH",
    "check_id": "LAMBDA-011",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.714Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/alb-hosting/.aws-sam/packaged.yaml",
    "resourceType": "AWS::ElasticLoadBalancingV2::LoadBalancer",
    "resourceName": "ALB",
    "issue": "Load balancer does not use multiple AZs or Cross-Zone Load Balancing is not enabled",
    "fix": "Configure load balancer to use at least 2 subnets in different Availability Zones",
    "priority": "HIGH",
    "check_id": "ELB-004",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.714Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "Checkov",
    "path": "nested/api-resolvers/template.yaml",
    "line": 2868,
    "issue": "Ensure WAF prevents message lookup in Log4j2. See CVE-2021-44228 aka log4jshell",
    "check_id": "CKV_AWS_192",
    "priority": "HIGH",
    "fix": "Add a rule to your AWS::WAFv2::WebACL resource that blocks requests containing Log4j2 JNDI lookup patterns. Create a ByteMatchSet with pattern strings like '${jndi:' and configure your WAF to block these patterns. This protects against Log4Shell vulnerabilities (CVE-2021-44228) that could allow remote code execution.",
    "status": "suppressed",
    "stack": "nested-api-resolvers-template",
    "resourceType": "WAFv2 WebACL",
    "resourceName": "ApiWafWebACL",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.717Z",
    "assessmentCount": 2,
    "suppressionReason": "Accepted: WebACL uses DefaultAction Block with an explicit IP allowlist (deny-by-default), so only allowlisted ranges reach the API; backend is Python (no Log4j2). Log4Shell managed rule adds no protection. Inline 'checkov:skip=CKV_AWS_192' comment present on the ApiWafWebACL resource in nested/api-resolvers/template.yaml."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/.aws-sam/packaged.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ChatDocumentSessionsTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.717Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/.aws-sam/packaged.yaml",
    "resourceType": "AWS::ApiGateway::RestApi",
    "resourceName": "HttpApi",
    "issue": "API Gateway has 1 method(s) without request validation",
    "fix": "Create a RequestValidator resource AFTER the RestApi definition (it references the API, so the API must exist first). Then update EACH Method resource to include a reference to the validator.",
    "priority": "HIGH",
    "check_id": "API-GW-002",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.717Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/.aws-sam/packaged.yaml",
    "resourceType": "AWS::ApiGateway::Stage",
    "resourceName": "HttpApiStage",
    "issue": "API Gateway does not have access logging enabled with proper retention",
    "fix": "Add AccessLogSetting with DestinationArn and Format properties",
    "priority": "HIGH",
    "check_id": "API-GW-001",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.717Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/.aws-sam/packaged.yaml",
    "resourceType": "AWS::ApiGateway::Stage",
    "resourceName": "HttpApiStage",
    "issue": "API Gateway does not have CloudWatch logs enabled",
    "fix": "Enable CloudWatch logs by setting MethodSettings with LoggingLevel to INFO or ERROR.",
    "priority": "HIGH",
    "check_id": "API-GW-006",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.717Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/main-stack-extensions/.aws-sam/packaged.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "InstalledFeaturesTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "resolved",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.725Z",
    "assessmentCount": 1,
    "resolvedAt": "2026-07-10T01:37:31.319Z"
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/sample-health-insurance-review/.aws-sam/packaged.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "ClaimsStatusTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.736Z",
    "assessmentCount": 2,
    "suppressionReason": "Accepted: duplicate finding from a SAM build artifact (.aws-sam/packaged.yaml) of feature-platform/sample-health-insurance-review/template.yaml ClaimsStatusTable. Same rationale as the source-template entry: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer. Build artifacts are excluded from scans by 'make srt-clean'."
  },
  {
    "source": "security-matrix",
    "path": ".aws-sam/idp-main.yaml",
    "resourceType": "AWS::KMS::Key",
    "resourceName": "BastionKey",
    "issue": "KMS key deployed without monitoring infrastructure for events and compliance",
    "fix": "Add EventBridge rule with EventPattern source \"aws.kms\" and Lambda function target for KMS event monitoring. The Lambda function should have X-Ray tracing enabled. The Lambda function should have CloudWatch Alarms for the following Lambda metrics: Errors, Throttles, Duration, Invocations, ConcurrentExecutions, and DeadLetterErrors.",
    "priority": "HIGH",
    "check_id": "KMS-007",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-08T23:11:40.736Z",
    "assessmentCount": 2,
    "suppressionReason": "Accepted: duplicate finding from a SAM build artifact (.aws-sam/idp-main.yaml) of template.yaml BastionKey. Same rationale as the source-template entry: KMS key event monitoring (CloudTrail/EventBridge/alarms) is account-level infrastructure owned by the deploying customer. Build artifacts are excluded from scans by 'make srt-clean'."
  },
  {
    "source": "Bandit",
    "path": "benchmarks/harness/aggregate.py",
    "line": 214,
    "issue": "subprocess call with shell=True identified, security issue.",
    "fix": "Avoid `shell=True` in subprocess calls. Use `shell=False` and pass command arguments as a list to prevent shell injection attacks.",
    "check_id": "B602",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b602_subprocess_popen_with_shell_equals_true.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-10T01:37:31.041Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: local benchmark harness (developer tooling in benchmarks/, never deployed); shell commands are composed from hard-coded constants and the operator's own CLI arguments, so no untrusted input crosses the shell boundary."
  },
  {
    "source": "Bandit",
    "path": "benchmarks/harness/run_matrix.py",
    "line": 40,
    "issue": "subprocess call with shell=True identified, security issue.",
    "fix": "Avoid `shell=True` in subprocess calls. Use `shell=False` and pass command arguments as a list to prevent shell injection attacks.",
    "check_id": "B602",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b602_subprocess_popen_with_shell_equals_true.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-10T01:37:31.041Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: local benchmark harness (developer tooling in benchmarks/, never deployed); shell commands are composed from hard-coded constants and the operator's own CLI arguments, so no untrusted input crosses the shell boundary."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::ApiGateway::Method",
    "resourceName": "WebUIRootMethod",
    "issue": "API Gateway lacks proper authentication configuration (AuthorizationType is NONE)",
    "fix": "Set AuthorizationType to 'AWS_IAM', 'COGNITO_USER_POOLS', or 'CUSTOM' and configure appropriate authorization settings.",
    "priority": "HIGH",
    "check_id": "API-GW-004",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-14T18:40:03.268Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: intentional public static-asset route (enabled only when ServeWebUI=true). This GET serves the React SPA shell (index.html) from S3 to the browser BEFORE the user authenticates in-app; auth happens later against the Cognito-authorized /op POST. A JWT cannot be attached to the browser's initial document fetch, so AuthorizationType NONE is required, and only non-sensitive static UI files are exposed. Defense-in-depth: the stage-level WAFv2 IP allowlist (IsWafEnabled) and the PRIVATE-endpoint VPC policy (UsePrivateApi) still gate this route."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::ApiGateway::Method",
    "resourceName": "WebUIProxyMethod",
    "issue": "API Gateway lacks proper authentication configuration (AuthorizationType is NONE)",
    "fix": "Set AuthorizationType to 'AWS_IAM', 'COGNITO_USER_POOLS', or 'CUSTOM' and configure appropriate authorization settings.",
    "priority": "HIGH",
    "check_id": "API-GW-004",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-14T18:40:03.268Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: intentional public static-asset route (enabled only when ServeWebUI=true). This GET /{proxy+} serves hashed SPA static assets (js/css/fonts/images) from S3 to the browser BEFORE the user authenticates in-app; auth happens later against the Cognito-authorized /op POST. A JWT cannot be attached to the browser's asset fetches, so AuthorizationType NONE is required, and only non-sensitive static UI files are exposed. Defense-in-depth: the stage-level WAFv2 IP allowlist (IsWafEnabled) and the PRIVATE-endpoint VPC policy (UsePrivateApi) still gate this route."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::ApiGateway::Stage",
    "resourceName": "HttpApiStage",
    "issue": "API Gateway does not have access logging enabled with proper retention",
    "fix": "Ensure both DestinationArn and Format properties are specified in AccessLogSetting",
    "priority": "HIGH",
    "check_id": "API-GW-001",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-14T18:40:03.268Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted (scanner limitation): the stage DOES configure AccessLogSetting with both a DestinationArn (HttpApiAccessLogGroup, KMS-encrypted, RetentionInDays=LogRetentionDays) and a JSON Format, but they are wrapped in Fn::If [EnableApiAccessLogs] (on at LogLevel INFO/DEBUG). SRT's security-matrix cannot resolve Fn::If, so it does not see the DestinationArn/Format inside the conditional branch. Access logging is correctly implemented; the conditionality is intentional to avoid log costs at higher LogLevels."
  },
  {
    "source": "security-matrix",
    "path": "nested/api-resolvers/template.yaml",
    "resourceType": "AWS::ApiGateway::Stage",
    "resourceName": "HttpApiStage",
    "issue": "API Gateway does not have CloudWatch logs enabled",
    "fix": "Enable CloudWatch logs by setting MethodSettings with LoggingLevel to INFO or ERROR.",
    "priority": "HIGH",
    "check_id": "API-GW-006",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-14T18:40:03.268Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted (scanner limitation): the stage DOES set MethodSettings with LoggingLevel ERROR for ResourcePath '/*' HttpMethod '*', but wrapped in Fn::If [EnableApiAccessLogs]. SRT cannot resolve Fn::If and reads MethodSettings as a non-array, so it reports execution logging disabled. Execution logging is deliberately ERROR-only (INFO would echo full request/response payloads, incl. customer document data, into CloudWatch); JSON access logs cover request metadata. Logging is correctly implemented; the conditionality is intentional."
  },
  {
    "source": "Bandit",
    "path": "scripts/sdlc/tests/test_api_security_cases.py",
    "line": 111,
    "issue": "Possible hardcoded password: 'b'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-18T00:00:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "False positive: test fixture uses dummy placeholder token identifiers in scripts/api_security_cases.py unit tests (fake per-user token ids 'a'/'b' and literal token args 'expired'/'t' driving injected fakes). Bandit's B105/B106 heuristic fires on literals near identifiers containing 'token'; none is a real credential. Also annotated inline with '# nosec'."
  },
  {
    "source": "Bandit",
    "path": "scripts/sdlc/tests/test_api_security_cases.py",
    "line": 113,
    "issue": "Possible hardcoded password: 'a'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B105",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b105_hardcoded_password_string.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-18T00:00:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "False positive: test fixture uses dummy placeholder token identifiers in scripts/api_security_cases.py unit tests (fake per-user token ids 'a'/'b' and literal token args 'expired'/'t' driving injected fakes). Bandit's B105/B106 heuristic fires on literals near identifiers containing 'token'; none is a real credential. Also annotated inline with '# nosec'."
  },
  {
    "source": "Bandit",
    "path": "scripts/sdlc/tests/test_api_security_cases.py",
    "line": 224,
    "issue": "Possible hardcoded password: 'expired'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B106",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b106_hardcoded_password_funcarg.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-18T00:00:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "False positive: test fixture uses dummy placeholder token identifiers in scripts/api_security_cases.py unit tests (fake per-user token ids 'a'/'b' and literal token args 'expired'/'t' driving injected fakes). Bandit's B105/B106 heuristic fires on literals near identifiers containing 'token'; none is a real credential. Also annotated inline with '# nosec'."
  },
  {
    "source": "Bandit",
    "path": "scripts/sdlc/tests/test_api_security_cases.py",
    "line": 251,
    "issue": "Possible hardcoded password: 't'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B106",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b106_hardcoded_password_funcarg.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-18T00:00:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "False positive: test fixture uses dummy placeholder token identifiers in scripts/api_security_cases.py unit tests (fake per-user token ids 'a'/'b' and literal token args 'expired'/'t' driving injected fakes). Bandit's B105/B106 heuristic fires on literals near identifiers containing 'token'; none is a real credential. Also annotated inline with '# nosec'."
  },
  {
    "source": "Bandit",
    "path": "scripts/sdlc/tests/test_api_security_cases.py",
    "line": 273,
    "issue": "Possible hardcoded password: 't'",
    "fix": "Remove hardcoded password strings from source code. Create a .env file named after the Python file (e.g., for script.py create script.env in the same directory) containing the ACTUAL secret values extracted from the code. Update the Python code to: 1) add 'from dotenv import load_dotenv' import if not present, 2) call load_dotenv('script.env') near the top of the file to load the environment file, 3) use os.environ.get() to read the values. Add *.env to the project root .gitignore.",
    "check_id": "B106",
    "priority": "High",
    "references": "https://bandit.readthedocs.io/en/1.9.4/plugins/b106_hardcoded_password_funcarg.html",
    "status": "suppressed",
    "firstDetectedAt": "2026-07-18T00:00:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "False positive: test fixture uses dummy placeholder token identifiers in scripts/api_security_cases.py unit tests (fake per-user token ids 'a'/'b' and literal token args 'expired'/'t' driving injected fakes). Bandit's B105/B106 heuristic fires on literals near identifiers containing 'token'; none is a real credential. Also annotated inline with '# nosec'."
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/idp-data-generator/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "BootstrapTrackingTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-21T19:49:25.736Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails. Consistent with all other DynamoDB tables in this solution."
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/idp-data-generator/template.yaml",
    "resourceType": "AWS::IAM::Role",
    "resourceName": "AgentCoreRuntimeManagerRole",
    "issue": "Lambda function IAM role violates principle of least privilege",
    "fix": "Replace wildcard actions (e.g., '*' or 'service:*') with specific actions that the Lambda function actually needs, and restrict resources to specific ARNs instead of using '*'.",
    "priority": "HIGH",
    "check_id": "LAMBDA-005",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-21T19:49:25.735Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: bedrock-agentcore:* on Resource '*' is required \u2014 CreateAgentRuntime transitively creates/deletes runtime endpoints and other sub-resources, so an action allow-list breaks create/delete; these control-plane APIs do not support resource-level permissions and the runtime ARN does not exist at create time. iam:PassRole/CreateServiceLinkedRole and logs are scoped."
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/pii-anonymizer/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "RedactionAuditTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-24T16:30:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails. Same rationale as every other table in the solution (TrackingTable, InstalledFeaturesTable, ClaimsStatusTable, ...). The audit table stores metadata only (never PII).",
    "suppressedAt": "2026-07-24T16:30:00.000Z"
  },
  {
    "source": "security-matrix",
    "path": "feature-platform/pii-anonymizer/template.yaml",
    "resourceType": "AWS::DynamoDB::Table",
    "resourceName": "RedactionMappingTable",
    "issue": "DynamoDB data plane events are not captured by CloudTrail logging",
    "fix": "Enable CloudTrail logging for DynamoDB data plane events for this table. Create a CloudTrail Trail if none exists. Add this table's ARN to an EventSelector with DataResources type 'AWS::DynamoDB::Table' \u2014 append to existing values array if one exists, otherwise create a new EventSelector. For CDK: use the L1 escape hatch (trail.node.defaultChild as CfnTrail).eventSelectors as DataResourceType.DYNAMODB_TABLE does not exist.\n\nIMPORTANT - S3 Bucket Logging Requirements:\nIf creating a new S3 bucket for CloudTrail logs:\n1. First check if the template already has an S3 bucket configured as a logging destination (look for buckets referenced in other buckets' LoggingConfiguration.DestinationBucketName)\n2. If a logging bucket exists, configure the new CloudTrail bucket's LoggingConfiguration to use it with LoggingPrefix 'cloudtrail-logs/'\n3. If no logging bucket exists, create a dedicated server access logging bucket first, then configure the CloudTrail bucket to log to it with LoggingPrefix 'cloudtrail-logs/'",
    "priority": "HIGH",
    "check_id": "DDB-002",
    "status": "suppressed",
    "isCustomResource": false,
    "firstDetectedAt": "2026-07-24T16:30:00.000Z",
    "assessmentCount": 1,
    "suppressionReason": "Accepted: CloudTrail DynamoDB data-plane logging is an account-level control (trail + log bucket) owned by the deploying customer; the solution template cannot assume or create account-wide trails. Same rationale as every other table in the solution (TrackingTable, InstalledFeaturesTable, ClaimsStatusTable, ...). The mapping table is SSE-encrypted, PITR-enabled, and reachable only via the RBAC-gated feature API.",
    "suppressedAt": "2026-07-24T16:30:00.000Z"
  }
]