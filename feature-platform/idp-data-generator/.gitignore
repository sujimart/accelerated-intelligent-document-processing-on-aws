# Build artifacts (regenerated by package_agent_source.sh / idp-feature-cli)
agent-source.zip
# Staged accelerator library copied into the build context(s) at package time
# (AgentCore image root + the bootstrap-processor Lambda CodeUri).
idp_common_pkg/
bootstrap-processor/idp_common_pkg/
feature-ui/dist/
feature-ui/node_modules/
.aws-sam/
__pycache__/
*.pyc
