# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: MIT-0

AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: AWS GenAI IDP Accelerator - Unified Pattern (BDA + Bedrock FMs Pipeline)

Metadata:
  cfn-lint:
    config:
      ignore_checks:
        - W1030  # ARN pattern matching when Ref resolves to empty string

Parameters:
  StackName:
    Type: String

  InputBucket:
    Type: String

  DiscoveryBucket:
    Type: String
    Default: ""

  DiscoveryTrackingTable:
    Type: String
    Default: ""

  ConfigurationBucket:
    Type: String

  WorkingBucket:
    Type: String

  OutputBucket:
    Type: String

  TrackingTable:
    Type: String

  CustomerManagedEncryptionKeyArn:
    Type: String

  LogRetentionDays:
    Type: Number

  LogLevel:
    Type: String
    Default: WARN
    AllowedValues:
      - DEBUG
      - INFO
      - WARN
      - ERROR
      - CRITICAL
    Description: Default logging level for all Lambda functions

  ExecutionTimeThresholdMs:
    Type: Number
    Default: 30000


  PreviousIDPPattern:
    Type: String
    Default: ''
    Description: Previous IDPPattern from SSM (used to detect Pattern-1 → Unified upgrades)

  UpdateConfigurationFunctionArn:
    Type: String

  ConfigurationTable:
    Type: String

  BedrockGuardrailId:
    Type: String
    Default: ""
    Description: "Optionally provide the Id (not name) of an existing Bedrock Guardrail to be used for Bedrock interactions"

  BedrockGuardrailVersion:
    Type: String
    Default: ""
    Description: "If you provided a Bedrock Guardrail Id above, provide the corresponding Guardrail version here"

  BedrockHubRoleArn:
    Type: String
    Default: ""
    AllowedPattern: "^$|^arn:(aws|aws-cn|aws-us-gov|aws-iso|aws-iso-b):iam::[0-9]{12}:role/[\\w+=,.@\\-/]+$"
    ConstraintDescription: "Must be empty or a valid IAM role ARN. The ARN must NOT contain spaces — pass ExternalId via the separate BedrockHubRoleExternalId parameter."
    Description: >
      (Optional) ARN of an IAM role in a centralized hub account to assume for all
      Bedrock invocations. When set, processing Lambdas use sts:AssumeRole and route
      Bedrock data-plane and control-plane traffic through this role.

  BedrockHubRoleExternalId:
    Type: String
    Default: ""
    NoEcho: true
    Description: "(Optional) ExternalId for sts:AssumeRole into the Bedrock hub-account role."

  BedrockHubRoleSessionName:
    Type: String
    Default: ""
    AllowedPattern: "^[\\w+=,.@-]{0,64}$"
    Description: "(Optional) Session name for sts:AssumeRole into the Bedrock hub-account role."

  ConfigurationDefaultS3Uri:
    Type: String
    Description: "S3 URI (s3://bucket/path/config.json) to import default configuration from S3"

  ConfigLibraryHash:
    Type: String
    Description: "Hash token from config library to force updates when config library changes"

  ReportingBucket:
    Type: String
    Description: "S3 bucket for evaluation metrics and analytics"

  BaselineBucket:
    Type: String
    Description: "S3 bucket containing baseline/ground truth documents"

  SaveReportingFunctionName:
    Type: String
    Description: "Name of Lambda function to save evaluation metrics"

  ImageVersion:
    Type: String
    Description: "Content-based version for Lambda container images"

  LambdaArchitecture:
    Type: String
    Default: "arm64"
    AllowedValues: ["arm64", "x86_64"]
    Description: >-
      CPU architecture for Lambda container images. Must match the base image
      used in the Docker build. Use arm64 (Graviton) for best price-performance
      or x86_64 when using custom base images that only support AMD64.

  ArtifactBucket:
    Type: String
    Description: "S3 bucket containing deployment artifacts"

  ArtifactsBucketKmsKeyArn:
    Type: String
    Default: ""
    Description: >-
      (Optional) KMS CMK ARN used to encrypt the artifacts bucket. When provided,
      the DockerBuildRole and other CodeBuild roles receive kms:Decrypt permission.
    AllowedPattern: "^(|arn:aws[a-z-]*:kms:[a-z0-9-]+:[0-9]{12}:key/.+)$"

  ArtifactPrefix:
    Type: String
    Description: "S3 prefix for deployment artifacts"

  SourceZipfile:
    Type: String
    Description: "Name of the unified source code zipfile in S3"

  BedrockModelId:
    Type: String
    Description: "Bedrock model id to use for label extraction"
    Default: "us.amazon.nova-pro-v1:0"

  EnableXRayTracing:
    Type: String
    Default: 'true'
    AllowedValues: ['true', 'false']
    Description: Enable X-Ray tracing

  PermissionsBoundaryArn:
    Type: String
    Default: ""
    Description: >-
      (Optional) ARN of an existing IAM Permissions Boundary policy to attach to all IAM roles.
    AllowedPattern: "^(|arn:aws[a-z-]*:iam::[0-9]{12}:policy/.+)$"
    ConstraintDescription: Must be empty or a valid IAM policy ARN

  EnableECRImageScanning:
    Type: String
    Default: 'true'
    AllowedValues: ['true', 'false']
    Description: Enable automatic vulnerability scanning for Lambda container images in ECR

  CustomClassificationModelARN:
    Type: String
    Default: ""

  CustomExtractionModelARN:
    Type: String
    Default: ""

  # Private REST API VPC config (req #2)
  UsePrivateApi:
    Type: String
    Default: "false"
    AllowedValues: ["true", "false"]
    Description: Set to "true" when ApiGatewayVisibility=PRIVATE — puts processing Lambdas in VPC

  LambdaSubnetIds:
    Type: CommaDelimitedList
    Default: ""
    Description: Subnet IDs for Lambda VpcConfig (required when UsePrivateApi=true)

  LambdaSecurityGroupId:
    Type: String
    Default: ""
    Description: Security group ID for Lambda VpcConfig (required when UsePrivateApi=true)

  VpcId:
    Type: String
    Default: ""
    Description: "Optional - VPC ID for VPC-secured deployment"

  PrivateSubnetIds:
    Type: CommaDelimitedList
    Default: ""
    Description: "Optional - Comma-separated list of private subnet IDs"

  EnableMLflow:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: Enable MLflow experiment tracking integration

  MlflowTrackingURI:
    Type: String
    Default: ""
    Description: SageMaker MLflow tracking server ARN

Conditions:
  DeployInVPC: !Not [!Equals [!Ref VpcId, ""]]
  HasGuardrailConfig:
    !And [
      !Not [!Equals [!Ref BedrockGuardrailId, ""]],
      !Not [!Equals [!Ref BedrockGuardrailVersion, ""]],
    ]
  HasBedrockHubRole:
    !Not [!Equals [!Ref BedrockHubRoleArn, ""]]
  HasBedrockHubRoleExternalId:
    !Not [!Equals [!Ref BedrockHubRoleExternalId, ""]]
  HasBedrockHubRoleSessionName:
    !Not [!Equals [!Ref BedrockHubRoleSessionName, ""]]
  HasCustomClassificationModelARN:
    !Not [!Equals [!Ref CustomClassificationModelARN, ""]]
  HasCustomExtractionModelARN:
    !Not [!Equals [!Ref CustomExtractionModelARN, ""]]
  HasPermissionsBoundary: !Not [!Equals [!Ref PermissionsBoundaryArn, ""]]
  IsECRImageScanningEnabled: !Equals [!Ref EnableECRImageScanning, "true"]
  IsPrivateApi: !Equals [!Ref UsePrivateApi, "true"]
  IsMLflowEnabled: !Equals [!Ref EnableMLflow, "true"]
  HasArtifactsBucketKmsKey: !Not [!Equals [!Ref ArtifactsBucketKmsKeyArn, ""]]

Resources:

  ##########################################################################
  # Configuration Schema and Default Config
  # Uses Pattern-2 schema (superset - includes use_bda toggle for routing)
  ##########################################################################

  UpdateSchemaConfig:
    Type: AWS::CloudFormation::CustomResource
    Properties:
      ServiceToken: !Ref UpdateConfigurationFunctionArn
      Schema:
        type: object
        required:
          - notes
          - classes
          - classification
          - extraction
        properties:
          notes:
            order: 0
            type: string
            description: Notes
          use_bda:
            order: 1
            type: boolean
            description: Use Bedrock Data Automation (BDA) for OCR, classification, extraction, and assessment
            default: false
          enable_blueprint_optimization:
            order: 2
            type: boolean
            description: Automatically optimize BDA blueprints using ground truth data during discovery to improve extraction accuracy
            default: false
            dependsOn: { field: "use_bda", value: true }
          preprocessing:
            order: 2.5
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Preprocessing
            description: A single generic preprocessing hook (Lambda ARN + args) that runs FIRST, before OCR/BDA routing, in both processing modes.
            properties:
              enabled:
                type: boolean
                description: Run the preprocessing hook for this config version.
                default: false
                order: 0
              arn:
                type: string
                description: >-
                  Lambda function ARN to invoke on the source document. Must
                  either be tagged `idp:feature-id=<anything>` OR have a function
                  name beginning with `GENAIIDP-`.
                order: 1
              onError:
                type: string
                enum: ["continue", "fail", "skip-remaining"]
                default: continue
                description: Behavior when the hook errors.
                order: 2
              args:
                type: array
                sectionLabel: Hook arguments
                description: >-
                  Key/value arguments passed to the hook (as strings). The hook
                  reads its own configuration from these — keeping the step
                  generic (e.g. PII Anonymization reads mode/model/redaction here).
                listLabel: Arguments
                itemLabel: Argument
                order: 3
                items:
                  type: object
                  required:
                    - key
                  properties:
                    key:
                      type: string
                      description: Argument name
                      order: 0
                    value:
                      type: string
                      description: Argument value (string)
                      order: 1
              featureId:
                type: string
                description: Feature/owner that provides this hook (label only).
                order: 4
          ocr:
            order: 3
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: OCR
            description: "Extracts text from document pages. Enable Textract features (LAYOUT, TABLES, FORMS) or use the BDA backend to add layout, tables, and forms support."
            dependsOn: { field: "use_bda", value: false }
            required:
              - features
            properties:
              image:
                type: object
                sectionLabel: Image Processing Settings
                description: Configure image resolution and processing options for OCR
                order: 0
                properties:
                  dpi:
                    type: number
                    description: "DPI (dots per inch) for extracting images from PDF pages. Higher values produce better quality but larger images. Default: 150"
                    minimum: 72
                    maximum: 300
                    default: 150
                    order: 0
                  target_width:
                    type: number
                    description: "Target image width in pixels. Images larger than this will be resized. If not specified, automatic default of 951 pixels is applied to optimize resource usage and token consumption."
                    minimum: 100
                    maximum: 3500
                    order: 1
                  target_height:
                    type: number
                    description: "Target image height in pixels. Images larger than this will be resized. If not specified, automatic default of 1268 pixels is applied to optimize resource usage and token consumption."
                    minimum: 100
                    maximum: 3500
                    order: 2
                  preprocessing:
                    type: boolean
                    description: "Apply adaptive binarization preprocessing to improve OCR accuracy on documents with uneven lighting or low contrast. Warning: May slightly increase processing time."
                    default: false
                    order: 3
              backend:
                type: string
                title: OCR backend
                description: >-
                  OCR engine. 'textract' (default): AWS Textract — cheapest for
                  plain text ($1.50/1K pages raw) and best when you need only raw
                  text or already tune specific Textract features (Tables
                  $15/1K, Forms $50/1K). 'bda': Amazon Bedrock Data Automation —
                  flat $10/1K pages returning markdown with tables and layout
                  plus word-level confidence/bounding boxes in one call; choose
                  it for table-aware OCR (enables the extraction table tool) and
                  predictable pricing without composing Textract features.
                  'bedrock': LLM-based OCR. 'none': image-only, no OCR.
                enum: ["textract", "bedrock", "bda", "none"]
                default: "textract"
                order: 1
              model_id:
                type: string
                title: Model
                description: "Bedrock model ID (only used if backend is 'bedrock'). Select 'LambdaHook' to use a custom Lambda function for inference."
                enum:
                  - "LambdaHook"
                  - "us.amazon.nova-lite-v1:0"
                  - "us.amazon.nova-pro-v1:0"
                  - "us.amazon.nova-premier-v1:0"
                  - "us.amazon.nova-2-lite-v1:0"
                  - "us.amazon.nova-2-lite-v1:0:priority"
                  - "us.amazon.nova-2-lite-v1:0:flex"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.amazon.nova-2-lite-v1:0:priority"
                  - "global.amazon.nova-2-lite-v1:0:flex"
                  - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "us.anthropic.claude-sonnet-4-6"
                  - "us.anthropic.claude-sonnet-4-6:1m"
                  - "us.anthropic.claude-sonnet-5"
                  - "us.anthropic.claude-sonnet-5:1m"
                  - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "us.anthropic.claude-opus-4-6-v1"
                  - "us.anthropic.claude-opus-4-6-v1:1m"
                  - "us.anthropic.claude-opus-4-7"
                  - "us.anthropic.claude-opus-4-7:1m"
                  - "us.anthropic.claude-opus-4-8"
                  - "us.anthropic.claude-opus-4-8:1m"
                  - "us.anthropic.claude-opus-5"
                  - "us.anthropic.claude-opus-5:1m"
                  - "openai.gpt-5.4"
                  - "openai.gpt-5.5"
                  - "openai.gpt-5.6-sol"
                  - "openai.gpt-5.6-terra"
                  - "openai.gpt-5.6-luna"
                  - "eu.amazon.nova-lite-v1:0"
                  - "eu.amazon.nova-pro-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0:priority"
                  - "eu.amazon.nova-2-lite-v1:0:flex"
                  - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "eu.anthropic.claude-sonnet-4-6"
                  - "eu.anthropic.claude-sonnet-4-6:1m"
                  - "eu.anthropic.claude-sonnet-5"
                  - "eu.anthropic.claude-sonnet-5:1m"
                  - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "eu.anthropic.claude-opus-4-6-v1"
                  - "eu.anthropic.claude-opus-4-6-v1:1m"
                  - "eu.anthropic.claude-opus-4-7"
                  - "eu.anthropic.claude-opus-4-7:1m"
                  - "eu.anthropic.claude-opus-4-8"
                  - "eu.anthropic.claude-opus-4-8:1m"
                  - "eu.anthropic.claude-opus-5"
                  - "eu.anthropic.claude-opus-5:1m"
                  - "qwen.qwen3-vl-235b-a22b"
                  - "us.meta.llama4-maverick-17b-instruct-v1:0"
                  - "us.meta.llama4-scout-17b-instruct-v1:0"
                  - "google.gemma-3-27b-it"
                  - "nvidia.nemotron-nano-12b-v2"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "global.anthropic.claude-sonnet-4-6:1m"
                  - "global.anthropic.claude-sonnet-5"
                  - "global.anthropic.claude-sonnet-5:1m"
                  - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "global.anthropic.claude-opus-4-6-v1"
                  - "global.anthropic.claude-opus-4-6-v1:1m"
                  - "global.anthropic.claude-opus-4-7"
                  - "global.anthropic.claude-opus-4-7:1m"
                  - "global.anthropic.claude-opus-4-8"
                  - "global.anthropic.claude-opus-4-8:1m"
                  - "global.anthropic.claude-opus-5"
                  - "global.anthropic.claude-opus-5:1m"
                order: 2
                dependsOn: { field: "backend", value: "bedrock" }
              model_lambda_hook_arn:
                type: string
                title: Model Lambda hook ARN
                description: "Lambda function ARN for custom inference (used when model_id is 'LambdaHook'). Function name must start with GENAIIDP-."
                default: ""
                order: 3
                dependsOn: { field: "model_id", value: "LambdaHook" }
              reasoning_effort:
                type: string
                title: Reasoning effort
                enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                default: "medium"
                description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Higher effort costs more output tokens with little accuracy gain for extraction (measured), so lower is usually better. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                order: 4
                dependsOn: { field: "model_id", reasoningCapable: true }
              features:
                type: array
                title: Textract features
                listLabel: "Textract Features"
                itemLabel: "Feature"
                description: "Textract features (only used if backend is 'textract')"
                order: 6
                dependsOn: { field: "backend", values: ["textract"] }
                items:
                  type: object
                  required:
                    - name
                  properties:
                    name:
                      type: string
                      description: "Feature - select one of the supported OCR feature types"
                      enum: ["TABLES", "FORMS", "SIGNATURES", "LAYOUT"]
              system_prompt:
                type: string
                title: System prompt
                format: textarea
                description: "System prompt for Bedrock OCR (only used if backend is 'bedrock')"
                default: "You are an expert OCR system. Extract all text from the provided image accurately, preserving layout where possible."
                order: 8
                dependsOn: { field: "backend", value: "bedrock" }
              task_prompt:
                type: string
                title: Task prompt
                format: textarea
                description: "Task prompt for Bedrock OCR (only used if backend is 'bedrock')"
                default: "Extract all text from this document image. Preserve the layout, including paragraphs, tables, and formatting."
                order: 9
                dependsOn: { field: "backend", value: "bedrock" }
              postHook:
                type: array
                collapsible: true
                defaultExpanded: false
                sectionLabel: Post-step pipeline hooks
                description: >-
                  Lambda functions invoked after this step completes,
                  before the next step runs. Each hook receives the
                  document state and returns a JSON-serializable result
                  (logged for traceability). Hooks let vertical-product
                  packs (claims, loans, etc.) inject business logic
                  without forking the accelerator. Admins can edit, add,
                  reorder, or disable hooks here. The Lambda function
                  ARN must EITHER be tagged with `idp:feature-id=<anything>`
                  (vertical packs do this automatically) OR have a function
                  name starting with `GENAIIDP-` (admin-managed Lambdas).
                  Functions outside both categories will be rejected by
                  the dispatcher's IAM policy at invoke time.
                listLabel: Post-step pipeline hooks
                itemLabel: Hook
                order: 99
                items:
                  type: object
                  required:
                    - featureId
                    - arn
                  properties:
                    featureId:
                      type: string
                      description: Feature that owns this hook (label only)
                      order: 0
                    arn:
                      type: string
                      description: >-
                        Lambda function ARN to invoke. Must either be
                        tagged `idp:feature-id=<anything>` OR have a
                        function name beginning with `GENAIIDP-`.
                      order: 1
                    order:
                      type: number
                      description: Execution order (lower = earlier)
                      default: 100
                      order: 2
                    onError:
                      type: string
                      enum: ["continue", "fail", "skip-remaining"]
                      default: continue
                      description: Behavior when this hook errors
                      order: 3
                    enabled:
                      type: boolean
                      description: Disable without removing
                      default: true
                      order: 4
          classes:
            order: 2
            type: array
            sectionLabel: Class definitions
            listLabel: Classes
            itemLabel: Class
            columns: 2
            items:
              type: object
              required:
                - name
                - description
                - attributes
              properties:
                name:
                  type: string
                  title: Class name
                  description: Class name
                  order: 0
                description:
                  type: string
                  title: Class description
                  description: Class description. Instruct LLM how to identify the class from the context provided in the classification prompt.
                  order: 1
                document_name_regex:
                  type: string
                  title: Document name regex
                  description: "Optional regex pattern to match against document ID/name. When matched, all pages will be classified as this class type without LLM processing. Use only when all pages should be the same class."
                  order: 2
                document_page_content_regex:
                  type: string
                  title: Page content regex
                  description: "Optional regex pattern to match against page content text. When matched during multi-modal page-level classification, the page will be classified as this class type without LLM processing."
                  order: 3
                extraction_model:
                  type: string
                  title: Extraction model override
                  description: "Optional per-class extraction model override. When set, this model is used for extraction instead of the global extraction.model. Useful for classes that benefit from a different model. NOTE: OpenAI GPT-5.x models (openai.gpt-5.*) are NOT compatible with 'advanced' Extraction mode; that combination is rejected by config validation."
                  order: 4
                  enum:
                    - ""
                    - "us.amazon.nova-lite-v1:0"
                    - "us.amazon.nova-pro-v1:0"
                    - "us.amazon.nova-premier-v1:0"
                    - "us.amazon.nova-2-lite-v1:0"
                    - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                    - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                    - "us.anthropic.claude-sonnet-4-6"
                    - "us.anthropic.claude-sonnet-4-6:1m"
                    - "us.anthropic.claude-sonnet-5"
                    - "us.anthropic.claude-sonnet-5:1m"
                    - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                    - "us.anthropic.claude-opus-4-6-v1"
                    - "us.anthropic.claude-opus-4-6-v1:1m"
                    - "us.anthropic.claude-opus-4-7"
                    - "us.anthropic.claude-opus-4-7:1m"
                    - "us.anthropic.claude-opus-4-8"
                    - "us.anthropic.claude-opus-4-8:1m"
                    - "us.anthropic.claude-opus-5"
                    - "us.anthropic.claude-opus-5:1m"
                    - "openai.gpt-5.4"
                    - "openai.gpt-5.5"
                    - "openai.gpt-5.6-sol"
                    - "openai.gpt-5.6-terra"
                    - "openai.gpt-5.6-luna"
                    - "eu.amazon.nova-lite-v1:0"
                    - "eu.amazon.nova-pro-v1:0"
                    - "eu.amazon.nova-2-lite-v1:0"
                    - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                    - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                    - "eu.anthropic.claude-sonnet-4-6"
                    - "eu.anthropic.claude-sonnet-4-6:1m"
                    - "eu.anthropic.claude-sonnet-5"
                    - "eu.anthropic.claude-sonnet-5:1m"
                    - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                    - "eu.anthropic.claude-opus-4-6-v1"
                    - "eu.anthropic.claude-opus-4-6-v1:1m"
                    - "eu.anthropic.claude-opus-4-7"
                    - "eu.anthropic.claude-opus-4-7:1m"
                    - "eu.anthropic.claude-opus-4-8"
                    - "eu.anthropic.claude-opus-4-8:1m"
                    - "eu.anthropic.claude-opus-5"
                    - "eu.anthropic.claude-opus-5:1m"
                    - "global.amazon.nova-2-lite-v1:0"
                    - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                    - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                    - "global.anthropic.claude-sonnet-4-6"
                    - "global.anthropic.claude-sonnet-4-6:1m"
                    - "global.anthropic.claude-sonnet-5"
                    - "global.anthropic.claude-sonnet-5:1m"
                    - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                    - "global.anthropic.claude-opus-4-6-v1"
                    - "global.anthropic.claude-opus-4-6-v1:1m"
                    - "global.anthropic.claude-opus-4-7"
                    - "global.anthropic.claude-opus-4-7:1m"
                    - "global.anthropic.claude-opus-4-8"
                    - "global.anthropic.claude-opus-4-8:1m"
                    - "global.anthropic.claude-opus-5"
                    - "global.anthropic.claude-opus-5:1m"
                examples:
                  type: array
                  description: Class few-shot examples
                  listLabel: Few-shot examples
                  itemLabel: Few-shot example
                  items:
                    type: object
                    required:
                      - name
                      - classPrompt
                      - attributesPrompt
                      - imagesPath
                    properties:
                      name:
                        type: string
                        description: Example name
                        order: 0
                      classPrompt:
                        type: string
                        description: Classification prompt as few-shot classification example for this class
                        order: 2
                      attributesPrompt:
                        type: string
                        description: Extraction prompt as few-shot attribute value extraction example for this class.
                        order: 3
                      imagePath:
                        type: string
                        description: Image(s) Path in the Configuration Bucket. Supports single files, or S3 prefixes for folders with multiple images. Optional but recommended for better visual understanding.
                        order: 4
                attributes:
                  type: array
                  listLabel: Attributes
                  itemLabel: Attribute
                  items:
                    type: object
                    required:
                      - name
                      - description
                      - attributeType
                    properties:
                      name:
                        type: string
                        description: Attribute name
                        order: 0
                      description:
                        type: string
                        description: Attribute description. Instruct the LLM how to identify the attribute from the context provided in the extraction prompt. Include synonyms and positional infomation where it might help the LLM to resolve ambiquities.
                        order: 1
                      attributeType:
                        type: string
                        description: Type of attribute
                        enum: ["simple", "group", "list"]
                        order: 2
                      # Simple attribute properties (existing fields)
                      evaluation_method:
                        type: string
                        description: Evaluation Method
                        enum:
                          [
                            "",
                            "LLM",
                            "SEMANTIC",
                            "FUZZY",
                            "NUMERIC_EXACT",
                            "EXACT",
                          ]
                        order: 3
                        dependsOn: { field: "attributeType", value: "simple" }
                      evaluation_threshold:
                        type: number
                        description: Evaluation Threshold - used for SEMANTIC, and FUZZY methods
                        minimum: 0
                        maximum: 1
                        order: 5
                        dependsOn:
                          {
                            field: "evaluation_method",
                            values: ["SEMANTIC", "FUZZY"],
                          }
                      confidence_threshold:
                        type: number
                        description: Confidence threshold for this specific attribute (0.0 to 1.0). If not set, the default confidence threshold from assessment section will be used.
                        minimum: 0
                        maximum: 1
                        order: 6
                        dependsOn: { field: "attributeType", value: "simple" }
                      # Group-specific properties
                      groupAttributes:
                        type: array
                        listLabel: Group Attributes
                        itemLabel: Attribute
                        order: 7
                        dependsOn: { field: "attributeType", value: "group" }
                        items:
                          type: object
                          required:
                            - name
                            - description
                          properties:
                            name:
                              type: string
                              description: Attribute name
                              order: 0
                            description:
                              type: string
                              description: Attribute description. Instruct the LLM how to identify the attribute from the context provided in the extraction prompt. Include synonyms and positional infomation where it might help the LLM to resolve ambiquities.
                              order: 1
                            evaluation_method:
                              type: string
                              description: Evaluation Method
                              enum:
                                [
                                  "",
                                  "LLM",
                                  "SEMANTIC",
                                  "FUZZY",
                                  "NUMERIC_EXACT",
                                  "EXACT",
                                ]
                              order: 2
                            evaluation_threshold:
                              type: number
                              description: Evaluation Threshold - used for SEMANTIC, and FUZZY methods
                              minimum: 0
                              maximum: 1
                              order: 4
                              dependsOn:
                                {
                                  field: "evaluation_method",
                                  values: ["SEMANTIC", "FUZZY"],
                                }
                            confidence_threshold:
                              type: number
                              description: Confidence threshold for this specific attribute (0.0 to 1.0). If not set, the default confidence threshold from assessment section will be used.
                              minimum: 0
                              maximum: 1
                              order: 5
                          columns: 2
                      # List-specific properties
                      listItemTemplate:
                        type: object
                        sectionLabel: List Item Template
                        order: 8
                        dependsOn: { field: "attributeType", value: "list" }
                        required:
                          - itemAttributes
                        properties:
                          itemDescription:
                            type: string
                            description: Description of each list item. Instruct the LLM how to identify the list item (e.g. a transaction in a bank statement).
                            order: 0
                          itemAttributes:
                            type: array
                            listLabel: Item Attributes
                            itemLabel: Attribute
                            order: 1
                            items:
                              type: object
                              required:
                                - name
                                - description
                              properties:
                                name:
                                  type: string
                                  description: Attribute name
                                  order: 0
                                description:
                                  type: string
                                  description: Attribute description. Instruct the LLM how to identify the attribute from the context provided in the extraction prompt. Include synonyms and positional infomation where it might help the LLM to resolve ambiquities.
                                  order: 1
                                evaluation_method:
                                  type: string
                                  description: Evaluation Method
                                  enum:
                                    [
                                      "",
                                      "LLM",
                                      "SEMANTIC",
                                      "FUZZY",
                                      "NUMERIC_EXACT",
                                      "EXACT",
                                    ]
                                  order: 2
                                evaluation_threshold:
                                  type: number
                                  description: Evaluation Threshold - used for SEMANTIC, and FUZZY methods
                                  minimum: 0
                                  maximum: 1
                                  order: 4
                                  dependsOn:
                                    {
                                      field: "evaluation_method",
                                      values: ["SEMANTIC", "FUZZY"],
                                    }
                                confidence_threshold:
                                  type: number
                                  description: Confidence threshold for this specific attribute (0.0 to 1.0). If not set, the default confidence threshold from assessment section will be used.
                                  minimum: 0
                                  maximum: 1
                                  order: 5
                              columns: 2
                    # This controls the layout of the form fields inside each attribute item
                    columns: 2
          classification:
            order: 4
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Classification
            description: "Assigns a class to each page/section. LLM-powered classification is skipped when only a single class is defined in the Document Schema (all pages are assigned to that class), or when a document name or page content matches a class's RegEx rule."
            dependsOn: { field: "use_bda", value: false }
            required:
              - model
              - temperature
              - top_k
              - top_p
              - system_prompt
              - task_prompt
            properties:
              image:
                type: object
                sectionLabel: Image Processing Settings
                description: Configure image resolution and processing options for image attachments in classification
                order: 0
                properties:
                  target_width:
                    type: number
                    description: "Target image width in pixels. Images larger than this will be resized. Default: 951"
                    minimum: 100
                    maximum: 3500
                    order: 0
                  target_height:
                    type: number
                    description: "Target image height in pixels. Images larger than this will be resized. Default: 1268"
                    minimum: 100
                    maximum: 3500
                    order: 1
              model:
                type: string
                title: Model
                description: "Model identifier. Select 'LambdaHook' to use a custom Lambda function for inference."
                enum:
                  - "LambdaHook"
                  - !If
                    - HasCustomClassificationModelARN
                    - !Ref CustomClassificationModelARN
                    - !Ref AWS::NoValue
                  - "us.amazon.nova-lite-v1:0"
                  - "us.amazon.nova-pro-v1:0"
                  - "us.amazon.nova-premier-v1:0"
                  - "us.amazon.nova-2-lite-v1:0"
                  - "us.amazon.nova-2-lite-v1:0:priority"
                  - "us.amazon.nova-2-lite-v1:0:flex"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.amazon.nova-2-lite-v1:0:priority"
                  - "global.amazon.nova-2-lite-v1:0:flex"
                  - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "us.anthropic.claude-sonnet-4-6"
                  - "us.anthropic.claude-sonnet-4-6:1m"
                  - "us.anthropic.claude-sonnet-5"
                  - "us.anthropic.claude-sonnet-5:1m"
                  - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "us.anthropic.claude-opus-4-6-v1"
                  - "us.anthropic.claude-opus-4-6-v1:1m"
                  - "us.anthropic.claude-opus-4-7"
                  - "us.anthropic.claude-opus-4-7:1m"
                  - "us.anthropic.claude-opus-4-8"
                  - "us.anthropic.claude-opus-4-8:1m"
                  - "us.anthropic.claude-opus-5"
                  - "us.anthropic.claude-opus-5:1m"
                  - "openai.gpt-5.4"
                  - "openai.gpt-5.5"
                  - "openai.gpt-5.6-sol"
                  - "openai.gpt-5.6-terra"
                  - "openai.gpt-5.6-luna"
                  - "eu.amazon.nova-lite-v1:0"
                  - "eu.amazon.nova-pro-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0:priority"
                  - "eu.amazon.nova-2-lite-v1:0:flex"
                  - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "eu.anthropic.claude-sonnet-4-6"
                  - "eu.anthropic.claude-sonnet-4-6:1m"
                  - "eu.anthropic.claude-sonnet-5"
                  - "eu.anthropic.claude-sonnet-5:1m"
                  - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "eu.anthropic.claude-opus-4-6-v1"
                  - "eu.anthropic.claude-opus-4-6-v1:1m"
                  - "eu.anthropic.claude-opus-4-7"
                  - "eu.anthropic.claude-opus-4-7:1m"
                  - "eu.anthropic.claude-opus-4-8"
                  - "eu.anthropic.claude-opus-4-8:1m"
                  - "eu.anthropic.claude-opus-5"
                  - "eu.anthropic.claude-opus-5:1m"
                  - "qwen.qwen3-vl-235b-a22b"
                  - "us.meta.llama4-maverick-17b-instruct-v1:0"
                  - "us.meta.llama4-scout-17b-instruct-v1:0"
                  - "google.gemma-3-27b-it"
                  - "nvidia.nemotron-nano-12b-v2"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "global.anthropic.claude-sonnet-4-6"
                  - "global.anthropic.claude-sonnet-4-6:1m"
                  - "global.anthropic.claude-sonnet-5"
                  - "global.anthropic.claude-sonnet-5:1m"
                  - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "global.anthropic.claude-opus-4-6-v1"
                  - "global.anthropic.claude-opus-4-6-v1:1m"
                  - "global.anthropic.claude-opus-4-7"
                  - "global.anthropic.claude-opus-4-7:1m"
                  - "global.anthropic.claude-opus-4-8"
                  - "global.anthropic.claude-opus-4-8:1m"
                  - "global.anthropic.claude-opus-5"
                  - "global.anthropic.claude-opus-5:1m"
                order: 1
              model_lambda_hook_arn:
                type: string
                title: Model Lambda hook ARN
                description: "Lambda function ARN for custom inference (used when model is 'LambdaHook'). Function name must start with GENAIIDP-."
                default: ""
                order: 3
                dependsOn: { field: "model", value: "LambdaHook" }
              model_params:
                type: object
                title: Model parameters
                sectionLabel: Model parameters
                # ghostGroup => children save at the PARENT path (classification.temperature,
                # classification.max_tokens, ...), so this is a purely visual grouping.
                ghostGroup: true
                collapsible: true
                defaultExpanded: false
                description: "Advanced inference tuning (sampling + output limits). Defaults are sensible; most users never change these."
                order: 2
                properties:
                  temperature:
                    type: number
                    title: Temperature
                    minimum: 0
                    maximum: 1
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Used when top_p is 0. Set temperature=0.0 for consistent, deterministic outputs. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 1
                  top_k:
                    type: integer
                    title: Top K
                    minimum: 1
                    description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 2
                  top_p:
                    type: number
                    title: Top P
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0). When set to a positive value (> 0), overrides temperature parameter. Set to 0 to use temperature instead. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 3
                  max_tokens:
                    type: number
                    title: Max tokens
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                  reasoning_effort:
                    type: string
                    title: Reasoning effort
                    enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                    default: "medium"
                    description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Higher effort costs more output tokens with little accuracy gain for extraction (measured), so lower is usually better. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                    order: 5
                    dependsOn: { field: "model", reasoningCapable: true }
              classificationMethod:
                type: string
                title: Classification method
                description: "Classification methodology to use"
                enum:
                  [
                    "multimodalPageLevelClassification",
                    "textbasedHolisticClassification",
                  ]
                order: 2
              maxPagesForClassification:
                type: string
                title: Pages to classify
                description: "Number of pages to use for classification [When set to a number, forces the entire document to be assigned a single class]"
                enum: ["ALL", "1", "2", "3", "5", "10"]
                order: 4
              sectionSplitting:
                type: string
                title: Section splitting
                description: "Section splitting strategy: 'disabled' (entire doc as one section), 'page' (one section per page - prevents joining same-type documents), 'llm_determined' (use LLM boundary detection - default)"
                enum: ["disabled", "page", "llm_determined"]
                default: "llm_determined"
                order: 5
              contextPagesCount:
                type: integer
                title: Context pages
                description: "Number of surrounding pages to include as context for multi-modal page-level classification. Value of 0 means no additional context (default). Value of 1 includes 1 page before and after. Value of 2 includes 2 pages before and after. Only applies to multimodalPageLevelClassification method."
                minimum: 0
                maximum: 5
                default: 0
                order: 6
                dependsOn: { field: "classificationMethod", value: "multimodalPageLevelClassification" }
              enforceValidClasses:
                type: boolean
                title: Enforce valid classes
                description: "When enabled, the predicted class is validated against the defined class vocabulary. If the model returns a class that is not in the list, it is re-prompted with the valid choices and retried (see Max Validation Retries). If retries are exhausted, the page is assigned the Invalid Class Fallback and flagged with a validation error. When disabled, an out-of-vocabulary prediction is used as-is (legacy behavior). Only applies to multimodalPageLevelClassification."
                default: true
                order: 7
                dependsOn: { field: "classificationMethod", value: "multimodalPageLevelClassification" }
              maxValidationRetries:
                type: integer
                title: Max validation retries
                description: "Maximum number of times to re-prompt the model when it returns a class outside the defined vocabulary. Only used when Enforce Valid Classes is enabled. 0 disables retries (a single invalid prediction goes straight to the fallback)."
                minimum: 0
                maximum: 5
                default: 2
                order: 8
                dependsOn: { field: "enforceValidClasses", value: true }
              invalidClassFallback:
                type: string
                title: Invalid class fallback
                description: "Class label assigned to a page when all validation retries are exhausted. Should be one of your defined classes, or the built-in 'unclassified'. Only used when Enforce Valid Classes is enabled."
                default: "unclassified"
                order: 9
                dependsOn: { field: "enforceValidClasses", value: true }
              system_prompt:
                type: string
                title: System prompt
                format: textarea
                description: System prompt
                order: 15
              task_prompt:
                type: string
                title: Task prompt
                format: textarea
                description: Task prompt - include placeholders {CLASS_NAMES_AND_DESCRIPTIONS} (replaced with the class names and descriptions for all specified classes), {FEW_SHOT_EXAMPLES} (replaced by classPrompt and image data from examples in class definitions), {DOCUMENT_TEXT} (replaced by the OCR output), and for multi-modal classification {DOCUMENT_IMAGE} (replaced by the page image attachment). Optionally include {CLASS_AND_ATTRIBUTE_NAMES_AND_DESCRIPTIONS} (replaced with class names, descriptions, AND schema attribute field names — useful for disambiguating classes with similar names but different extraction schemas; opt-in and cost-neutral when not referenced). Optionally use <<CACHEPOINT>> to separate static and dynamic elements of prompt for Bedrock prompt caching.
                order: 16
              postHook:
                type: array
                collapsible: true
                defaultExpanded: false
                sectionLabel: Post-step pipeline hooks
                description: >-
                  Lambda functions invoked after this step completes,
                  before the next step runs. Each hook receives the
                  document state and returns a JSON-serializable result
                  (logged for traceability). Hooks let vertical-product
                  packs (claims, loans, etc.) inject business logic
                  without forking the accelerator. Admins can edit, add,
                  reorder, or disable hooks here. The Lambda function
                  ARN must EITHER be tagged with `idp:feature-id=<anything>`
                  (vertical packs do this automatically) OR have a function
                  name starting with `GENAIIDP-` (admin-managed Lambdas).
                  Functions outside both categories will be rejected by
                  the dispatcher's IAM policy at invoke time.
                listLabel: Post-step pipeline hooks
                itemLabel: Hook
                order: 99
                items:
                  type: object
                  required:
                    - featureId
                    - arn
                  properties:
                    featureId:
                      type: string
                      description: Feature that owns this hook (label only)
                      order: 0
                    arn:
                      type: string
                      description: >-
                        Lambda function ARN to invoke. Must either be
                        tagged `idp:feature-id=<anything>` OR have a
                        function name beginning with `GENAIIDP-`.
                      order: 1
                    order:
                      type: number
                      description: Execution order (lower = earlier)
                      default: 100
                      order: 2
                    onError:
                      type: string
                      enum: ["continue", "fail", "skip-remaining"]
                      default: continue
                      description: Behavior when this hook errors
                      order: 3
                    enabled:
                      type: boolean
                      description: Disable without removing
                      default: true
                      order: 4
          extraction:
            order: 5
            type: object
            format: section
            collapsible: true
            defaultExpanded: false
            sectionLabel: Extraction
            description: "Extracts structured attributes from each section. LLM-powered extraction is skipped for any section whose class has no attributes defined in the Document Schema (an empty extraction result is recorded)."
            dependsOn: { field: "use_bda", value: false }
            required:
              - model
              - temperature
              - top_k
              - top_p
              - system_prompt
              - task_prompt
            properties:
              mode:
                type: string
                title: Extraction mode
                enum: ["simple", "advanced"]
                default: "simple"
                order: 1
                description: "'simple' (default): single-pass extraction — fast and cheap, best for short documents. 'advanced': the robust, sharded extraction engine for large documents, big tables, and completeness (exposes the Advanced extraction settings below). Recommended with Anthropic Claude models."
              agentic:
                type: object
                sectionLabel: Advanced extraction settings
                description: "Tuning for Advanced extraction mode: page sharding/concurrency, deterministic table parsing, and schema validation/escalation. Shown only when Extraction mode is 'advanced'."
                order: 2
                dependsOn: { field: "extraction.mode", value: "advanced" }
                properties:
                  max_concurrent_batches:
                    type: number
                    description: "Max concurrent extraction agents / shards. 1 = single pass. >1 shards a section's pages into ranges (each agent sees only its pages) and runs them concurrently — speeds up long documents and avoids context-window overflow. Higher values raise Bedrock requests/min; tune to your quota. Default: 10."
                    minimum: 1
                    maximum: 10
                    default: 10
                    order: 1
                  max_pages_per_shard:
                    type: number
                    description: "Max pages per shard (wall-clock guard). Shard token budget is auto-sized from the model's context window, but a shard is still capped at this many pages so very large documents can't collapse into one long-running agent. 0 = page cap off (token budget alone). Default: 5."
                    minimum: 0
                    maximum: 100
                    default: 5
                    order: 2
                  table_parsing:
                    type: object
                    sectionLabel: Table Parsing Tool (Requires OCR TABLES enabled)
                    description: "Only used when Agentic Extraction is enabled. Gives the agent a deterministic parse_table tool that extracts well-formatted Markdown tables from OCR output without LLM inference - faster and more reliable for large/dense tables (bank statements, transaction logs). The agent decides when to use it based on table quality."
                    order: 4
                    properties:
                      enabled:
                        type: boolean
                        description: "Enable the parse_table tool. When enabled, the agent can use deterministic table parsing for well-formatted OCR tabular data instead of generating table rows with the LLM."
                        order: 0
                        default: false
                      min_confidence_threshold:
                        type: number
                        description: "Minimum average OCR text confidence (Textract 0-100 scale) for the agent to prefer table parsing over LLM extraction. Included in the agent system prompt as guidance. Default: 95."
                        minimum: 0
                        maximum: 100
                        default: 95
                        order: 1
                        dependsOn: { field: "enabled", value: true }
                      min_parse_success_rate:
                        type: number
                        description: "Minimum parse_success_rate from the parse_table tool for the agent to trust parsed results. Below this threshold, the agent falls back to LLM extraction. Default: 0.9."
                        minimum: 0
                        maximum: 1
                        default: 0.9
                        order: 2
                        dependsOn: { field: "enabled", value: true }
                      use_confidence_data:
                        type: boolean
                        description: "Load and provide OCR confidence data to the parse_table tool for quality assessment. Requires text_confidence_uri from Textract OCR (no effect with other OCR backends). Default: on."
                        default: true
                        order: 3
                        dependsOn: { field: "enabled", value: true }
                      max_empty_line_gap:
                        type: number
                        description: "Maximum consecutive empty lines tolerated inside a table before treating it as a boundary - helps recover tables split by OCR page breaks. Higher = more tolerant but may merge unrelated tables. Tuning: 2 for high-quality OCR, 3 standard, 5-7 for noisy documents. Default: 3."
                        minimum: 0
                        maximum: 10
                        default: 3
                        order: 4
                        dependsOn: { field: "enabled", value: true }
                      auto_merge_adjacent_tables:
                        type: boolean
                        description: "Automatically merge consecutive tables with identical columns (recovers tables split by page breaks). Disable if a document legitimately contains multiple separate tables with the same columns. Default: on."
                        default: true
                        order: 5
                        dependsOn: { field: "enabled", value: true }
                      lazy_images:
                        type: boolean
                        description: "When a table parse succeeds, skip pre-loading page images into the extraction prompt (the parser is text-driven; the agent can still fetch a page via view_image). Big cost saver on multi-page documents. Turn off for image-dependent corpora where the model must see page layout even when a table is present. Default: on."
                        default: true
                        order: 6
                        dependsOn: { field: "enabled", value: true }
                  validation:
                    type: object
                    sectionLabel: Schema Validation & Escalation
                    description: "Only used when Agentic Extraction is enabled. Validates the agent's output against the full class JSON Schema (notably 'format' keywords like date/email/uuid that the model's type validation does not enforce) and optionally escalates failing fields to a stronger model. Off by default."
                    order: 5
                    properties:
                      enabled:
                        type: boolean
                        description: "Enable full JSON-Schema validation of the extraction result. Violations are fed back to the agent for self-correction; remaining failures are handled per Fail Action below."
                        order: 0
                        default: false
                      check_formats:
                        type: boolean
                        description: "Enforce JSON-Schema 'format' keywords (date, date-time, email, uri, uuid, ...). Note: 'format: date' means ISO-8601 (YYYY-MM-DD); disable this if your schema uses 'format: date' for non-ISO values such as MM/DD/YYYY (or use a 'pattern' instead). Default: on."
                        order: 1
                        default: true
                        dependsOn: { field: "enabled", value: true }
                      fail_action:
                        type: string
                        description: "What to do when validation still fails after the agent's own retries. 'warn': record the outcome in metadata and proceed. 'escalate': re-extract only the failing fields with the Escalation Model (roughly doubles that section's cost, still far cheaper than human review), then warn if still invalid. 'reject': mark the section as failed so downstream/HITL can act. Default: escalate."
                        enum: ["warn", "escalate", "reject"]
                        order: 2
                        default: "escalate"
                        dependsOn: { field: "enabled", value: true }
                      escalation_model:
                        type: string
                        description: "Stronger Bedrock model used to re-extract failing fields (used when Fail Action is 'escalate'). Select blank to retry with the extraction model. Can be overridden per document class via the 'x-aws-idp-extraction-escalation-model' schema extension in the Document Schema editor."
                        order: 3
                        dependsOn: { field: "fail_action", value: "escalate" }
                        hideWhen: { field: "extraction.agentic.validation.enabled", value: false }
                        enum:
                          - ""
                          - "us.amazon.nova-lite-v1:0"
                          - "us.amazon.nova-pro-v1:0"
                          - "us.amazon.nova-premier-v1:0"
                          - "us.amazon.nova-2-lite-v1:0"
                          - "us.amazon.nova-2-lite-v1:0:priority"
                          - "us.amazon.nova-2-lite-v1:0:flex"
                          - "global.amazon.nova-2-lite-v1:0"
                          - "global.amazon.nova-2-lite-v1:0:priority"
                          - "global.amazon.nova-2-lite-v1:0:flex"
                          - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                          - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                          - "us.anthropic.claude-sonnet-4-6"
                          - "us.anthropic.claude-sonnet-4-6:1m"
                          - "us.anthropic.claude-sonnet-5"
                          - "us.anthropic.claude-sonnet-5:1m"
                          - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                          - "us.anthropic.claude-opus-4-6-v1"
                          - "us.anthropic.claude-opus-4-6-v1:1m"
                          - "us.anthropic.claude-opus-4-7"
                          - "us.anthropic.claude-opus-4-7:1m"
                          - "us.anthropic.claude-opus-4-8"
                          - "us.anthropic.claude-opus-4-8:1m"
                          - "us.anthropic.claude-opus-5"
                          - "us.anthropic.claude-opus-5:1m"
                          - "openai.gpt-5.4"
                          - "openai.gpt-5.5"
                          - "openai.gpt-5.6-sol"
                          - "openai.gpt-5.6-terra"
                          - "openai.gpt-5.6-luna"
                          - "eu.amazon.nova-lite-v1:0"
                          - "eu.amazon.nova-pro-v1:0"
                          - "eu.amazon.nova-2-lite-v1:0"
                          - "eu.amazon.nova-2-lite-v1:0:priority"
                          - "eu.amazon.nova-2-lite-v1:0:flex"
                          - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                          - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                          - "eu.anthropic.claude-sonnet-4-6"
                          - "eu.anthropic.claude-sonnet-4-6:1m"
                          - "eu.anthropic.claude-sonnet-5"
                          - "eu.anthropic.claude-sonnet-5:1m"
                          - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                          - "eu.anthropic.claude-opus-4-6-v1"
                          - "eu.anthropic.claude-opus-4-6-v1:1m"
                          - "eu.anthropic.claude-opus-4-7"
                          - "eu.anthropic.claude-opus-4-7:1m"
                          - "eu.anthropic.claude-opus-4-8"
                          - "eu.anthropic.claude-opus-4-8:1m"
                          - "eu.anthropic.claude-opus-5"
                          - "eu.anthropic.claude-opus-5:1m"
                          - "qwen.qwen3-vl-235b-a22b"
                          - "us.meta.llama4-maverick-17b-instruct-v1:0"
                          - "us.meta.llama4-scout-17b-instruct-v1:0"
                          - "google.gemma-3-27b-it"
                          - "nvidia.nemotron-nano-12b-v2"
                          - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                          - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                          - "global.anthropic.claude-sonnet-4-6"
                          - "global.anthropic.claude-sonnet-4-6:1m"
                          - "global.anthropic.claude-sonnet-5"
                          - "global.anthropic.claude-sonnet-5:1m"
                          - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                          - "global.anthropic.claude-opus-4-6-v1"
                          - "global.anthropic.claude-opus-4-6-v1:1m"
                          - "global.anthropic.claude-opus-4-7"
                          - "global.anthropic.claude-opus-4-7:1m"
                          - "global.anthropic.claude-opus-4-8"
                          - "global.anthropic.claude-opus-4-8:1m"
                          - "global.anthropic.claude-opus-5"
                          - "global.anthropic.claude-opus-5:1m"
                        default: ""
                      min_population_ratio:
                        type: number
                        description: "Advisory completeness threshold (0-1). After extraction, the fraction of schema-defined fields that came back populated is computed; if it falls below this value a warning is logged and metadata.population_check is flagged (catches silent loss such as nested fields returning null or a table extracting zero rows). Advisory only - never fails extraction. Set to 0 to disable. Default: 0.5."
                        minimum: 0
                        maximum: 1
                        order: 4
                        default: 0.5
                        dependsOn: { field: "enabled", value: true }
              image:
                type: object
                sectionLabel: Image Processing Settings
                description: Configure image resolution and processing options for image attachments in extraction
                order: 15
                properties:
                  target_width:
                    type: number
                    description: "Target image width in pixels. Images larger than this will be resized. Default: 951"
                    minimum: 100
                    maximum: 3500
                    order: 0
                  target_height:
                    type: number
                    description: "Target image height in pixels. Images larger than this will be resized. Default: 1268"
                    minimum: 100
                    maximum: 3500
                    order: 1
              missing_field_handling:
                type: object
                collapsible: false
                sectionLabel: Missing vs Blank Fields
                description: >-
                  Distinguish a field that is BLANK (its source page IS in the section but the value is empty)
                  from one that is MISSING (its source page is ABSENT from the section — e.g. a multi-page
                  packet where a whole document is not present). Without this, both look identical (empty/null),
                  so you can't tell "the form had this field but it was left blank" from "that page wasn't
                  submitted at all." Applies to both standard and agentic extraction. Requires class schemas to
                  declare 'x-aws-idp-page-types' and properties to declare 'x-aws-idp-source-page-types'. Off by default.
                order: 22
                properties:
                  enabled:
                    type: boolean
                    title: Enable missing vs blank detection
                    description: "When off (default), all unfound fields are treated the same (blank/null). When on, fields whose source page is absent are marked MISSING per the representation below."
                    order: 0
                    default: false
                  representation:
                    type: string
                    title: Missing field representation
                    description: "Only used when enabled. How a MISSING field appears in the output. 'omit': drop the key entirely. 'null_with_metadata': keep the key as null and list it under a sibling 'missing_fields' array. Default: omit."
                    enum: ["omit", "null_with_metadata"]
                    default: "omit"
                    order: 1
                    dependsOn: { field: "enabled", value: true }
              model:
                type: string
                description: "Model identifier. Select 'LambdaHook' to use a custom Lambda function for inference. NOTE: OpenAI GPT-5.x models (openai.gpt-5.*) run via the bedrock-mantle Responses API and are NOT compatible with 'advanced' Extraction mode; that combination is rejected by config validation."
                enum:
                  - "LambdaHook"
                  - !If
                    - HasCustomExtractionModelARN
                    - !Ref CustomExtractionModelARN
                    - !Ref AWS::NoValue
                  - "us.amazon.nova-lite-v1:0"
                  - "us.amazon.nova-pro-v1:0"
                  - "us.amazon.nova-premier-v1:0"
                  - "us.amazon.nova-2-lite-v1:0"
                  - "us.amazon.nova-2-lite-v1:0:priority"
                  - "us.amazon.nova-2-lite-v1:0:flex"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.amazon.nova-2-lite-v1:0:priority"
                  - "global.amazon.nova-2-lite-v1:0:flex"
                  - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "us.anthropic.claude-sonnet-4-6"
                  - "us.anthropic.claude-sonnet-4-6:1m"
                  - "us.anthropic.claude-sonnet-5"
                  - "us.anthropic.claude-sonnet-5:1m"
                  - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "us.anthropic.claude-opus-4-6-v1"
                  - "us.anthropic.claude-opus-4-6-v1:1m"
                  - "us.anthropic.claude-opus-4-7"
                  - "us.anthropic.claude-opus-4-7:1m"
                  - "us.anthropic.claude-opus-4-8"
                  - "us.anthropic.claude-opus-4-8:1m"
                  - "us.anthropic.claude-opus-5"
                  - "us.anthropic.claude-opus-5:1m"
                  - "openai.gpt-5.4"
                  - "openai.gpt-5.5"
                  - "openai.gpt-5.6-sol"
                  - "openai.gpt-5.6-terra"
                  - "openai.gpt-5.6-luna"
                  - "eu.amazon.nova-lite-v1:0"
                  - "eu.amazon.nova-pro-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0"
                  - "eu.amazon.nova-2-lite-v1:0:priority"
                  - "eu.amazon.nova-2-lite-v1:0:flex"
                  - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "eu.anthropic.claude-sonnet-4-6"
                  - "eu.anthropic.claude-sonnet-4-6:1m"
                  - "eu.anthropic.claude-sonnet-5"
                  - "eu.anthropic.claude-sonnet-5:1m"
                  - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "eu.anthropic.claude-opus-4-6-v1"
                  - "eu.anthropic.claude-opus-4-6-v1:1m"
                  - "eu.anthropic.claude-opus-4-7"
                  - "eu.anthropic.claude-opus-4-7:1m"
                  - "eu.anthropic.claude-opus-4-8"
                  - "eu.anthropic.claude-opus-4-8:1m"
                  - "eu.anthropic.claude-opus-5"
                  - "eu.anthropic.claude-opus-5:1m"
                  - "qwen.qwen3-vl-235b-a22b"
                  - "us.meta.llama4-maverick-17b-instruct-v1:0"
                  - "us.meta.llama4-scout-17b-instruct-v1:0"
                  - "google.gemma-3-27b-it"
                  - "nvidia.nemotron-nano-12b-v2"
                  - "global.amazon.nova-2-lite-v1:0"
                  - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                  - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                  - "global.anthropic.claude-sonnet-4-6"
                  - "global.anthropic.claude-sonnet-4-6:1m"
                  - "global.anthropic.claude-sonnet-5"
                  - "global.anthropic.claude-sonnet-5:1m"
                  - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                  - "global.anthropic.claude-opus-4-6-v1"
                  - "global.anthropic.claude-opus-4-6-v1:1m"
                  - "global.anthropic.claude-opus-4-7"
                  - "global.anthropic.claude-opus-4-7:1m"
                  - "global.anthropic.claude-opus-4-8"
                  - "global.anthropic.claude-opus-4-8:1m"
                  - "global.anthropic.claude-opus-5"
                  - "global.anthropic.claude-opus-5:1m"
                order: 5
              model_lambda_hook_arn:
                type: string
                description: "Lambda function ARN for custom inference (used when model is 'LambdaHook'). Function name must start with GENAIIDP-."
                default: ""
                order: 6
                dependsOn: { field: "model", value: "LambdaHook" }
              model_params:
                type: object
                title: Model parameters
                sectionLabel: Model parameters
                # ghostGroup => children save at the PARENT path (extraction.temperature,
                # extraction.reasoning_effort, ...), so this is a purely visual grouping.
                # Extraction always requests the model's max output tokens (no max_tokens knob).
                ghostGroup: true
                collapsible: true
                defaultExpanded: false
                description: "Advanced inference tuning (sampling + reasoning effort). Defaults are sensible; most users never change these."
                order: 7
                properties:
                  reasoning_effort:
                    type: string
                    title: Reasoning effort
                    enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                    default: "low"
                    description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Defaults to low. Raise for reasoning-heavy documents. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                    order: 1
                    dependsOn: { field: "model", reasoningCapable: true }
                  temperature:
                    type: number
                    title: Temperature
                    minimum: 0
                    maximum: 1
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Used when top_p is 0. Set temperature=0.0 for consistent, deterministic outputs. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 2
                  top_k:
                    type: integer
                    title: Top K
                    minimum: 1
                    description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 3
                  top_p:
                    type: number
                    title: Top P
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0). When set to a positive value (> 0), overrides temperature parameter. Set to 0 to use temperature instead. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 4
              system_prompt:
                type: string
                description: "System prompt. Can be overridden per document class via the 'x-aws-idp-extraction-system-prompt' schema extension in the Document Schema editor."
                order: 12
              task_prompt:
                type: string
                title: Task prompt (extraction)
                format: textarea
                description: "Extraction-only task prompt — used when confidence runs separately or is disabled. Placeholders: {DOCUMENT_CLASS}, {ATTRIBUTE_NAMES_AND_DESCRIPTIONS}, {FEW_SHOT_EXAMPLES}, {DOCUMENT_TEXT}, {DOCUMENT_IMAGE}. Use <<CACHEPOINT>> for Bedrock prompt caching. Overridable per class via 'x-aws-idp-extraction-task-prompt'."
                order: 13
                hideWhen: { field: "extraction.confidence.mode", value: "integrated" }
              task_prompt_extraction_with_confidence:
                type: string
                title: Task prompt (extraction + confidence — advanced/agentic)
                format: textarea
                description: "INTEGRATED task prompt for ADVANCED (agentic) mode — used when Confidence Assessment mode is 'integrated' and Extraction mode is 'advanced'. Instructs the agent to call the provide_field_assessment tool after extracting. The bounding-box block (Bounding-box Geometry section) is appended automatically when Geometry mode is 'llm' or 'llm_grounded'."
                order: 14
                dependsOn: { field: "extraction.confidence.mode", value: "integrated" }
                hideWhen: { field: "extraction.mode", value: "simple" }
              task_prompt_extraction_with_confidence_topk:
                type: string
                title: Task prompt (1-Stage TopK extraction + confidence — simple)
                format: textarea
                description: "1-Stage TopK INTEGRATED task prompt for SIMPLE (non-agentic) mode — used when Confidence Assessment mode is 'integrated' and Extraction mode is 'simple'. A single LLM call emits its top-K guesses with probabilities per field (G1/P1 … GK/PK); the top guess becomes the value and its probability the confidence. Requesting ranked alternatives yields better-calibrated confidence than single-value self-assessment."
                order: 15
                dependsOn: { field: "extraction.confidence.mode", value: "integrated" }
                hideWhen: { field: "extraction.mode", value: "advanced" }
              custom_prompt_lambda_arn:
                type: string
                title: Custom Prompt Lambda ARN
                format: single-line
                description: "(Optional) ARN of a Lambda function to generate custom extraction prompts. Function name must start with 'GENAIIDP-'. If not provided, default prompts will be used. The Lambda function receives the complete config, prompt placeholders, default task prompt content, and serialized document, and returns custom system_prompt and task_prompt_content. Example: arn:partition:lambda:us-east-1:123456789012:function:GENAIIDP-my-extractor"
                order: 100
              geometry:
                type: object
                order: 4
                collapsible: false
                sectionLabel: Bounding-box Geometry
                description: "How field bounding boxes (geometry) are produced. Geometry is advisory enrichment shown in the UI viewer."
                properties:
                  mode:
                    type: string
                    title: Geometry mode
                    enum: ["ocr_only", "llm_grounded", "llm", "off"]
                    default: "ocr_only"
                    description: "'ocr_only' (default): derive boxes purely by matching each extracted value to real OCR lines — cheapest and most accurate, no bbox tokens. 'llm_grounded': the model emits boxes and OCR grounding refines them. 'llm': use the model's boxes as-is (no grounding). 'off': produce no geometry."
                    order: 0
                  task_prompt_bbox:
                    type: string
                    title: Bounding-box instructions
                    format: textarea
                    description: "Instruction block telling the model to emit bounding boxes. Appended to whichever confidence-bearing prompt is active (integrated or confidence-only) ONLY when Geometry mode is 'llm' or 'llm_grounded'. Ignored for 'ocr_only'/'off' (geometry comes from OCR matching)."
                    order: 1
                    hideWhen: { field: "extraction.geometry.mode", value: "ocr_only" }
              confidence:
                type: object
                order: 3
                sectionLabel: Confidence Assessment
                collapsible: false
                description: "Per-field confidence scoring. Confidence is an optional output of extraction; when enabled, each extracted value gets a confidence score (and drives HITL review). Turn OFF to skip assessment entirely (no second model pass, no explainability info)."
                properties:
                  mode:
                    type: string
                    title: Confidence scoring mode
                    enum: ["off", "separate", "integrated"]
                    default: "separate"
                    description: "How per-field confidence is produced. 'off': no confidence scoring (no extra model pass, no confidence data). 'separate' (default): scored in a distinct inference — a per-shard second pass in Advanced extraction mode, or the standalone Assessment step in Simple mode. 'integrated': the extraction inference emits each value's confidence in one pass (saves a model call). Confidence drives HITL review and the UI confidence display."
                    order: 0
                  list_batch_size:
                    type: integer
                    title: List batch size
                    description: "Max list rows scored per inference in separate mode — both the Advanced in-shard pass and the Simple standalone Assessment step. Large lists (e.g. 75 rows) are unreliable in one call; batching guarantees every row is scored. Lower = more reliable but more inferences; raise for capable models."
                    minimum: 1
                    default: 25
                    order: 2
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                  model:
                    type: string
                    title: Confidence model
                    description: "Bedrock model ID for confidence scoring (used by the separate/standalone assessment path). Use 'LambdaHook' for a custom Lambda. Unused when mode is 'integrated' (the extraction model emits confidence)."
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                    enum:
                      [
                        "LambdaHook",
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "openai.gpt-5.4",
                        "openai.gpt-5.5",
                        "openai.gpt-5.6-sol",
                        "openai.gpt-5.6-terra",
                        "openai.gpt-5.6-luna",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    order: 3
                  model_lambda_hook_arn:
                    type: string
                    description: "Lambda function ARN for custom inference (used when model is 'LambdaHook'). Function name must start with GENAIIDP-."
                    default: ""
                    order: 4
                    dependsOn: { field: "model", value: "LambdaHook" }
                  model_params:
                    type: object
                    title: Model parameters
                    sectionLabel: Model parameters
                    # ghostGroup => children save at the PARENT path
                    # (extraction.confidence.temperature, ...) — purely visual grouping.
                    # The confidence pass always requests the model's max output tokens.
                    ghostGroup: true
                    collapsible: true
                    defaultExpanded: false
                    description: "Advanced inference tuning (sampling + reasoning effort). Defaults are sensible; most users never change these."
                    order: 5
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                    properties:
                      temperature:
                        type: number
                        title: Temperature
                        minimum: 0
                        maximum: 1
                        description: "Sampling temperature. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        order: 1
                        dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                      top_k:
                        type: number
                        title: Top K
                        description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        order: 2
                        dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                      top_p:
                        type: number
                        title: Top P
                        description: "Sampling Top P. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        order: 3
                        dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                      reasoning_effort:
                        type: string
                        title: Reasoning effort
                        enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                        default: "medium"
                        description: "Reasoning effort for reasoning-capable confidence models (OpenAI GPT-5.x; Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5). Ignored by the default Nova Lite confidence model and by Sonnet 4.5 / Haiku 4.5."
                        order: 4
                        dependsOn: { field: "model", reasoningCapable: true }
                  system_prompt:
                    type: string
                    title: System prompt (confidence)
                    format: textarea
                    description: "System prompt for confidence scoring."
                    order: 10
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                  task_prompt:
                    type: string
                    title: Task prompt (confidence)
                    format: textarea
                    description: "CONFIDENCE-ONLY task prompt — used by the separate confidence pass (agentic in-shard second inference and the non-agentic standalone Assessment step). Placeholders: {DOCUMENT_CLASS}, {ATTRIBUTE_NAMES_AND_DESCRIPTIONS}, {EXTRACTION_RESULTS}, {OCR_TEXT_CONFIDENCE}, {DOCUMENT_IMAGE}. The Bounding-box instructions (Geometry section) are appended when Geometry mode is 'llm'/'llm_grounded'. Use <<CACHEPOINT>> for Bedrock prompt caching."
                    order: 11
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                  image:
                    type: object
                    sectionLabel: Image Processing Settings
                    description: "Image resolution for confidence-scoring page attachments."
                    order: 12
                    dependsOn: { field: "extraction.confidence.mode", value: "separate" }
                    properties:
                      target_width:
                        type: number
                        description: "Target image width in pixels. Default: 951"
                        minimum: 100
                        maximum: 3500
                        order: 0
                      target_height:
                        type: number
                        description: "Target image height in pixels. Default: 1268"
                        minimum: 100
                        maximum: 3500
                        order: 1
                  # NOTE: granular assessment has been retired. Large lists are
                  # handled by the "List batch size" control above
                  # (extraction.confidence.list_batch_size). Leftover
                  # `extraction.confidence.granular.*` keys in an existing config
                  # validate but are ignored at runtime.
              postHook:
                type: array
                collapsible: true
                defaultExpanded: false
                sectionLabel: Post-step pipeline hooks
                description: >-
                  Lambda functions invoked after this step completes,
                  before the next step runs. Each hook receives the
                  document state and returns a JSON-serializable result
                  (logged for traceability). Hooks let vertical-product
                  packs (claims, loans, etc.) inject business logic
                  without forking the accelerator. Admins can edit, add,
                  reorder, or disable hooks here. The Lambda function
                  ARN must EITHER be tagged with `idp:feature-id=<anything>`
                  (vertical packs do this automatically) OR have a function
                  name starting with `GENAIIDP-` (admin-managed Lambdas).
                  Functions outside both categories will be rejected by
                  the dispatcher's IAM policy at invoke time.
                listLabel: Post-step pipeline hooks
                itemLabel: Hook
                order: 99
                items:
                  type: object
                  required:
                    - featureId
                    - arn
                  properties:
                    featureId:
                      type: string
                      description: Feature that owns this hook (label only)
                      order: 0
                    arn:
                      type: string
                      description: >-
                        Lambda function ARN to invoke. Must either be
                        tagged `idp:feature-id=<anything>` OR have a
                        function name beginning with `GENAIIDP-`.
                      order: 1
                    order:
                      type: number
                      description: Execution order (lower = earlier)
                      default: 100
                      order: 2
                    onError:
                      type: string
                      enum: ["continue", "fail", "skip-remaining"]
                      default: continue
                      description: Behavior when this hook errors
                      order: 3
                    enabled:
                      type: boolean
                      description: Disable without removing
                      default: true
                      order: 4
          # v0.6: confidence/geometry inference knobs live under
          # extraction.confidence / extraction.geometry; HITL is the top-level
          # 'hitl' section below. (The standalone 'assessment' config block and
          # its postAssessment hook point were retired in v0.6.)
          hitl:
            order: 6.5
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Human-in-the-Loop (HITL) Review (Optional)
            description: "Route low-confidence extractions to human review. Requires Confidence Assessment to be enabled."
            properties:
              enabled:
                type: boolean
                title: Enabled
                description: "Enable Human-in-the-Loop review for low-confidence extractions."
                default: false
                order: 0
              confidence_threshold:
                type: number
                title: Confidence threshold
                description: "Confidence threshold (0.0-1.0). Fields scoring below this trigger HITL review and show confidence alerts."
                minimum: 0.0
                maximum: 1.0
                default: 0.8
                order: 1
                hideWhen: { field: "hitl.enabled", value: false }
          summarization:
            order: 7
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Summarization (Optional)
            description: "Generates a natural-language summary of the document."
            properties:
              enabled:
                type: boolean
                title: Enabled
                description: Enable or disable document summarization
                default: true
                order: 0
              model:
                type: string
                title: Model
                description: "Bedrock model ID. Select 'LambdaHook' to use a custom Lambda function for inference."
                dependsOn: { field: "summarization.enabled", value: true }
                enum:
                  [
                    "LambdaHook",
                    "us.amazon.nova-lite-v1:0",
                    "us.amazon.nova-pro-v1:0",
                    "us.amazon.nova-premier-v1:0",
                    "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                    "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "us.anthropic.claude-sonnet-4-6",
                    "us.anthropic.claude-sonnet-4-6:1m",
                    "us.anthropic.claude-sonnet-5",
                    "us.anthropic.claude-sonnet-5:1m",
                    "us.anthropic.claude-opus-4-5-20251101-v1:0",
                    "us.anthropic.claude-opus-4-6-v1",
                    "us.anthropic.claude-opus-4-6-v1:1m",
                    "us.anthropic.claude-opus-4-7",
                    "us.anthropic.claude-opus-4-7:1m",
                    "us.anthropic.claude-opus-4-8",
                    "us.anthropic.claude-opus-4-8:1m",
                    "us.anthropic.claude-opus-5",
                    "us.anthropic.claude-opus-5:1m",
                    "openai.gpt-5.4",
                    "openai.gpt-5.5",
                    "openai.gpt-5.6-sol",
                    "openai.gpt-5.6-terra",
                    "openai.gpt-5.6-luna",
                    "eu.amazon.nova-lite-v1:0",
                    "eu.amazon.nova-pro-v1:0",
                    "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                    "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "eu.anthropic.claude-sonnet-4-6",
                    "eu.anthropic.claude-sonnet-4-6:1m",
                    "eu.anthropic.claude-sonnet-5",
                    "eu.anthropic.claude-sonnet-5:1m",
                    "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                    "eu.anthropic.claude-opus-4-6-v1",
                    "eu.anthropic.claude-opus-4-6-v1:1m",
                    "eu.anthropic.claude-opus-4-7",
                    "eu.anthropic.claude-opus-4-7:1m",
                    "eu.anthropic.claude-opus-4-8",
                    "eu.anthropic.claude-opus-4-8:1m",
                    "eu.anthropic.claude-opus-5",
                    "eu.anthropic.claude-opus-5:1m",
                    "qwen.qwen3-vl-235b-a22b",
                    "us.meta.llama4-maverick-17b-instruct-v1:0",
                    "us.meta.llama4-scout-17b-instruct-v1:0",
                    "google.gemma-3-27b-it",
                    "nvidia.nemotron-nano-12b-v2",
                    "global.amazon.nova-2-lite-v1:0",
                    "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "global.anthropic.claude-sonnet-4-6",
                    "global.anthropic.claude-sonnet-4-6:1m",
                    "global.anthropic.claude-sonnet-5",
                    "global.anthropic.claude-sonnet-5:1m",
                    "global.anthropic.claude-opus-4-5-20251101-v1:0",
                    "global.anthropic.claude-opus-4-6-v1",
                    "global.anthropic.claude-opus-4-6-v1:1m",
                    "global.anthropic.claude-opus-4-7",
                    "global.anthropic.claude-opus-4-7:1m",
                    "global.anthropic.claude-opus-4-8",
                    "global.anthropic.claude-opus-4-8:1m",
                    "global.anthropic.claude-opus-5",
                    "global.anthropic.claude-opus-5:1m"
                  ]
                order: 1
              model_lambda_hook_arn:
                type: string
                description: "Lambda function ARN for custom inference (used when model is 'LambdaHook'). Function name must start with GENAIIDP-."
                default: ""
                order: 2
                dependsOn: { field: "model", value: "LambdaHook" }
              model_params:
                type: object
                title: Model parameters
                sectionLabel: Model parameters
                # ghostGroup => children save at the PARENT path (summarization.temperature,
                # summarization.max_tokens, ...), so this is a purely visual grouping.
                ghostGroup: true
                collapsible: true
                defaultExpanded: false
                description: "Advanced inference tuning (sampling + output limits). Defaults are sensible; most users never change these."
                order: 3
                dependsOn: { field: "summarization.enabled", value: true }
                properties:
                  temperature:
                    type: number
                    title: Temperature
                    description: "Sampling temperature. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 1
                    dependsOn: { field: "summarization.enabled", value: true }
                  top_k:
                    type: number
                    title: Top K
                    description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 2
                    dependsOn: { field: "summarization.enabled", value: true }
                  top_p:
                    type: number
                    title: Top P
                    description: "Sampling Top P. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 3
                    dependsOn: { field: "summarization.enabled", value: true }
                  max_tokens:
                    type: number
                    title: Max tokens
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                    dependsOn: { field: "summarization.enabled", value: true }
                  reasoning_effort:
                    type: string
                    title: Reasoning effort
                    enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                    default: "medium"
                    description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Higher effort costs more output tokens with little accuracy gain for extraction (measured), so lower is usually better. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                    order: 5
                    dependsOn: { field: "model", reasoningCapable: true }
              system_prompt:
                type: string
                title: System prompt
                format: textarea
                description: System prompt
                order: 8
                dependsOn: { field: "summarization.enabled", value: true }
              task_prompt:
                type: string
                title: Task prompt
                format: textarea
                description: Task prompt - supports parameters {DOCUMENT_TEXT} and {EXTRACTION_RESULTS}. Optionally use <<CACHEPOINT>> to separate static and dynamic elements of prompt for Bedrock prompt caching.
                order: 9
                dependsOn: { field: "summarization.enabled", value: true }
              postHook:
                type: array
                collapsible: true
                defaultExpanded: false
                sectionLabel: Post-step pipeline hooks
                description: >-
                  Lambda functions invoked after this step completes,
                  before the next step runs. Each hook receives the
                  document state and returns a JSON-serializable result
                  (logged for traceability). Hooks let vertical-product
                  packs (claims, loans, etc.) inject business logic
                  without forking the accelerator. Admins can edit, add,
                  reorder, or disable hooks here. The Lambda function
                  ARN must EITHER be tagged with `idp:feature-id=<anything>`
                  (vertical packs do this automatically) OR have a function
                  name starting with `GENAIIDP-` (admin-managed Lambdas).
                  Functions outside both categories will be rejected by
                  the dispatcher's IAM policy at invoke time.
                listLabel: Post-step pipeline hooks
                itemLabel: Hook
                order: 99
                items:
                  type: object
                  required:
                    - featureId
                    - arn
                  properties:
                    featureId:
                      type: string
                      description: Feature that owns this hook (label only)
                      order: 0
                    arn:
                      type: string
                      description: >-
                        Lambda function ARN to invoke. Must either be
                        tagged `idp:feature-id=<anything>` OR have a
                        function name beginning with `GENAIIDP-`.
                      order: 1
                    order:
                      type: number
                      description: Execution order (lower = earlier)
                      default: 100
                      order: 2
                    onError:
                      type: string
                      enum: ["continue", "fail", "skip-remaining"]
                      default: continue
                      description: Behavior when this hook errors
                      order: 3
                    enabled:
                      type: boolean
                      description: Disable without removing
                      default: true
                      order: 4
          evaluation:
            order: 9
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Evaluation (Optional)
            description: "Scores extraction results against baseline ground-truth data to measure accuracy."
            properties:
              enabled:
                type: boolean
                title: Enabled
                description: Enable or disable evaluation processing
                default: true
                order: 0
              llm_method:
                type: object
                order: 1
                sectionLabel: LLM method
                collapsible: false
                description: "Model and prompts used to score extraction accuracy against ground truth (LLM-as-judge)."
                dependsOn: { field: "evaluation.enabled", value: true }
                properties:
                  model:
                    type: string
                    title: Model
                    description: Bedrock model ID
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "openai.gpt-5.4",
                        "openai.gpt-5.5",
                        "openai.gpt-5.6-sol",
                        "openai.gpt-5.6-terra",
                        "openai.gpt-5.6-luna",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    order: 1
                  model_params:
                    type: object
                    title: Model parameters
                    sectionLabel: Model parameters
                    # ghostGroup => children save at the PARENT path
                    # (evaluation.llm_method.temperature, ...) — purely visual grouping.
                    ghostGroup: true
                    collapsible: true
                    defaultExpanded: false
                    description: "Advanced inference tuning (sampling + output limits). Defaults are sensible; most users never change these."
                    order: 2
                    properties:
                      temperature:
                        type: number
                        title: Temperature
                        description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Used when top_p is 0. Set temperature=0.0 for consistent, deterministic outputs. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        default: 0.0
                        order: 1
                      top_k:
                        type: number
                        title: Top K
                        description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        default: 250
                        order: 2
                      top_p:
                        type: number
                        title: Top P
                        description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0). When set to a positive value (> 0), overrides temperature parameter. Set to 0 to use temperature instead. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                        order: 3
                      max_tokens:
                        type: number
                        title: Max tokens
                        description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                        order: 4
                      reasoning_effort:
                        type: string
                        title: Reasoning effort
                        enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                        default: "medium"
                        description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Higher effort costs more output tokens with little accuracy gain for extraction (measured), so lower is usually better. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                        order: 5
                        dependsOn: { field: "model", reasoningCapable: true }
                  system_prompt:
                    type: string
                    title: System prompt
                    format: textarea
                    description: System prompt for LLM evaluation
                    order: 7
                  task_prompt:
                    type: string
                    title: Task prompt
                    format: textarea
                    description: Task prompt for LLM evaluation - supports placeholders {DOCUMENT_CLASS}, {ATTRIBUTE_NAME}, {ATTRIBUTE_DESCRIPTION}, {EXPECTED_VALUE} and {ACTUAL_VALUE}
                    order: 8
          discovery:
            order: 10
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Discovery
            description: "Analyzes sample documents to auto-generate class definitions and extraction schemas."
            properties:
              without_ground_truth:
                order: 0
                type: object
                sectionLabel: Discovery Without Ground Truth
                description: Configuration for discovering document classes without reference data
                properties:
                  model_id:
                    type: string
                    description: Bedrock model ID for discovery without ground truth
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "us.amazon.nova-pro-v1:0"
                    order: 0
                  temperature:
                    type: number
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Used when top_p is 0. Set temperature=0.0 for consistent, deterministic outputs. Not used for Claude 4.7+ models."
                    minimum: 0.0
                    maximum: 1.0
                    default: 1.0
                    order: 1
                  top_p:
                    type: number
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0). When set to a positive value (> 0), overrides temperature parameter. Set to 0 to use temperature instead. Not used for Claude 4.7+ models."
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.1
                    order: 2
                  max_tokens:
                    type: number
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 3
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for the discovery model
                    default: "You are an expert in processing forms. Extracting data from images and documents. Analyze forms line by line to identify field names, data types, and organizational structure. Focus on creating comprehensive blueprints for document processing without extracting actual values."
                    order: 4
                  user_prompt:
                    type: string
                    format: textarea
                    description: User prompt template for discovery without ground truth
                    default: 'This image contains forms data. Analyze the form line by line. Image may contains multiple pages, process all the pages. Form may contain multiple name value pair in one line. Extract all the names in the form including the name value pair which doesn''t have value. Organize them into groups, extract field_name, data_type and field description. Field_name should be less than 60 characters, should not have space use ''-'' instead of space. field_description is a brief description of the field and the location of the field like box number or line number in the form and section of the form. Field_name should be unique within the group. Add two fields document_class and document_description. For document_class generate a short name based on the document content like W4, I-9, Paystub. For document_description generate a description about the document in less than 50 words. Group the fields based on the section they are grouped in the form. Group should have attributeType as "group". If the group repeats, add an additional field groupType and set the value as "Table". Do not extract the values. Return the extracted data in JSON format.'
                    order: 5
              with_ground_truth:
                order: 1
                type: object
                sectionLabel: Discovery With Ground Truth
                description: Configuration for discovering document classes using reference data
                properties:
                  model_id:
                    type: string
                    description: Bedrock model ID for discovery with ground truth
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "us.amazon.nova-pro-v1:0"
                    order: 0
                  temperature:
                    type: number
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Used when top_p is 0. Set temperature=0.0 for consistent, deterministic outputs. Not used for Claude 4.7+ models."
                    minimum: 0.0
                    maximum: 1.0
                    default: 1.0
                    order: 1
                  top_p:
                    type: number
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0). When set to a positive value (> 0), overrides temperature parameter. Set to 0 to use temperature instead. Not used for Claude 4.7+ models."
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.1
                    order: 2
                  max_tokens:
                    type: number
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 3
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for the discovery model with ground truth
                    default: "You are an expert in processing forms. Extracting data from images and documents. Use provided ground truth data as reference to optimize field extraction and ensure consistency with expected document structure and field definitions."
                    order: 4
                  user_prompt:
                    type: string
                    format: textarea
                    description: User prompt template for discovery with ground truth (use {ground_truth_json} placeholder)
                    default: 'This image contains unstructured data. Analyze the data line by line using the provided ground truth as reference. <GROUND_TRUTH_REFERENCE>{ground_truth_json}</GROUND_TRUTH_REFERENCE> Ground truth reference JSON has the fields we are interested in extracting from the document/image. Use the ground truth to optimize field extraction. Match field names, data types, and groupings from the reference. Image may contain multiple pages, process all pages. Extract all field names including those without values. Do not change the group name and field name from ground truth in the extracted data json. Add field_description field for every field which will contain instruction to LLM to extract the field data from the image/document. Add data_type field for every field. Add two fields document_class and document_description. For document_class generate a short name based on the document content like W4, I-9, Paystub. For document_description generate a description about the document in less than 50 words. If the group repeats and follows table format, add a special field group_type with value "Table" and description field for the group. Do not extract the values.'
                    order: 5
              auto_split:
                order: 2
                type: object
                sectionLabel: Auto-Detect Sections (Multi-Section Package)
                description: Configuration for AI-powered auto-detection of document section boundaries in multi-page packages
                properties:
                  model_id:
                    type: string
                    description: Bedrock model ID for auto-detecting document sections
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "us.amazon.nova-pro-v1:0"
                    order: 0
                  temperature:
                    type: number
                    description: "Temperature for section detection (0.0 recommended for consistent boundary detection)"
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.0
                    order: 1
                  top_p:
                    type: number
                    description: "Top P for section detection"
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.1
                    order: 2
                  max_tokens:
                    type: number
                    description: "Optional cap on output tokens for section detection. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 3
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for auto-detecting document sections
                    default: "You are an expert document analyst. Your task is to identify distinct document sections within a multi-page document package. Return only valid JSON."
                    order: 4
                  user_prompt:
                    type: string
                    format: textarea
                    description: User prompt for auto-detecting document sections
                    default: 'Analyze this multi-page document package. Identify the page boundaries where different document types or sections begin and end. For each distinct document section, provide: - "start": the first page number (1-based) - "end": the last page number (1-based) - "type": a short label for the document type (e.g., "W2 Form", "Letter", "Invoice"). Return ONLY a JSON array, no other text: [{"start": 1, "end": 2, "type": "Letter"}, {"start": 3, "end": 3, "type": "Invoice"}]'
                    order: 5
              output_format:
                order: 3
                type: object
                sectionLabel: Output Format Configuration
                description: Configuration for discovery output format
                properties:
                  sample_json:
                    type: string
                    format: textarea
                    description: Sample JSON format for discovery output
                    default: "{\n  \"document_class\": \"Form-1040\",\n  \"document_description\": \"Brief summary of the document\",\n  \"groups\": [\n    {\n      \"name\": \"PersonalInformation\",\n      \"description\": \"Personal information of Tax payer\",\n      \"attributeType\": \"group\",\n      \"groupType\": \"normal\",\n      \"groupAttributes\": [\n        {\n          \"name\": \"FirstName\",\n          \"dataType\": \"string\",\n          \"description\": \"First Name of Taxpayer\"\n        },\n        {\n          \"name\": \"Age\",\n          \"dataType\": \"number\",\n          \"description\": \"Age of Taxpayer\"\n        }\n      ]\n    }\n  ]\n}"
                    order: 0
              multi_document:
                order: 4
                type: object
                sectionLabel: Multi-Document Discovery (Batch Clustering)
                description: Configuration for discovering document classes from a collection of documents using embedding-based clustering
                properties:
                  embedding_model_id:
                    type: string
                    description: Bedrock model ID for generating document embeddings
                    default: "us.cohere.embed-v4:0"
                    order: 0
                  analysis_model_id:
                    type: string
                    description: Bedrock model ID for analyzing document clusters
                    default: "us.anthropic.claude-sonnet-4-6"
                    enum:
                      - "us.amazon.nova-lite-v1:0"
                      - "us.amazon.nova-pro-v1:0"
                      - "us.amazon.nova-premier-v1:0"
                      - "us.amazon.nova-2-lite-v1:0"
                      - "us.anthropic.claude-3-5-haiku-20241022-v1:0"
                      - "us.anthropic.claude-sonnet-4-20250514-v1:0"
                      - "us.anthropic.claude-sonnet-4-6"
                      - "us.anthropic.claude-sonnet-5"
                      - "us.anthropic.claude-sonnet-5:1m"
                      - "us.anthropic.claude-opus-4-20250514-v1:0"
                      - "us.anthropic.claude-opus-4-6-v1:1m"
                      - "us.anthropic.claude-opus-4-7"
                      - "us.anthropic.claude-opus-4-7:1m"
                      - "us.anthropic.claude-opus-4-8"
                      - "us.anthropic.claude-opus-4-8:1m"
                      - "us.anthropic.claude-opus-5"
                      - "us.anthropic.claude-opus-5:1m"
                    order: 1
                  temperature:
                    type: number
                    description: Temperature for cluster analysis model
                    default: 0.0
                    minimum: 0
                    maximum: 1
                    order: 2
                  max_tokens:
                    type: number
                    description: "Optional cap on output tokens for cluster analysis. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 3
                  max_documents:
                    type: number
                    description: Maximum documents to process in a single discovery run
                    default: 500
                    minimum: 1
                    order: 4
                  min_cluster_size:
                    type: number
                    description: Minimum documents required to form a cluster
                    default: 2
                    minimum: 1
                    order: 5
                  num_sample_documents:
                    type: number
                    description: Number of sample documents selected per cluster for analysis
                    default: 3
                    minimum: 1
                    order: 6
                  max_sample_size:
                    type: number
                    description: Maximum sample size for cluster analysis
                    default: 5
                    minimum: 1
                    order: 7
                  max_concurrent_embeddings:
                    type: number
                    description: Maximum concurrent embedding API requests
                    default: 5
                    minimum: 1
                    order: 8
                  max_concurrent_clusters:
                    type: number
                    description: Maximum concurrent cluster analysis requests
                    default: 3
                    minimum: 1
                    order: 9
              rules:
                order: 5
                type: object
                sectionLabel: Policy Discovery
                description: Configuration for extracting policy rules from regulatory documents
                properties:
                  model:
                    type: string
                    description: Bedrock model ID for Policy Discovery
                    enum:
                      [
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "global.anthropic.claude-sonnet-4-6"
                    order: 0
                  temperature:
                    type: number
                    description: "Sampling temperature for Policy Discovery (0.0 recommended for consistent extraction)"
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.0
                    order: 1
                  top_p:
                    type: number
                    description: "Top P for Policy Discovery"
                    minimum: 0.0
                    maximum: 1.0
                    default: 0.0
                    order: 2
                  top_k:
                    type: number
                    description: "Top K for Policy Discovery"
                    minimum: 1
                    maximum: 100
                    default: 5
                    order: 3
                  max_tokens:
                    type: number
                    description: "Optional cap on output tokens for Policy Discovery. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for Policy Discovery
                    order: 5
                  task_prompt:
                    type: string
                    format: textarea
                    description: Task prompt for Policy Discovery
                    order: 6
          agents:
            order: 11
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Agent Companion
            description: "Configures the AI agents used for interactive analysis (e.g. chat companion, error analysis)."
            properties:
              chat_companion:
                type: object
                sectionLabel: Chat Companion Agents
                description: Configuration for the chat companion agents
                properties:
                  model_id:
                    type: string
                    description: Model ID for the chat companion agents
                    enum:
                      - "us.amazon.nova-lite-v1:0"
                      - "us.amazon.nova-pro-v1:0"
                      - "us.amazon.nova-premier-v1:0"
                      - "us.amazon.nova-2-lite-v1:0"
                      - "us.amazon.nova-2-lite-v1:0:priority"
                      - "us.amazon.nova-2-lite-v1:0:flex"
                      - "global.amazon.nova-2-lite-v1:0"
                      - "global.amazon.nova-2-lite-v1:0:priority"
                      - "global.amazon.nova-2-lite-v1:0:flex"
                      - "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                      - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "us.anthropic.claude-sonnet-4-6"
                      - "us.anthropic.claude-sonnet-4-6:1m"
                      - "us.anthropic.claude-sonnet-5"
                      - "us.anthropic.claude-sonnet-5:1m"
                      - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "us.anthropic.claude-opus-4-6-v1"
                      - "us.anthropic.claude-opus-4-6-v1:1m"
                      - "us.anthropic.claude-opus-4-7"
                      - "us.anthropic.claude-opus-4-7:1m"
                      - "us.anthropic.claude-opus-4-8"
                      - "us.anthropic.claude-opus-4-8:1m"
                      - "us.anthropic.claude-opus-5"
                      - "us.anthropic.claude-opus-5:1m"
                      - "global.anthropic.claude-haiku-4-5-20251001-v1:0"
                      - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "global.anthropic.claude-sonnet-4-6"
                      - "global.anthropic.claude-sonnet-4-6:1m"
                      - "global.anthropic.claude-sonnet-5"
                      - "global.anthropic.claude-sonnet-5:1m"
                      - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "global.anthropic.claude-opus-4-6-v1"
                      - "global.anthropic.claude-opus-4-6-v1:1m"
                      - "global.anthropic.claude-opus-4-7"
                      - "global.anthropic.claude-opus-4-7:1m"
                      - "global.anthropic.claude-opus-4-8"
                      - "global.anthropic.claude-opus-4-8:1m"
                      - "global.anthropic.claude-opus-5"
                      - "global.anthropic.claude-opus-5:1m"
                      - "eu.amazon.nova-lite-v1:0"
                      - "eu.amazon.nova-pro-v1:0"
                      - "eu.amazon.nova-2-lite-v1:0"
                      - "eu.amazon.nova-2-lite-v1:0:priority"
                      - "eu.amazon.nova-2-lite-v1:0:flex"
                      - "eu.anthropic.claude-haiku-4-5-20251001-v1:0"
                      - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "eu.anthropic.claude-sonnet-4-6"
                      - "eu.anthropic.claude-sonnet-4-6:1m"
                      - "eu.anthropic.claude-sonnet-5"
                      - "eu.anthropic.claude-sonnet-5:1m"
                      - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "eu.anthropic.claude-opus-4-6-v1"
                      - "eu.anthropic.claude-opus-4-6-v1:1m"
                      - "eu.anthropic.claude-opus-4-7"
                      - "eu.anthropic.claude-opus-4-7:1m"
                      - "eu.anthropic.claude-opus-4-8"
                      - "eu.anthropic.claude-opus-4-8:1m"
                      - "eu.anthropic.claude-opus-5"
                      - "eu.anthropic.claude-opus-5:1m"
                      - "qwen.qwen3-vl-235b-a22b"
                      - "us.meta.llama4-maverick-17b-instruct-v1:0"
                      - "us.meta.llama4-scout-17b-instruct-v1:0"
                      - "google.gemma-3-27b-it"
                      - "nvidia.nemotron-nano-12b-v2"
                    default: "us.anthropic.claude-haiku-4-5-20251001-v1:0"
                    order: 0
              error_analyzer:
                type: object
                sectionLabel: Error Analysis Agent
                description: Configuration for the error analysis agent
                properties:
                  model_id:
                    type: string
                    description: Bedrock model ID for the error analysis agent (requires tool use support)
                    enum:
                      - "us.amazon.nova-pro-v1:0"
                      - "us.amazon.nova-premier-v1:0"
                      - "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "us.anthropic.claude-sonnet-4-6"
                      - "us.anthropic.claude-sonnet-4-6:1m"
                      - "us.anthropic.claude-sonnet-5"
                      - "us.anthropic.claude-sonnet-5:1m"
                      - "us.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "us.anthropic.claude-opus-4-6-v1"
                      - "us.anthropic.claude-opus-4-6-v1:1m"
                      - "us.anthropic.claude-opus-4-7"
                      - "us.anthropic.claude-opus-4-7:1m"
                      - "us.anthropic.claude-opus-4-8"
                      - "us.anthropic.claude-opus-4-8:1m"
                      - "us.anthropic.claude-opus-5"
                      - "us.anthropic.claude-opus-5:1m"
                      - "global.amazon.nova-pro-v1:0"
                      - "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "global.anthropic.claude-sonnet-4-6"
                      - "global.anthropic.claude-sonnet-4-6:1m"
                      - "global.anthropic.claude-sonnet-5"
                      - "global.anthropic.claude-sonnet-5:1m"
                      - "global.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "global.anthropic.claude-opus-4-6-v1"
                      - "global.anthropic.claude-opus-4-6-v1:1m"
                      - "global.anthropic.claude-opus-4-7"
                      - "global.anthropic.claude-opus-4-7:1m"
                      - "global.anthropic.claude-opus-4-8"
                      - "global.anthropic.claude-opus-4-8:1m"
                      - "global.anthropic.claude-opus-5"
                      - "global.anthropic.claude-opus-5:1m"
                      - "eu.amazon.nova-pro-v1:0"
                      - "eu.anthropic.claude-sonnet-4-5-20250929-v1:0"
                      - "eu.anthropic.claude-sonnet-4-6"
                      - "eu.anthropic.claude-sonnet-4-6:1m"
                      - "eu.anthropic.claude-sonnet-5"
                      - "eu.anthropic.claude-sonnet-5:1m"
                      - "eu.anthropic.claude-opus-4-5-20251101-v1:0"
                      - "eu.anthropic.claude-opus-4-6-v1"
                      - "eu.anthropic.claude-opus-4-6-v1:1m"
                      - "eu.anthropic.claude-opus-4-7"
                      - "eu.anthropic.claude-opus-4-7:1m"
                      - "eu.anthropic.claude-opus-4-8"
                      - "eu.anthropic.claude-opus-4-8:1m"
                      - "eu.anthropic.claude-opus-5"
                      - "eu.anthropic.claude-opus-5:1m"
                    default: "us.anthropic.claude-sonnet-4-6"
                    order: 0
                  lookback_hours:
                    type: integer
                    description: How far back the error analyzer searches logs, traces, and execution history (in hours)
                    default: 24
                    order: 1
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for the error analysis agent
                    order: 2
          chat:
            order: 12
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Chat-with-Document
            description: >-
              Controls the interactive "Chat with Document" feature on the
              Document Detail screen.
            properties:
              enabled:
                type: boolean
                title: Enabled
                description: Enable or disable the Chat-with-Document feature
                default: true
                order: 0
              model:
                type: string
                title: Model
                description: "Bedrock model ID for chat. A large-context model is recommended because the entire document text is sent in one prompt. Select 'LambdaHook' to use a custom Lambda function for inference."
                dependsOn: { field: "chat.enabled", value: true }
                enum:
                  [
                    "LambdaHook",
                    "us.amazon.nova-lite-v1:0",
                    "us.amazon.nova-pro-v1:0",
                    "us.amazon.nova-premier-v1:0",
                    "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                    "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "us.anthropic.claude-sonnet-4-6",
                    "us.anthropic.claude-sonnet-4-6:1m",
                    "us.anthropic.claude-sonnet-5",
                    "us.anthropic.claude-sonnet-5:1m",
                    "us.anthropic.claude-opus-4-5-20251101-v1:0",
                    "us.anthropic.claude-opus-4-6-v1",
                    "us.anthropic.claude-opus-4-6-v1:1m",
                    "us.anthropic.claude-opus-4-7",
                    "us.anthropic.claude-opus-4-7:1m",
                    "us.anthropic.claude-opus-4-8",
                    "us.anthropic.claude-opus-4-8:1m",
                    "us.anthropic.claude-opus-5",
                    "us.anthropic.claude-opus-5:1m",
                    "openai.gpt-5.4",
                    "openai.gpt-5.5",
                    "openai.gpt-5.6-sol",
                    "openai.gpt-5.6-terra",
                    "openai.gpt-5.6-luna",
                    "eu.amazon.nova-lite-v1:0",
                    "eu.amazon.nova-pro-v1:0",
                    "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                    "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "eu.anthropic.claude-sonnet-4-6",
                    "eu.anthropic.claude-sonnet-4-6:1m",
                    "eu.anthropic.claude-sonnet-5",
                    "eu.anthropic.claude-sonnet-5:1m",
                    "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                    "eu.anthropic.claude-opus-4-6-v1",
                    "eu.anthropic.claude-opus-4-6-v1:1m",
                    "eu.anthropic.claude-opus-4-7",
                    "eu.anthropic.claude-opus-4-7:1m",
                    "eu.anthropic.claude-opus-4-8",
                    "eu.anthropic.claude-opus-4-8:1m",
                    "eu.anthropic.claude-opus-5",
                    "eu.anthropic.claude-opus-5:1m",
                    "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                    "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                    "global.anthropic.claude-sonnet-4-6",
                    "global.anthropic.claude-sonnet-4-6:1m",
                    "global.anthropic.claude-sonnet-5",
                    "global.anthropic.claude-sonnet-5:1m",
                    "global.anthropic.claude-opus-4-5-20251101-v1:0",
                    "global.anthropic.claude-opus-4-6-v1",
                    "global.anthropic.claude-opus-4-6-v1:1m",
                    "global.anthropic.claude-opus-4-7",
                    "global.anthropic.claude-opus-4-7:1m",
                    "global.anthropic.claude-opus-4-8",
                    "global.anthropic.claude-opus-4-8:1m",
                    "global.anthropic.claude-opus-5",
                    "global.anthropic.claude-opus-5:1m"
                  ]
                order: 1
              model_lambda_hook_arn:
                type: string
                description: "Lambda function ARN for custom inference (used when model is 'LambdaHook'). Function name must start with GENAIIDP-."
                default: ""
                order: 2
                dependsOn: { field: "model", value: "LambdaHook" }
              model_params:
                type: object
                title: Model parameters
                sectionLabel: Model parameters
                # ghostGroup => children save at the PARENT path (chat.temperature,
                # chat.max_tokens, ...), so this is a purely visual grouping.
                ghostGroup: true
                collapsible: true
                defaultExpanded: false
                description: "Advanced inference tuning (sampling + output limits). Defaults are sensible; most users never change these."
                order: 3
                dependsOn: { field: "chat.enabled", value: true }
                properties:
                  temperature:
                    type: number
                    title: Temperature
                    description: "Sampling temperature. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 1
                    dependsOn: { field: "chat.enabled", value: true }
                  top_k:
                    type: number
                    title: Top K
                    description: "Sampling Top K. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 2
                    dependsOn: { field: "chat.enabled", value: true }
                  top_p:
                    type: number
                    title: Top P
                    description: "Sampling Top P. Not used for Claude 4.7+ or OpenAI GPT-5.x models."
                    order: 3
                    dependsOn: { field: "chat.enabled", value: true }
                  max_tokens:
                    type: number
                    title: Max tokens
                    description: "Optional cap on output tokens for the chat response. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                    dependsOn: { field: "chat.enabled", value: true }
                  reasoning_effort:
                    type: string
                    title: Reasoning effort
                    enum: ["minimal", "low", "medium", "high", "xhigh", "max"]
                    default: "medium"
                    description: "Reasoning effort for reasoning-capable models: OpenAI GPT-5.x (minimal|low|medium|high) and Claude Sonnet 5 / Sonnet 4.6 / Opus 4.5-4.8 / Fable 5 (low|medium|high|xhigh|max). Controls how much the model reasons before answering. Higher effort costs more output tokens with little accuracy gain for extraction (measured), so lower is usually better. Ignored by Nova, Sonnet 4.5, and Haiku 4.5."
                    order: 5
                    dependsOn: { field: "model", reasoningCapable: true }
              system_prompt:
                type: string
                title: System prompt
                format: textarea
                description: System prompt for the Chat-with-Document assistant
                order: 8
                dependsOn: { field: "chat.enabled", value: true }
          rule_validation:
            order: 8
            type: object
            collapsible: true
            defaultExpanded: false
            sectionLabel: Rule Validation (Optional)
            description: "Validates extracted data against business rules for matching policy classes. Requires one or more policy classes to be configured."
            properties:
              enabled:
                type: boolean
                title: Enabled
                description: Enable or disable rule validation processing
                default: true
                order: 0
              recommendation_options:
                type: string
                format: text-area
                description: "Recommendation options that define the possible outcomes for rule validation: Pass (criteria fully met), Fail (partially met or needs more info), Information Not Found (no relevant data)"
                default: |-
                  Pass: The requirement criteria are fully met.
                  Fail: The requirement is partially met or requires additional information.
                  Information Not Found: No relevant data exists in the user history.
                order: 1
                dependsOn: { field: "enabled", value: true }
              semaphore:
                type: integer
                minimum: 1
                maximum: 10
                description: Number of concurrent API calls for rule validation
                default: 5
                order: 2
                dependsOn: { field: "enabled", value: true }
              max_chunk_size:
                type: integer
                minimum: 1000
                maximum: 200000
                description: Maximum chunk size (in characters) for rule validation processing
                default: 8000
                order: 3
                dependsOn: { field: "enabled", value: true }
              fact_extraction:
                type: object
                sectionLabel: Fact Extraction
                description: Configuration for extracting relevant facts from documents
                order: 4
                dependsOn: { field: "enabled", value: true }
                properties:
                  model:
                    type: string
                    description: Bedrock model ID for fact extraction
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                    order: 0
                  temperature:
                    type: number
                    minimum: 0
                    maximum: 1
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Not used for Claude 4.7+ models."
                    default: 0.0
                    order: 1
                  top_k:
                    type: integer
                    minimum: 1
                    description: "Sampling Top K. Not used for Claude 4.7+ models."
                    default: 20
                    order: 2
                  top_p:
                    type: number
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0)"
                    default: 0.01
                    order: 3
                  max_tokens:
                    type: integer
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt that defines the LLM's role as a fact extractor for identifying relevant information from documents
                    order: 5
                  task_prompt:
                    type: string
                    format: textarea
                    description: Task prompt for fact extraction - supports placeholders {DOCUMENT_TEXT} (replaced with document content), {recommendation_options} (replaced with available recommendation options), {policy_type} (replaced with policy type), and {rule} (replaced with rule description). Optionally use <<CACHEPOINT>> to separate static and dynamic elements of prompt for Bedrock prompt caching.
                    order: 6
              rule_validation_orchestrator:
                type: object
                sectionLabel: Rule Validation Orchestrator
                description: Configuration for consolidating fact extraction results into final compliance decisions
                order: 5
                dependsOn: { field: "enabled", value: true }
                properties:
                  model:
                    type: string
                    description: Bedrock model ID for rule validation orchestrator
                    enum:
                      [
                        "us.amazon.nova-lite-v1:0",
                        "us.amazon.nova-pro-v1:0",
                        "us.amazon.nova-premier-v1:0",
                        "us.amazon.nova-2-lite-v1:0", "us.amazon.nova-2-lite-v1:0:priority", "us.amazon.nova-2-lite-v1:0:flex", "global.amazon.nova-2-lite-v1:0", "global.amazon.nova-2-lite-v1:0:priority", "global.amazon.nova-2-lite-v1:0:flex",
                        "us.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "us.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "us.anthropic.claude-sonnet-4-6",
                        "us.anthropic.claude-sonnet-4-6:1m",
                        "us.anthropic.claude-sonnet-5",
                        "us.anthropic.claude-sonnet-5:1m",
                        "us.anthropic.claude-opus-4-5-20251101-v1:0",
                        "us.anthropic.claude-opus-4-6-v1",
                        "us.anthropic.claude-opus-4-6-v1:1m",
                        "us.anthropic.claude-opus-4-7",
                        "us.anthropic.claude-opus-4-7:1m",
                        "us.anthropic.claude-opus-4-8",
                        "us.anthropic.claude-opus-4-8:1m",
                        "us.anthropic.claude-opus-5",
                        "us.anthropic.claude-opus-5:1m",
                        "eu.amazon.nova-lite-v1:0",
                        "eu.amazon.nova-pro-v1:0",
                        "eu.amazon.nova-2-lite-v1:0", "eu.amazon.nova-2-lite-v1:0:priority", "eu.amazon.nova-2-lite-v1:0:flex",
                        "eu.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "eu.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "eu.anthropic.claude-sonnet-4-6",
                        "eu.anthropic.claude-sonnet-4-6:1m",
                        "eu.anthropic.claude-sonnet-5",
                        "eu.anthropic.claude-sonnet-5:1m",
                        "eu.anthropic.claude-opus-4-5-20251101-v1:0",
                        "eu.anthropic.claude-opus-4-6-v1",
                        "eu.anthropic.claude-opus-4-6-v1:1m",
                        "eu.anthropic.claude-opus-4-7",
                        "eu.anthropic.claude-opus-4-7:1m",
                        "eu.anthropic.claude-opus-4-8",
                        "eu.anthropic.claude-opus-4-8:1m",
                        "eu.anthropic.claude-opus-5",
                        "eu.anthropic.claude-opus-5:1m",
                        "qwen.qwen3-vl-235b-a22b",
                        "us.meta.llama4-maverick-17b-instruct-v1:0",
                        "us.meta.llama4-scout-17b-instruct-v1:0",
                        "google.gemma-3-27b-it",
                        "nvidia.nemotron-nano-12b-v2",
                        "global.amazon.nova-2-lite-v1:0",
                        "global.anthropic.claude-haiku-4-5-20251001-v1:0",
                        "global.anthropic.claude-sonnet-4-5-20250929-v1:0",
                        "global.anthropic.claude-sonnet-4-6",
                        "global.anthropic.claude-sonnet-4-6:1m",
                        "global.anthropic.claude-sonnet-5",
                        "global.anthropic.claude-sonnet-5:1m",
                        "global.anthropic.claude-opus-4-5-20251101-v1:0",
                        "global.anthropic.claude-opus-4-6-v1",
                        "global.anthropic.claude-opus-4-6-v1:1m",
                        "global.anthropic.claude-opus-4-7",
                        "global.anthropic.claude-opus-4-7:1m",
                        "global.anthropic.claude-opus-4-8",
                        "global.anthropic.claude-opus-4-8:1m",
                        "global.anthropic.claude-opus-5",
                        "global.anthropic.claude-opus-5:1m"
                      ]
                    default: "us.anthropic.claude-sonnet-4-5-20250929-v1:0"
                    order: 0
                  temperature:
                    type: number
                    minimum: 0
                    maximum: 1
                    description: "Sampling temperature: Controls randomness in model output (0.0 = deterministic, higher = more random). Not used for Claude 4.7+ models."
                    default: 0.0
                    order: 1
                  top_k:
                    type: integer
                    minimum: 1
                    description: "Sampling Top K. Not used for Claude 4.7+ models."
                    default: 20
                    order: 2
                  top_p:
                    type: number
                    description: "Sampling Top P: Nucleus sampling threshold (0.0-1.0)"
                    default: 0.01
                    order: 3
                  max_tokens:
                    type: integer
                    description: "Optional cap on output tokens. Leave empty to use the selected model's maximum output limit (recommended). If set, it must not exceed the model's limit."
                    order: 4
                  system_prompt:
                    type: string
                    format: textarea
                    description: System prompt for rule validation orchestrator - defines the LLM's role in consolidating extracted facts into final compliance decisions
                    order: 5
                  task_prompt:
                    type: string
                    format: textarea
                    description: Task prompt for rule validation orchestrator - supports placeholders {recommendation_options}, {extracted_evidence}, {policy_class}, and {rule}. Optionally use <<CACHEPOINT>> to separate static and dynamic elements of prompt for Bedrock prompt caching.
                    order: 6
              postHook:
                type: array
                collapsible: true
                defaultExpanded: false
                sectionLabel: Post-step pipeline hooks
                description: >-
                  Lambda functions invoked after this step completes,
                  before the next step runs. Each hook receives the
                  document state and returns a JSON-serializable result
                  (logged for traceability). Hooks let vertical-product
                  packs (claims, loans, etc.) inject business logic
                  without forking the accelerator. Admins can edit, add,
                  reorder, or disable hooks here. The Lambda function
                  ARN must EITHER be tagged with `idp:feature-id=<anything>`
                  (vertical packs do this automatically) OR have a function
                  name starting with `GENAIIDP-` (admin-managed Lambdas).
                  Functions outside both categories will be rejected by
                  the dispatcher's IAM policy at invoke time.
                listLabel: Post-step pipeline hooks
                itemLabel: Hook
                order: 99
                items:
                  type: object
                  required:
                    - featureId
                    - arn
                  properties:
                    featureId:
                      type: string
                      description: Feature that owns this hook (label only)
                      order: 0
                    arn:
                      type: string
                      description: >-
                        Lambda function ARN to invoke. Must either be
                        tagged `idp:feature-id=<anything>` OR have a
                        function name beginning with `GENAIIDP-`.
                      order: 1
                    order:
                      type: number
                      description: Execution order (lower = earlier)
                      default: 100
                      order: 2
                    onError:
                      type: string
                      enum: ["continue", "fail", "skip-remaining"]
                      default: continue
                      description: Behavior when this hook errors
                      order: 3
                    enabled:
                      type: boolean
                      description: Disable without removing
                      default: true
                      order: 4
    UpdateReplacePolicy: Delete
    DeletionPolicy: Delete

  UpdateDefaultConfig:
    Type: AWS::CloudFormation::CustomResource
    Properties:
      ServiceToken: !Ref UpdateConfigurationFunctionArn
      Default: !Ref ConfigurationDefaultS3Uri
      DefaultPricing: !Sub "s3://${ConfigurationBucket}/config_library/pricing.yaml"
      DefaultModelConfigLimits: !Sub "s3://${ConfigurationBucket}/config_library/model_config_limits.yaml"
      ConfigLibraryHash: !Ref ConfigLibraryHash
      CustomClassificationModelARN: !Ref CustomClassificationModelARN
      CustomExtractionModelARN: !Ref CustomExtractionModelARN
      PreviousIDPPattern: !Ref PreviousIDPPattern
      fake-w2: !Sub "s3://${ConfigurationBucket}/config_library/managed_config/fake-w2/config.yaml"
      docsplit: !Sub "s3://${ConfigurationBucket}/config_library/managed_config/docsplit/config.yaml"
      ocr-benchmark: !Sub "s3://${ConfigurationBucket}/config_library/managed_config/ocr-benchmark/config.yaml"
      realkie-fcc-verified: !Sub "s3://${ConfigurationBucket}/config_library/managed_config/realkie-fcc-verified/config.yaml"
      confbench: !Sub "s3://${ConfigurationBucket}/config_library/managed_config/confbench/config.yaml"


  ##########################################################################
  # ECR Repository for Lambda container images
  ##########################################################################

  ECRRepository:
    Type: AWS::ECR::Repository
    # checkov:skip=CKV_AWS_51: "Mutable tags allowed for workflow flexibility and version updates."
    Properties:
      EmptyOnDelete: true
      # checkov:skip=CKV_AWS_163: "ECR scanning controlled by parameter for environment-specific deployment requirements"
      ImageScanningConfiguration:
        ScanOnPush: !If [IsECRImageScanningEnabled, true, false]
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Keep last 15 images",
                "selection": {
                  "tagStatus": "any",
                  "countType": "imageCountMoreThan",
                  "countNumber": 15
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }
      EncryptionConfiguration:
        EncryptionType: KMS
        KmsKey: !Ref CustomerManagedEncryptionKeyArn

  ##########################################################################
  # CodeBuild for building Lambda container images
  ##########################################################################

  DockerBuildRole:
    Type: AWS::IAM::Role
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Wildcard permissions required for CloudWatch Logs creation"
          - id: W12
            reason: "Amazon Inspector ListCoverage and ListFindings require wildcard resource per AWS documentation"
    Properties:
      Path: /
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Action: sts:AssumeRole
            Effect: Allow
            Principal:
              Service: !Sub "codebuild.${AWS::URLSuffix}"
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      ManagedPolicyArns:
        - !Sub "arn:${AWS::Partition}:iam::aws:policy/AmazonEC2ContainerRegistryPowerUser"
      Policies:
        - PolicyName: CodeBuildAccess
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Resource:
                  - !Sub "arn:${AWS::Partition}:s3:::${ArtifactBucket}"
                  - !Sub "arn:${AWS::Partition}:s3:::${ArtifactBucket}/${ArtifactPrefix}/*"
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                  - s3:GetBucketLocation
                  - s3:ListBucket
              - Resource:
                  - !Sub "arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/*"
                  - !Sub "arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/codebuild/*:*"
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
              - Resource:
                  - !GetAtt ECRRepository.Arn
                Effect: Allow
                Action:
                  - ecr:DescribeImageScanFindings
                  - ecr:DescribeImages
                  - ecr:StartImageScan
              # Required for Amazon Inspector Enhanced Scanning
              # https://docs.aws.amazon.com/AmazonECR/latest/userguide/image-scanning-enhanced-iam.html
              - Resource: "*"
                Effect: Allow
                Action:
                  - inspector2:ListCoverage
                  - inspector2:ListFindings
              # Required when ECR repository uses customer-managed KMS key encryption
              - Resource:
                  - !Ref CustomerManagedEncryptionKeyArn
                Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:CreateGrant
              # Grant kms:Decrypt on the artifacts bucket KMS key when provided
              # This allows DockerBuildRole to download source code from a KMS-encrypted artifact bucket
              - !If
                - HasArtifactsBucketKmsKey
                - Effect: Allow
                  Action:
                    - kms:Decrypt
                    - kms:GenerateDataKey*
                    - kms:DescribeKey
                  Resource: !Ref ArtifactsBucketKmsKeyArn
                - !Ref AWS::NoValue
              - !If
                - DeployInVPC
                - Effect: Allow
                  Action:
                    - ec2:CreateNetworkInterface
                    - ec2:DescribeNetworkInterfaces
                    - ec2:DeleteNetworkInterface
                    - ec2:DescribeSecurityGroups
                    - ec2:DescribeSubnets
                    - ec2:DescribeVpcs
                    - ec2:DescribeDhcpOptions
                  Resource: "*"
                - !Ref AWS::NoValue
              - !If
                - DeployInVPC
                - Effect: Allow
                  Action:
                    - ec2:CreateNetworkInterfacePermission
                  Resource: !Sub "arn:${AWS::Partition}:ec2:${AWS::Region}:${AWS::AccountId}:network-interface/*"
                  Condition:
                    StringEquals:
                      ec2:AuthorizedService: !Sub "codebuild.${AWS::URLSuffix}"
                - !Ref AWS::NoValue

  DockerBuildProject:
    Type: AWS::CodeBuild::Project
    # cdk-nag suppress: AwsSolutions-CB5 - Privileged mode required for Docker image builds
    Properties:
      Name: !Sub "${AWS::StackName}-docker-build"
      Description: !Sub >-
        Unified Docker image builder for GenAIIDP stack - ${AWS::StackName}
      ServiceRole: !GetAtt DockerBuildRole.Arn
      EncryptionKey: !Ref CustomerManagedEncryptionKeyArn
      VpcConfig:
        !If
          - DeployInVPC
          - VpcId: !Ref VpcId
            Subnets: !Ref PrivateSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Artifacts:
        Type: NO_ARTIFACTS
      Source:
        Location: !Sub "arn:${AWS::Partition}:s3:::${ArtifactBucket}/${ArtifactPrefix}/${SourceZipfile}"
        Type: S3
        BuildSpec: patterns/unified/buildspec.yml
      Environment:
        ComputeType: BUILD_GENERAL1_LARGE
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
        Type: LINUX_CONTAINER
        PrivilegedMode: true
        EnvironmentVariables:
          - Name: ECR_URI
            Value: !GetAtt ECRRepository.RepositoryUri
          - Name: IMAGE_VERSION
            Value: !Ref ImageVersion
          - Name: AWS_REGION
            Value: !Ref AWS::Region
          - Name: LAMBDA_ARCHITECTURE
            Value: !Ref LambdaArchitecture
      TimeoutInMinutes: 90

  CodeBuildExecutionRole:
    Type: AWS::IAM::Role
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Role requires * resource access for CloudWatch Logs and CloudFormation custom resource operations"
    # checkov:skip=CKV_AWS_111: "Write access required for CloudWatch Logs and CloudFormation custom resource operations"
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - !Sub "lambda.${AWS::URLSuffix}"
            Action:
              - sts:AssumeRole
      Path: "/"
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      Policies:
        - PolicyName: root
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - codebuild:StartBuild
                  - codebuild:BatchGetBuilds
                Resource:
                  - !GetAtt DockerBuildProject.Arn
              - Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
              - Effect: Allow
                Action:
                  - ecr:ListImages
                  - ecr:BatchDeleteImage
                  - ecr:DescribeImages
                Resource:
                  - !GetAtt ECRRepository.Arn
        # Used by custom resource helper poller
        # https://github.com/aws-cloudformation/custom-resource-helper
        - PolicyName: CustomResourcePoller
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - events:PutRule
                  - events:DeleteRule
                  - events:PutTargets
                  - events:RemoveTargets
                Resource:
                  - !Sub "arn:${AWS::Partition}:events:${AWS::Region}:${AWS::AccountId}:rule/*"
              - Effect: Allow
                Action:
                  - lambda:AddPermission
                  - lambda:RemovePermission
                Resource:
                  - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:*"
      ManagedPolicyArns:
        - !If
          - DeployInVPC
          - !Sub "arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole"
          - !Ref AWS::NoValue

  CodeBuildTrigger:
    Type: AWS::Serverless::Function
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "Function does not require VPC access as it only interacts with AWS services via APIs"
          - id: W92
            reason: "Function does not require reserved concurrency as it scales based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for Cfn Custom Resource function"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      Role: !GetAtt CodeBuildExecutionRole.Arn
      Runtime: python3.12
      Timeout: 60
      VpcConfig:
        !If
          - DeployInVPC
          - SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
            SubnetIds: !Ref PrivateSubnetIds
          - !Ref AWS::NoValue
      MemorySize: 128
      Handler: index.handler
      CodeUri: ../../src/lambda/start_codebuild
      Description:  CodeBuild trigger Lambda for Docker image builds
      LoggingConfig:
        LogGroup: !Ref CodeBuildTriggerLogGroup

  CodeBuildTriggerLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/CodeBuildTrigger"
      RetentionInDays: !Ref LogRetentionDays

  DockerBuildRun:
    Type: Custom::CodeBuildRun
    Properties:
      ServiceToken: !GetAtt CodeBuildTrigger.Arn
      BuildProjectName: !Ref DockerBuildProject
      # Force rebuild when source changes (filename includes content hash)
      CodeLocation: !Sub "arn:${AWS::Partition}:s3:::${ArtifactBucket}/${ArtifactPrefix}/${SourceZipfile}"
      ImageVersion: !Ref ImageVersion
      LambdaArchitecture: !Ref LambdaArchitecture
      ExpectedImages:
        - "bda-invoke-function"
        - "bda-completion-function"
        - "bda-processresults-function"
        - "ocr-function"
        - "classification-function"
        - "extraction-function"
        - "assessment-function"
        - "processresults-function"
        - "summarization-function"
        - "evaluation-function"
        - "rule-validation-function"
        - "rule-validation-orchestration-function"
        - "mlflow-logger-function"

  EcrRepositoryCleanup:
    Type: Custom::ECRRepositoryCleanup
    DependsOn:
      - DockerBuildRun
    Properties:
      ServiceToken: !GetAtt CodeBuildTrigger.Arn
      RepositoryName: !Ref ECRRepository

  # Shared IAM policy for Lambda functions to pull container images from ECR
  LambdaECRAccessPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Allows Lambda functions to pull container images from ECR
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action:
              - ecr:GetDownloadUrlForLayer
              - ecr:BatchGetImage
              - ecr:BatchCheckLayerAvailability
            Resource: !Sub "arn:${AWS::Partition}:ecr:${AWS::Region}:${AWS::AccountId}:repository/*"


  ##########################################################################
  # BDA Branch Lambda Functions (from Pattern-1)
  ##########################################################################

  InvokeBDAFunction:
    DependsOn: DockerBuildRun
    Type: AWS::Serverless::Function
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:bda-invoke-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      Environment:
        Variables:
          TRACKING_TABLE: !Ref TrackingTable
          METRIC_NAMESPACE: !Ref StackName
          MAX_WORKERS: 20
          LOG_LEVEL: !Ref LogLevel
      LoggingConfig:
        LogGroup: !Ref InvokeBDAFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - DynamoDBCrudPolicy:
           TableName: !Ref TrackingTable
        - Statement:
          - Effect: Allow
            Action: cloudwatch:PutMetricData
            Resource: '*'
          - Effect: Allow
            Action:
              - kms:Encrypt
              - kms:Decrypt
              - kms:ReEncrypt*
              - kms:GenerateDataKey*
              - kms:DescribeKey
            Resource: !Ref CustomerManagedEncryptionKeyArn
          - Effect: Allow
            Action: bedrock:InvokeDataAutomationAsync
            Resource: 
              - !Sub "arn:${AWS::Partition}:bedrock:*:${AWS::AccountId}:data-automation-project/*"
              - !Sub "arn:${AWS::Partition}:bedrock:*:${AWS::AccountId}:data-automation-profile/*.data-automation-v1"
  
  InvokeBDAFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/InvokeBDAFunction"
      RetentionInDays: !Ref LogRetentionDays
  
  BDAProcessResultsFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs — placed in VPC when ApiGatewayVisibility=PRIVATE"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"    
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:bda-processresults-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          DB_NAME: !Ref BDAMetadataTable
          WORKING_BUCKET: !Ref WorkingBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
      LoggingConfig:
        LogGroup: !Ref BDAProcessResultsFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref BDAMetadataTable
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
          - Effect: Allow
            Action: cloudwatch:PutMetricData
            Resource: '*'
          - Effect: Allow
            Action:
              - kms:Encrypt
              - kms:Decrypt
              - kms:ReEncrypt*
              - kms:GenerateDataKey*
              - kms:DescribeKey
            Resource: !Ref CustomerManagedEncryptionKeyArn
          - Effect: Allow
            Action:
              - ssm:GetParameter
              - ssm:PutParameter
              - ssm:GetParametersByPath
            Resource: '*'
          - Effect: Allow
            Action:
              - bedrock:GetDataAutomationProject
              - bedrock:ListDataAutomationProjects
              - bedrock:GetBlueprint
              - bedrock:GetBlueprintRecommendation
            Resource: '*'

  
  BDAProcessResultsFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/BDAProcessResultsFunction"
      RetentionInDays: !Ref LogRetentionDays

  BDACompletionFunctionDLQ:
    Type: AWS::SQS::Queue
    Metadata:
      cdk_nag:
        rules_to_suppress:
          - id: AwsSolutions-SQS3
            reason: "This queue is itself a DLQ - does not need its own DLQ"
    Properties:
      KmsMasterKeyId: !Ref CustomerManagedEncryptionKeyArn
      VisibilityTimeout: 30
      MessageRetentionPeriod: 345600 # 4 days

  BDACompletionFunctionDLQPolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      Queues:
        - !Ref BDACompletionFunctionDLQ
      PolicyDocument:
        Statement:
          - Effect: Deny
            Principal: '*'
            Action: 'sqs:*'
            Resource: !GetAtt BDACompletionFunctionDLQ.Arn
            Condition:
              Bool:
                'aws:SecureTransport': false

  BDACompletionFunction:
    DependsOn: DockerBuildRun
    Type: AWS::Serverless::Function
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:bda-completion-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      Environment:
        Variables:
          TRACKING_TABLE: !Ref TrackingTable
          METRIC_NAMESPACE: !Ref StackName
          LOG_LEVEL: !Ref LogLevel
      DeadLetterQueue:
       Type: SQS
       TargetArn: !GetAtt BDACompletionFunctionDLQ.Arn
      LoggingConfig:
        LogGroup: !Ref BDACompletionFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - DynamoDBReadPolicy:
           TableName: !Ref TrackingTable
        - Statement:
          - Effect: Allow
            Action: cloudwatch:PutMetricData
            Resource: '*'
          - Effect: Allow
            Action: 
              - states:SendTaskSuccess
              - states:SendTaskFailure
            Resource: !GetAtt DocumentProcessingStateMachine.Arn
          - Effect: Allow
            Action:
              - kms:Encrypt
              - kms:Decrypt
              - kms:ReEncrypt*
              - kms:GenerateDataKey*
              - kms:DescribeKey
            Resource: !Ref CustomerManagedEncryptionKeyArn

  
  BDACompletionFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/BDACompletionFunction"
      RetentionInDays: !Ref LogRetentionDays

  BDAEventRule:
    Type: AWS::Events::Rule
    Properties:
      EventPattern:
        source:
          - aws.bedrock
        detail-type:
          - Bedrock Data Automation Job Succeeded
          - Bedrock Data Automation Job Failed With Client Error
          - Bedrock Data Automation Job Failed With Service Error
      State: ENABLED
      Targets:
        - Arn: !GetAtt BDACompletionFunction.Arn
          Id: BDACompletionFunction
          RetryPolicy:
            MaximumRetryAttempts: 3

  BDAEventPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref BDACompletionFunction
      Principal: !Sub "events.${AWS::URLSuffix}"
      SourceArn: !GetAtt BDAEventRule.Arn


  # DynamoDB Table for BDA process records metadata
  BDAMetadataTable:
    Type: AWS::DynamoDB::Table
    Properties:
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: execution_id
          AttributeType: S
        - AttributeName: record_number
          AttributeType: N
      KeySchema:
        - AttributeName: execution_id
          KeyType: HASH
        - AttributeName: record_number
          KeyType: RANGE
      TimeToLiveSpecification:
        AttributeName: ExpiresAfter
        Enabled: true
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !Ref CustomerManagedEncryptionKeyArn


  ##########################################################################
  # Stack-scoped BDA OCR project (CloudFormation custom resource)
  #
  # Provisions a per-stack Bedrock Data Automation standard-output SYNC project
  # for the 'bda' OCR backend, created on stack create and deleted on stack
  # delete. Replaces the former account-global auto-created project so multiple
  # stacks in one account no longer interfere. Degrades gracefully (empty ARN)
  # in regions/partitions without Bedrock Data Automation.
  ##########################################################################

  BDAOCRProjectFunction:
    Type: AWS::Serverless::Function
    Metadata:
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "Function does not require VPC access as it only interacts with AWS services via APIs"
          - id: W92
            reason: "Function does not require reserved concurrency as it scales based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for Cfn Custom Resource function"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      Runtime: python3.12
      Handler: index.handler
      CodeUri: ../../src/lambda/bda_ocr_project
      Description: Manages the stack-scoped BDA OCR standard-output project
      # Timeout must exceed the ~120s wait for the project to reach COMPLETED.
      Timeout: 300
      MemorySize: 128
      VpcConfig:
        !If
          - DeployInVPC
          - SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
            SubnetIds: !Ref PrivateSubnetIds
          - !Ref AWS::NoValue
      Environment:
        Variables:
          LOG_LEVEL: !Ref LogLevel
          # Main stack name (not the verbose nested stack name) so the project
          # is named <mainstack>_OCR_StdOutput.
          STACK_NAME: !Ref StackName
      LoggingConfig:
        LogGroup: !Ref BDAOCRProjectFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !If
          - DeployInVPC
          - Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - ec2:CreateNetworkInterface
                  - ec2:DescribeNetworkInterfaces
                  - ec2:DeleteNetworkInterface
                Resource: "*"
          - !Ref AWS::NoValue
        - Statement:
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # Control-plane management of the stack-scoped BDA OCR project.
            - Effect: Allow
              Action:
                - bedrock:CreateDataAutomationProject
                - bedrock:GetDataAutomationProject
                - bedrock:UpdateDataAutomationProject
                - bedrock:DeleteDataAutomationProject
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:data-automation-project/*"
            # ListDataAutomationProjects is a collection op without resource-level scoping.
            - Effect: Allow
              Action:
                - bedrock:ListDataAutomationProjects
              Resource: "*"

  BDAOCRProjectFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/BDAOCRProjectFunction"
      RetentionInDays: !Ref LogRetentionDays

  BDAOCRProject:
    Type: Custom::BDAOCRProject
    Properties:
      ServiceToken: !GetAtt BDAOCRProjectFunction.Arn
      StackName: !Ref StackName
      # Bump ConfigVersion to force the custom resource to re-run (e.g. after
      # changing the project's standard-output / modality-routing config).
      ConfigVersion: "1"

  ##########################################################################
  # Pipeline Branch Lambda Functions (from Pattern-2)
  ##########################################################################

  OCRFunction:
    Type: AWS::Serverless::Function
    # Ordering on BDAOCRProject is enforced implicitly by the !GetAtt on its
    # ProjectArn in the environment below.
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
          - id: W11
            reason: "Textract DetectDocumentText API and Cloudwatch APIs do not support resource-level permissions"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:ocr-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          MAX_WORKERS: 20
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          # ARN of the stack-scoped BDA OCR project (empty in regions without
          # Bedrock Data Automation; the 'bda' backend then errors clearly).
          BDA_OCR_PROJECT_ARN: !GetAtt BDAOCRProject.ProjectArn
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref OCRFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # Textract APIs require '*' as they don't support resource-level permissions
            - Effect: Allow
              Action:
                - textract:DetectDocumentText
                - textract:AnalyzeDocument
              Resource: "*"
            # Bedrock permissions for OCR
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            # Optional cross-account Bedrock invocation via hub-account role.
            # When BedrockHubRoleArn is set, the function assumes this role for
            # all Bedrock data-plane and control-plane calls. The local Bedrock
            # statements above are left in place so same-account invocations
            # continue to work when the parameter is unset.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            # Lambda invoke permission for LambdaHook custom inference
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*"
              Condition:
                StringLike:
                  "lambda:FunctionName": "GENAIIDP-*"
            # Bedrock Data Automation data-plane permission for the 'bda' OCR
            # backend. Sync InvokeDataAutomation on the stack-scoped standard-
            # output project (provisioned by the BDAOCRProject custom resource)
            # + the standard data-automation profile. Control-plane management
            # of the project lives on BDAOCRProjectFunction, not here.
            - Effect: Allow
              Action:
                - bedrock:InvokeDataAutomation
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:data-automation-project/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:data-automation-profile/*.data-automation-v1"
                - !Sub "arn:${AWS::Partition}:bedrock:*:${AWS::AccountId}:data-automation-profile/*.data-automation-v1"

  OCRFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/OCRFunction"
      RetentionInDays: !Ref LogRetentionDays

  ClassificationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:classification-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          MAX_WORKERS: 20
          TRACKING_TABLE: !Ref TrackingTable
          CONFIGURATION_BUCKET: !Ref ConfigurationBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          LOG_LEVEL: !Ref LogLevel
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          # Guardrails break classification
          # GUARDRAIL_ID_AND_VERSION: !If [HasGuardrailConfig, !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}", ""]
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref ClassificationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - S3ReadPolicy:
            BucketName: !Ref ConfigurationBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
                - !If
                  - HasCustomClassificationModelARN
                  - !Ref CustomClassificationModelARN
                  - !Ref AWS::NoValue
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            # Optional cross-account Bedrock invocation via hub-account role.
            # When BedrockHubRoleArn is set, the function assumes this role for
            # all Bedrock data-plane and control-plane calls. The local Bedrock
            # statements above are left in place so same-account invocations
            # continue to work when the parameter is unset.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            # Lambda invoke permission for LambdaHook custom inference
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*"
              Condition:
                StringLike:
                  "lambda:FunctionName": "GENAIIDP-*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  ClassificationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/ClassificationFunction"
      RetentionInDays: !Ref LogRetentionDays

  ExtractionFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions, and Bedrock should support any enabled Bedrock model_id or inference profile"
          - id: W76
            reason: "Suppressing W76: SPCM for IAM policy document is higher than 25"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:extraction-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_BUCKET: !Ref ConfigurationBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          GUARDRAIL_ID_AND_VERSION:
            !If [
              HasGuardrailConfig,
              !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}",
              "",
            ]
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref ExtractionFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3ReadPolicy:
            BucketName: !Ref ConfigurationBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
                - !If
                  - HasCustomExtractionModelARN
                  - !Ref CustomExtractionModelARN
                  - !Ref AWS::NoValue
            # Optional cross-account Bedrock invocation via hub-account role.
            # When BedrockHubRoleArn is set, the function assumes this role for
            # all Bedrock data-plane and control-plane calls. The local Bedrock
            # statements above are left in place so same-account invocations
            # continue to work when the parameter is unset.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*"
              Condition:
                StringLike:
                  "lambda:FunctionName": "GENAIIDP-*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  ExtractionFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/ExtractionFunction"
      RetentionInDays: !Ref LogRetentionDays

  # Nested SFN Distributed Map shard-runtime function (Phase 3). Reuses the SAME
  # extraction container image (no extra Docker build), selected via a different
  # ImageConfig.Command. A single handler dispatches plan/shard/merge modes, each
  # reusing idp_common.extraction primitives (extract_one_shard, merge_shard_dicts)
  # — the single source of truth shared with the in-process runtime. One fresh
  # 15-min Lambda runs per shard, so SFN's native per-iteration retry re-runs only
  # failed/incomplete shards (completed shards load idempotently from S3).
  ShardRuntimeFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions, and Bedrock should support any enabled Bedrock model_id or inference profile"
          - id: W76
            reason: "Suppressing W76: SPCM for IAM policy document is higher than 25"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:extraction-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "sfn_runtime_handler.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_BUCKET: !Ref ConfigurationBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          GUARDRAIL_ID_AND_VERSION:
            !If [
              HasGuardrailConfig,
              !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}",
              "",
            ]
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref ShardRuntimeFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3ReadPolicy:
            BucketName: !Ref ConfigurationBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
                - !If
                  - HasCustomExtractionModelARN
                  - !Ref CustomExtractionModelARN
                  - !Ref AWS::NoValue
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  ShardRuntimeFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/ShardRuntimeFunction"
      RetentionInDays: !Ref LogRetentionDays

  AssessmentFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions, and Bedrock should support any enabled Bedrock model_id or inference profile"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:assessment-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_BUCKET: !Ref ConfigurationBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref AssessmentFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3ReadPolicy:
            BucketName: !Ref ConfigurationBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            # Optional cross-account Bedrock invocation via hub-account role.
            # When BedrockHubRoleArn is set, the function assumes this role for
            # all Bedrock data-plane and control-plane calls. The local Bedrock
            # statements above are left in place so same-account invocations
            # continue to work when the parameter is unset.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            # Lambda invoke permission for LambdaHook custom inference
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*"
              Condition:
                StringLike:
                  "lambda:FunctionName": "GENAIIDP-*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  AssessmentFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/AssessmentFunction"
      RetentionInDays: !Ref LogRetentionDays

  ProcessResultsFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:processresults-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          OUTPUT_BUCKET: !Ref OutputBucket
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
      LoggingConfig:
        LogGroup: !Ref ProcessResultsFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn

  ProcessResultsFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/ProcessResultsFunction"
      RetentionInDays: !Ref LogRetentionDays

  SummarizationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:summarization-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          GUARDRAIL_ID_AND_VERSION:
            !If [
              HasGuardrailConfig,
              !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}",
              "",
            ]
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref SummarizationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            # Optional cross-account Bedrock invocation via hub-account role.
            # When BedrockHubRoleArn is set, the function assumes this role for
            # all Bedrock data-plane and control-plane calls. The local Bedrock
            # statements above are left in place so same-account invocations
            # continue to work when the parameter is unset.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            # Lambda invoke permission for LambdaHook custom inference
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*"
              Condition:
                StringLike:
                  "lambda:FunctionName": "GENAIIDP-*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  SummarizationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/SummarizationFunction"
      RetentionInDays: !Ref LogRetentionDays

  ##########################################################################
  # PipelineHooksDispatcherFunction — invoked from the SFN at each pipeline
  # extension point. Reads `Configuration#System#PipelineHooks` from the
  # ConfigurationTable and fans out to feature-registered hook Lambdas.
  # Zero-cost when no hooks are registered (one DDB GetItem + return).
  ##########################################################################
  PipelineHooksDispatcherFunctionRole:
    Type: AWS::IAM::Role
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: !Sub "lambda.${AWS::URLSuffix}" }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Sub "arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
      Policies:
        - PolicyName: ReadActiveConfigForHooks
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              # GetItem on any Config#* row, plus Scan to find the
              # IsActive=true version when the document doesn't pin one.
              # Hooks are stored inline in each config version under
              # `<step>.postHook` lists.
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:Scan
                Resource: !Sub 'arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${ConfigurationTable}'
              # Status-only write: flip the doc row to PREPROCESSING while a
              # preprocessing hook runs (UI step visibility; best-effort).
              - Effect: Allow
                Action:
                  - dynamodb:UpdateItem
                Resource: !Sub 'arn:${AWS::Partition}:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${TrackingTable}'
              - Effect: Allow
                Action:
                  - kms:Decrypt
                  - kms:DescribeKey
                  # Encrypt/GenerateDataKey are needed to WRITE the
                  # compressed-document object below (the working bucket is
                  # encrypted with the customer-managed key).
                  - kms:Encrypt
                  - kms:GenerateDataKey
                Resource: !Ref CustomerManagedEncryptionKeyArn
              # Document mutation: when a hook returns an INLINE updated
              # document dict, the dispatcher spills it to the working bucket
              # in the same compressed-wrapper shape the step Lambdas use, so
              # the next step's Document.load_document() resolves it normally.
              # Scoped to the compressed_documents/ prefix the wrapper points
              # at. PutObject only — the dispatcher never reads documents back
              # (it passes references through opaquely), so a hook that needs
              # to READ the document uses its own role.
              - Effect: Allow
                Action: s3:PutObject
                Resource: !Sub 'arn:${AWS::Partition}:s3:::${WorkingBucket}/compressed_documents/*'
              # Two parallel allow paths for hook Lambdas:
              #
              # 1. Tag-based ABAC for vertical-product packs. Pack-shipped
              #    hook Lambdas MUST carry the tag `idp:feature-id=<id>`
              #    (see e.g. claims-pack/template.yaml). The condition
              #    matches any value of that tag.
              #
              # 2. Name-prefix for admin-managed Lambdas. Mirrors the
              #    GENAIIDP-* convention used elsewhere in the host (e.g.
              #    custom-extraction-prompt). Admins can paste any
              #    GENAIIDP-* function ARN into the postHook fields without
              #    having to tag it.
              #
              # The dispatcher trusts whichever clears IAM. Function ARNs
              # outside both categories fail closed with AccessDenied —
              # the schema description warns admins.
              - Effect: Allow
                Action: lambda:InvokeFunction
                Resource: !Sub 'arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:*'
                Condition:
                  StringLike:
                    'aws:ResourceTag/idp:feature-id': '*'
              - Effect: Allow
                Action: lambda:InvokeFunction
                Resource: !Sub 'arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:GENAIIDP-*'

  PipelineHooksDispatcherFunction:
    Type: AWS::Serverless::Function
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      CodeUri: src/pipeline_hooks_function/
      Handler: index.lambda_handler
      Runtime: python3.12
      Architectures: [arm64]
      # Hooks are invoked synchronously (RequestResponse) through the
      # dispatcher, so its timeout must cover the LONGEST hook it fronts —
      # the PII-redaction preprocessing hook budgets up to ~890s for
      # multi-page Textract+vision passes. 900 is the Lambda maximum. (The
      # boto3 lambda client read_timeout is raised to match in index.py;
      # boto's default ~60s would sever the invoke early regardless of this
      # value.)
      Timeout: 900
      MemorySize: 256
      Tracing: Active
      Role: !GetAtt PipelineHooksDispatcherFunctionRole.Arn
      Environment:
        Variables:
          LOG_LEVEL: !Ref LogLevel
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          # Lets the dispatcher surface PREPROCESSING as the document's visible
          # status while a preprocessing hook runs (best-effort, cosmetic).
          TRACKING_TABLE: !Ref TrackingTable
          # Destination for a hook-returned INLINE updated document, spilled to
          # S3 as a compressed reference for the next step. Unused when hooks
          # are read-only or return their own compressed reference.
          WORKING_BUCKET: !Ref WorkingBucket

  PipelineHooksDispatcherFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${PipelineHooksDispatcherFunction}'
      RetentionInDays: !Ref LogRetentionDays
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn

  EvaluationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Role requires * resource access for CloudWatch Metrics and Logs"
          - id: W89
            reason: "Function does not require VPC access as it only interacts with AWS services via APIs"
          - id: W92
            reason: "Function does not require reserved concurrency as it scales based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:evaluation-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          LOG_LEVEL: !Ref LogLevel
          METRIC_NAMESPACE: !Ref StackName
          PROCESSING_OUTPUT_BUCKET: !Ref OutputBucket
          EVALUATION_OUTPUT_BUCKET: !Ref OutputBucket
          REPORTING_BUCKET: !Ref ReportingBucket
          BASELINE_BUCKET: !Ref BaselineBucket
          SAVE_REPORTING_FUNCTION_NAME: !Ref SaveReportingFunctionName
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          WORKING_BUCKET: !Ref WorkingBucket
          DOCUMENT_TRACKING_MODE: dynamodb
          TRACKING_TABLE: !Ref TrackingTable
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref EvaluationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - DynamoDBReadPolicy:
            TableName: !Ref ConfigurationTable
        - S3ReadPolicy:
            BucketName: !Ref BaselineBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
            # Optional cross-account Bedrock invocation via hub-account role.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            # Lambda invoke permission for SaveReportingFunction (in main template)
            - Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:${SaveReportingFunctionName}"

  EvaluationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/EvaluationFunction"
      RetentionInDays: !Ref LogRetentionDays

  ##########################################################################
  # Test Execution Aggregation Function
  ##########################################################################

  TestExecutionAggregationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as it is invoked synchronously"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:test-execution-aggregation-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      Timeout: 900
      MemorySize: 4096
      Tracing: Active
      Environment:
        Variables:
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          OUTPUT_BUCKET: !Ref OutputBucket
      LoggingConfig:
        LogGroup: !Ref TestExecutionAggregationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - DynamoDBReadPolicy:
            TableName: !Ref TrackingTable
        - S3ReadPolicy:
            BucketName: !Ref OutputBucket
        - Statement:
          - Effect: Allow
            Action: cloudwatch:PutMetricData
            Resource: '*'
          - Effect: Allow
            Action:
              - kms:Decrypt
              - kms:DescribeKey
            Resource: !Ref CustomerManagedEncryptionKeyArn

  TestExecutionAggregationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/TestExecutionAggregationFunction"
      RetentionInDays: !Ref LogRetentionDays

  RuleValidationPolicyClassificationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:rule-validation-policy-classification-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 300
      MemorySize: 1024
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          OUTPUT_BUCKET: !Ref OutputBucket
          REPORTING_BUCKET: !Ref ReportingBucket
          SAVE_REPORTING_FUNCTION_NAME: !Ref SaveReportingFunctionName
      LoggingConfig:
        LogGroup: !Ref RuleValidationPolicyClassificationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - S3CrudPolicy:
            BucketName: !Ref ReportingBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            - Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource:
                - !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:${SaveReportingFunctionName}"

  RuleValidationPolicyClassificationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/RuleValidationPolicyClassificationFunction"
      RetentionInDays: !Ref LogRetentionDays

  RuleValidationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values like feature flags and non-sensitive settings"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      # Container configuration
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:rule-validation-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      # Local testing configuration (commented for deployment)
      # CodeUri: src/rule-validation-function/
      # Handler: index.handler
      # Runtime: python3.12
      Timeout: 900
      MemorySize: 4096
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          GUARDRAIL_ID_AND_VERSION:
            !If [
              HasGuardrailConfig,
              !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}",
              "",
            ]
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref RuleValidationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
                # Custom model deployments created via fine-tuning feature
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:custom-model-deployment/*"
            # Optional cross-account Bedrock invocation via hub-account role.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action: bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue

  RuleValidationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/RuleValidationFunction"
      RetentionInDays: !Ref LogRetentionDays

  RuleValidationOrchestrationFunction:
    Type: AWS::Serverless::Function
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "Cloudwatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      # Container configuration
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:rule-validation-orchestration-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      # Local testing configuration (commented for deployment)
      # CodeUri: src/rule-validation-orchestration-function/
      # Handler: index.handler
      # Runtime: python3.12
      Timeout: 900
      MemorySize: 4096
      VpcConfig: !If
        - DeployInVPC
        - SubnetIds: !Ref PrivateSubnetIds
          SecurityGroupIds:
            - !Ref LambdaSecurityGroupId
        - !If
          - IsPrivateApi
          - SubnetIds: !Ref LambdaSubnetIds
            SecurityGroupIds:
              - !Ref LambdaSecurityGroupId
          - !Ref AWS::NoValue
      Environment:
        Variables:
          METRIC_NAMESPACE: !Ref StackName
          CONFIGURATION_TABLE_NAME: !Ref ConfigurationTable
          GUARDRAIL_ID_AND_VERSION:
            !If [
              HasGuardrailConfig,
              !Sub "${BedrockGuardrailId}:${BedrockGuardrailVersion}",
              "",
            ]
          LOG_LEVEL: !Ref LogLevel
          TRACKING_TABLE: !Ref TrackingTable
          DOCUMENT_TRACKING_MODE: dynamodb
          WORKING_BUCKET: !Ref WorkingBucket
          REPORTING_BUCKET: !Ref ReportingBucket
          SAVE_REPORTING_FUNCTION_NAME: !Ref SaveReportingFunctionName
          BEDROCK_ASSUME_ROLE_ARN:
            !If [HasBedrockHubRole, !Ref BedrockHubRoleArn, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_EXTERNAL_ID:
            !If [HasBedrockHubRoleExternalId, !Ref BedrockHubRoleExternalId, !Ref AWS::NoValue]
          BEDROCK_ASSUME_ROLE_SESSION_NAME:
            !If [HasBedrockHubRoleSessionName, !Ref BedrockHubRoleSessionName, !Ref AWS::NoValue]
      LoggingConfig:
        LogGroup: !Ref RuleValidationOrchestrationFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - S3ReadPolicy:
            BucketName: !Ref InputBucket
        - S3CrudPolicy:
            BucketName: !Ref OutputBucket
        - S3CrudPolicy:
            BucketName: !Ref WorkingBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref ConfigurationTable
        - DynamoDBCrudPolicy:
            TableName: !Ref TrackingTable
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            # OpenAI GPT-5.x models are served via the bedrock-mantle endpoint
            # (OpenAI Responses API) and use a separate IAM action namespace.
            - Effect: Allow
              Action:
                - bedrock-mantle:CreateInference
                - bedrock-mantle:GetProject
                - bedrock-mantle:ListProjects
                - bedrock-mantle:ListTagsForResources
              Resource: "*"
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
                - bedrock:GetInferenceProfile
              Resource:
                - !Sub "arn:${AWS::Partition}:bedrock:*::foundation-model/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"
                - !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:application-inference-profile/*"
            # Optional cross-account Bedrock invocation via hub-account role.
            - !If
              - HasBedrockHubRole
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: !Ref BedrockHubRoleArn
              - !Ref AWS::NoValue
            - Effect: Allow
              Action:
                - aws-marketplace:Subscribe
                - aws-marketplace:Unsubscribe
                - aws-marketplace:ViewSubscriptions
              Resource: "*"
            - !If
              - HasGuardrailConfig
              - Effect: Allow
                Action:
                  - bedrock:ApplyGuardrail
                Resource: !Sub "arn:${AWS::Partition}:bedrock:${AWS::Region}:${AWS::AccountId}:guardrail/${BedrockGuardrailId}"
              - !Ref AWS::NoValue
            - Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource: !Sub "arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:${SaveReportingFunctionName}"

  RuleValidationOrchestrationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/RuleValidationOrchestrationFunction"
      RetentionInDays: !Ref LogRetentionDays

  ##########################################################################
  # MLflow Logger Function
  ##########################################################################

  MLflowLoggerFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Condition: IsMLflowEnabled
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/${AWS::StackName}/lambda/MLflowLoggerFunction"
      RetentionInDays: !Ref LogRetentionDays

  MLflowLoggerFunction:
    Type: AWS::Serverless::Function
    Condition: IsMLflowEnabled
    DependsOn: DockerBuildRun
    Metadata:
      SkipBuild: True
      cfn_nag:
        rules_to_suppress:
          - id: W11
            reason: "CloudWatch does not support resource-level permissions"
          - id: W89
            reason: "This Lambda function does not require VPC access as it only interacts with AWS services via AWS APIs"
          - id: W92
            reason: "Function does not require concurrent execution limits as it is designed to scale based on demand"
    # checkov:skip=CKV_AWS_116: "DLQ not required for this function as StepFunctions will handle retries"
    # checkov:skip=CKV_AWS_173: "Environment variables do not contain sensitive data - only configuration values"
    # checkov:skip=CKV_AWS_117: "Function does not require VPC access as it only interacts with AWS services via APIs"
    # checkov:skip=CKV_AWS_115: "Function does not require reserved concurrency as it scales based on demand"
    Properties:
      PermissionsBoundary:
        !If [
          HasPermissionsBoundary,
          !Ref PermissionsBoundaryArn,
          !Ref AWS::NoValue,
        ]
      PackageType: Image
      ImageUri: !Sub "${ECRRepository.RepositoryUri}:mlflow-logger-function-${ImageVersion}"
      Architectures:
        - !Ref LambdaArchitecture
      ImageConfig:
        Command:
          - "index.handler"
      Timeout: 900
      MemorySize: 1024
      Tracing: Active
      Environment:
        Variables:
          MLFLOW_TRACKING_URI: !Ref MlflowTrackingURI
          METRIC_NAMESPACE: !Ref StackName
          LOG_LEVEL: !Ref LogLevel
      LoggingConfig:
        LogGroup: !Ref MLflowLoggerFunctionLogGroup
      Policies:
        - AWSLambdaBasicExecutionRole
        - !Ref LambdaECRAccessPolicy
        - Statement:
            - Effect: Allow
              Action: cloudwatch:PutMetricData
              Resource: "*"
            - Effect: Allow
              Action:
                - sagemaker-mlflow:*
              Resource: !Ref MlflowTrackingURI
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !Ref CustomerManagedEncryptionKeyArn
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:PutObjectAcl
              Resource: !Sub "arn:${AWS::Partition}:s3:::sagemaker-${AWS::Region}-${AWS::AccountId}/mlflow-artifacts/*"


  ##########################################################################
  # Unified Document Processing State Machine
  # Routes between BDA and step-by-step pipeline based on use_bda config
  ##########################################################################

  DocumentProcessingStateMachine:
    Type: AWS::Serverless::StateMachine
    Properties:
      PermissionsBoundary: !If [HasPermissionsBoundary, !Ref PermissionsBoundaryArn, !Ref AWS::NoValue]
      Name: !Sub "${AWS::StackName}-DocumentProcessingWorkflow"
      DefinitionUri: statemachine/workflow.asl.json
      DefinitionSubstitutions:
        # Partition for service-integration ARNs (arn:${Partition}:states:::...).
        # Hardcoded 'aws' breaks in GovCloud — Step Functions validates the
        # partition and fails states like PostOcrHook with "resource belongs to a
        # different partition ... Expected 'aws-us-gov', was 'aws'".
        Partition: !Ref AWS::Partition
        # BDA branch functions
        InvokeBDALambdaArn: !GetAtt InvokeBDAFunction.Arn
        BDAProcessResultsLambdaArn: !GetAtt BDAProcessResultsFunction.Arn
        WorkingBucket: !Ref WorkingBucket
        # Pipeline branch functions
        OCRFunctionArn: !GetAtt OCRFunction.Arn
        ClassificationFunctionArn: !GetAtt ClassificationFunction.Arn
        ExtractionFunctionArn: !GetAtt ExtractionFunction.Arn
        ShardRuntimeFunctionArn: !GetAtt ShardRuntimeFunction.Arn
        AssessmentFunctionArn: !GetAtt AssessmentFunction.Arn
        PipelineProcessResultsLambdaArn: !GetAtt ProcessResultsFunction.Arn
        RuleValidationPolicyClassificationLambdaArn: !GetAtt RuleValidationPolicyClassificationFunction.Arn
        RuleValidationLambdaArn: !GetAtt RuleValidationFunction.Arn
        RuleValidationOrchestrationLambdaArn: !GetAtt RuleValidationOrchestrationFunction.Arn
        # Shared tail functions
        SummarizationLambdaArn: !GetAtt SummarizationFunction.Arn
        EvaluationLambdaArn: !GetAtt EvaluationFunction.Arn
        # Pipeline-hooks dispatcher — invoked by the SFN at each extension
        # point. Returns immediately when no hooks are registered.
        PipelineHooksDispatcherLambdaArn: !GetAtt PipelineHooksDispatcherFunction.Arn
        OutputBucket: !Ref OutputBucket
      Logging:
        Level: ALL
        IncludeExecutionData: true
        Destinations:
          - CloudWatchLogsLogGroup: !GetAtt StateMachineLogGroup.Arn
      Tracing:
        Enabled: !Ref EnableXRayTracing
      Policies:
        # BDA branch
        - LambdaInvokePolicy:
            FunctionName: !Ref InvokeBDAFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref BDAProcessResultsFunction
        # Pipeline branch
        - LambdaInvokePolicy:
            FunctionName: !Ref OCRFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref ClassificationFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref ExtractionFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref ShardRuntimeFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref AssessmentFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref ProcessResultsFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref RuleValidationPolicyClassificationFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref RuleValidationFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref RuleValidationOrchestrationFunction
        # Shared tail
        - LambdaInvokePolicy:
            FunctionName: !Ref SummarizationFunction
        - LambdaInvokePolicy:
            FunctionName: !Ref EvaluationFunction
        # Pipeline-hooks dispatcher — single SFN Task per hook point.
        - LambdaInvokePolicy:
            FunctionName: !Ref PipelineHooksDispatcherFunction
        - CloudWatchLogsFullAccess
        # Nested Distributed Map (Phase 3 sharded extraction): the state machine
        # starts/manages its own child executions for the inline Distributed Map
        # and reads/writes map item batches in the working bucket.
        - Statement:
            - Effect: Allow
              Action:
                - states:StartExecution
                - states:DescribeExecution
                - states:StopExecution
              Resource:
                - !Sub "arn:${AWS::Partition}:states:${AWS::Region}:${AWS::AccountId}:stateMachine:${AWS::StackName}-DocumentProcessingWorkflow"
                - !Sub "arn:${AWS::Partition}:states:${AWS::Region}:${AWS::AccountId}:execution:${AWS::StackName}-DocumentProcessingWorkflow/*"
            - Effect: Allow
              Action:
                - s3:GetObject
                - s3:PutObject
                - s3:ListBucket
              Resource:
                - !Sub "arn:${AWS::Partition}:s3:::${WorkingBucket}"
                - !Sub "arn:${AWS::Partition}:s3:::${WorkingBucket}/*"

  StateMachineLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      KmsKeyId: !Ref CustomerManagedEncryptionKeyArn
      LogGroupName: !Sub "/aws/vendedlogs/states/${AWS::StackName}/workflow"
      RetentionInDays: !Ref LogRetentionDays


  ##########################################################################
  # CloudWatch Dashboard
  ##########################################################################

  Dashboard:
    Type: AWS::CloudWatch::Dashboard
    Properties:
      DashboardName: !Sub "${AWS::StackName}-${AWS::Region}-Unified"
      DashboardBody: !Sub |
        {
          "widgets": [
            {
              "type": "metric",
              "x": 0,
              "y": 0,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Documents per Minute"}],
                  ["${StackName}", "InputDocuments", {"id": "m1", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Input Documents (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 0,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Pages per Minute"}],
                  ["${StackName}", "InputDocumentPages", {"id": "m1", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Input Document Pages (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 0,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "OCR Throttles per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "OCR Non-Retryable Errors per Minute"}],
                  ["${StackName}", "OCRThrottles", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "OCRNonRetryableErrors", {"id": "m2", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "OCR Throttling & Errors (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 6,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Tokens per Minute (TPM)"}],
                  [{"expression": "m2/PERIOD(m1)*60", "label": "Cache Read TPM"}],
                  [{"expression": "m3/PERIOD(m1)*60", "label": "Cache Write TPM"}],
                  ["${StackName}", "InputTokens", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "CacheReadInputTokens", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "CacheWriteInputTokens", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Input Tokens (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 6,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Tokens per Minute (TPM)"}],
                  ["${StackName}", "OutputTokens", {"id": "m1", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Output Tokens (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 6,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Tokens per Minute"}],
                  ["${StackName}", "TotalTokens", {"id": "m1", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Total Tokens (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 12,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Total per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "Succeeded per Minute"}],
                  [{"expression": "m3/PERIOD(m3)*60", "label": "Failed per Minute"}],
                  ["${StackName}", "BedrockRequestsTotal", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "BedrockRequestsSucceeded", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "BedrockRequestsFailed", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Bedrock Request Status (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 12,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Throttles per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "Retry Success per Minute"}],
                  [{"expression": "m3/PERIOD(m3)*60", "label": "Max Retries Exceeded per Minute"}],
                  ["${StackName}", "BedrockThrottles", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "BedrockRetrySuccess", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "BedrockMaxRetriesExceeded", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "Bedrock Retries (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 12,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["${StackName}", "BedrockRequestLatency", {"stat": "Average"}],
                  [".", "BedrockRequestLatency", {"stat": "p90"}],
                  [".", "BedrockRequestLatency", {"stat": "Maximum"}],
                  [".", "BedrockTotalLatency", {"stat": "Average"}],
                  [".", "BedrockTotalLatency", {"stat": "p90"}],
                  [".", "BedrockTotalLatency", {"stat": "Maximum"}]
                ],
                "region": "${AWS::Region}",
                "title": "Bedrock Latency - per request, and total (including backoff/retries)",
                "period": 300,
                "view": "timeSeries",
                "stacked": false,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 18,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${OCRFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "OCR Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 18,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${ClassificationFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "Classification Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 18,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${ExtractionFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "Extraction Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 24,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${AssessmentFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "Assessment Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 24,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${SummarizationFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "Summarization Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 24,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${OCRFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "OCR Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 24,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${OCRFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "OCR Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 30,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${ClassificationFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Classification Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 30,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${ClassificationFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Classification Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 36,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${ExtractionFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Extraction Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 36,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${ExtractionFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Extraction Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 42,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${AssessmentFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Assessment Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 42,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${AssessmentFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Assessment Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 48,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${SummarizationFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Summarization Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 48,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${SummarizationFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Summarization Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 24,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${RuleValidationFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "Rule Validation Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 54,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${RuleValidationFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Rule Validation Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 54,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${RuleValidationFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "Rule Validation Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 60,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Total per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "Succeeded per Minute"}],
                  [{"expression": "m3/PERIOD(m3)*60", "label": "Failed per Minute"}],
                  ["${StackName}", "BDARequestsTotal", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "BDARequestsSucceeded", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "BDARequestsFailed", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "BDA API Request Status (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 8,
              "y": 60,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Throttles per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "Retry Success per Minute"}],
                  [{"expression": "m3/PERIOD(m3)*60", "label": "Max Retries Exceeded per Minute"}],
                  ["${StackName}", "BDARequestsThrottles", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "BDARequestsRetrySuccess", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "BDARequestsMaxRetriesExceeded", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "BDA API Request Retries (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 60,
              "width": 8,
              "height": 6,
              "properties": {
                "metrics": [
                  [{"expression": "m1/PERIOD(m1)*60", "label": "Total per Minute"}],
                  [{"expression": "m2/PERIOD(m2)*60", "label": "Succeeded per Minute"}],
                  [{"expression": "m3/PERIOD(m3)*60", "label": "Failed per Minute"}],
                  ["${StackName}", "BDAJobsTotal", {"id": "m1", "stat": "Sum", "visible": false}],
                  [".", "BDAJobsSucceeded", {"id": "m2", "stat": "Sum", "visible": false}],
                  [".", "BDAJobsFailed", {"id": "m3", "stat": "Sum", "visible": false}]
                ],
                "region": "${AWS::Region}",
                "title": "BDA Job Executions (per Minute)",
                "view": "timeSeries",
                "period": 60,
                "yAxis": {
                  "left": {
                    "label": "Count per Minute"
                  }
                }
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 66,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${BDAProcessResultsFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "ProcessResults Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "metric",
              "x": 16,
              "y": 66,
              "width": 12,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Duration", "FunctionName", "${InvokeBDAFunction}"]
                ],
                "region": "${AWS::Region}",
                "title": "InvokeBDAFunction Function Duration",
                "period": 300,
                "annotations": {
                  "horizontal": [{
                    "value": ${ExecutionTimeThresholdMs},
                    "label": "Threshold (${ExecutionTimeThresholdMs}ms)",
                    "color": "#ff0000"
                  }]
                },
                "stat": "Average",
                "view": "timeSeries"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 72,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${InvokeBDAFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "InvokeBDA Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 72,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${InvokeBDAFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "InvokeBDA Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 0,
              "y": 78,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${BDAProcessResultsFunctionLogGroup}' | filter @type = \"REPORT\" | fields @timestamp, @logStream, @billedDuration, @maxMemoryUsed/1024/1024 as memoryUsedMB | filter @billedDuration > ${ExecutionTimeThresholdMs} | sort by @billedDuration desc | limit 20",
                "region": "${AWS::Region}",
                "title": "ProcessResults Lambda Long Running Invocations",
                "view": "table"
              }
            },
            {
              "type": "log",
              "x": 12,
              "y": 78,
              "width": 12,
              "height": 6,
              "properties": {
                "query": "SOURCE '${BDAProcessResultsFunctionLogGroup}' | fields @timestamp, @logStream, @message | filter @message like /ERROR/ or @message like /Task timed out/ | parse @message /RequestId: (?<requestId>[^ ]*)/ | sort @timestamp desc | limit 20",
                "region": "${AWS::Region}",
                "title": "ProcessResults Lambda Errors",
                "view": "table"
              }
            },
            {
              "type": "metric",
              "x": 0,
              "y": 84,
              "width": 24,
              "height": 6,
              "properties": {
                "metrics": [
                  ["AWS/Lambda", "Throttles", "FunctionName", "${OCRFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${ClassificationFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${ExtractionFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${AssessmentFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${SummarizationFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${EvaluationFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${RuleValidationFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${InvokeBDAFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${BDAProcessResultsFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${BDACompletionFunction}", {"stat": "Sum"}],
                  [".", ".", ".", "${ProcessResultsFunction}", {"stat": "Sum"}]
                ],
                "region": "${AWS::Region}",
                "title": "Lambda Concurrency Throttles (429) - All Functions",
                "view": "timeSeries",
                "period": 60,
                "stacked": true,
                "yAxis": {
                  "left": {
                    "label": "Throttle Count"
                  }
                }
              }
            }
          ]
        }

Outputs:

  StateMachineName:
    Description: Step Functions State machine Name
    Value: !GetAtt DocumentProcessingStateMachine.Name

  StateMachineArn:
    Description: Step Functions State machine ARN
    Value: !GetAtt DocumentProcessingStateMachine.Arn

  StateMachineLogGroup:
    Description: Step Functions State machine LogGroup
    Value: !Ref StateMachineLogGroup

  PatternLogGroups:
    Description: Comma-separated list of all pattern-specific log groups
    Value: !Join
      - ","
      - # BDA branch log groups
        - !Ref InvokeBDAFunctionLogGroup
        - !Ref BDAProcessResultsFunctionLogGroup
        - !Ref BDACompletionFunctionLogGroup
        # Pipeline branch log groups
        - !Ref OCRFunctionLogGroup
        - !Ref ClassificationFunctionLogGroup
        - !Ref ExtractionFunctionLogGroup
        - !Ref AssessmentFunctionLogGroup
        - !Ref ProcessResultsFunctionLogGroup
        - !Ref RuleValidationFunctionLogGroup
        - !Ref RuleValidationOrchestrationFunctionLogGroup
        # Shared log groups
        - !Ref SummarizationFunctionLogGroup
        - !Ref EvaluationFunctionLogGroup
        - !Ref TestExecutionAggregationFunctionLogGroup
        - !Ref StateMachineLogGroup

  DashboardName:
    Description: Name of the Unified CloudWatch Dashboard
    Value: !Sub "${AWS::StackName}-${AWS::Region}-Unified"

  DashboardArn:
    Description: ARN of the Unified CloudWatch Dashboard
    Value: !Sub "arn:${AWS::Partition}:cloudwatch::${AWS::AccountId}:dashboard/${AWS::StackName}-${AWS::Region}-Unified"

  TestExecutionAggregationFunctionArn:
    Description: ARN of the Test Execution Aggregation Function
    Value: !GetAtt TestExecutionAggregationFunction.Arn

  MLflowLoggerFunctionArn:
    Condition: IsMLflowEnabled
    Description: ARN of the MLflow Logger Function
    Value: !GetAtt MLflowLoggerFunction.Arn
